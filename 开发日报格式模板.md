# 开发日报格式模板

> 本文件定义 `开发日报.md` 的规范格式。以后每天写日报、或重构旧日报，都按这个格式来。
> 核心原则：**同类问题合并成一个任务，减少任务数，让内容更框架化、更集中。**

## 层级规范

| 层级 | Markdown | 用途 | 示例 |
|------|----------|------|------|
| 文档标题 | `#` | 整个文件的标题，只有一个 | `# RISC-V WebSoC 开发日报` |
| 日期 | `#` | 每天一个 H1 | `# 2026-08-13` |
| 任务 | `##` | 每天 N 个任务 | `## 任务 2: 丢包根因定位 ✅ 站点 A 确认` |
| 小节 | `###` | 任务内的固定分节 | `### 目标` |

> 关键点：**小节用真正的 `###` 标题，不要用加粗 `**背景:**`**。这样编辑器大纲（outline）能直接折叠/跳转。

> 标题规范：**小节标题一律不用 `/`**（写 `### 修改的文件`、`### 待验证`，不要写 `### 修改/创建的文件`）。**同类内容形式统一**：文件清单一律用表格，判断/测量一律用表格，不要一会儿表格一会儿文字。

## 目录

文档顶部放目录，用锚点链接到每个日期和任务（GitHub 锚点规则：小写、去标点、空格转 `-`）。

**日期块倒序排列**：最新日期在最上面（`2026-08-20` 在 `2026-08-19` 上面），往下依次递减。

**任务编号**：每个日期下从「任务 1」重新编号。把某个知识点挪到新的一天时，重命名为「任务 1」并同步改目录锚点。

## 跨笔记链接（引用其它笔记）

正文里引用别的笔记时，用 Markdown 链接 `[文件名](文件名.md)`（**带 `.md` 后缀**），这样在 Obsidian、GitHub、VS Code 里都能点击跳转：

- 引用学习笔记写 `[网络协议学习计划](网络协议学习计划.md)`，而不是写成 Obsidian 双链 `[[网络协议学习计划]]`（GitHub 上点不动，只显示纯文本）
- 想改显示文字用 `[自定义显示名](网络协议学习计划.md)`

## 库与同步（Obsidian vault）

笔记库（vault）= `/home/zhihuiw/fpga_work/DOC/`，用 Obsidian「打开本地库」选这个文件夹。Markdown 链接 `[网络协议学习计划](网络协议学习计划.md)` 在 Obsidian 和 GitHub 都能点击跳转。

7 个「镜像笔记」同时存在于 `RiscV_WebSoC_3/` 与 `DOC/`，两边内容需一致：`16-RiscV_WebSoC_3.md`、`ILA调试过程与问题记录.md`、`mattpocock_skills_参考手册.md`、`开发日报.md`、`开发日报格式模板.md`、`网络协议学习计划.md`、`项目状态_20260807.md`

**同步**：改了任一边后跑 `bash /home/zhihuiw/fpga_work/DOC/sync_notes.sh`，双向 rsync（新覆盖旧）。

**新增镜像笔记**：在 `sync_notes.sh` 的 `NOTES` 数组里加一行文件名。

**自动上传到两个 git 仓库**：`开发日报.md` 等 7 个镜像笔记同时被两个仓库跟踪——`zhihui322wang/fpga`（源在 `RiscV_WebSoC_3/`）和 `zhihui322wang/notes`（源在 `DOC/`）。每次改完日报按固定三步收尾，保证改动「上传到正确位置」：

```bash
# ① 双向同步（先让两边内容一致）
bash /home/zhihuiw/fpga_work/DOC/sync_notes.sh

# ② 推 fpga 仓库
cd /home/zhihuiw/fpga_work/Prj/RiscV_WebSoC_3
git add 开发日报.md
git commit -m "日报: <一句话改动>"
git push

# ③ 推 notes 仓库
cd /home/zhihuiw/fpga_work/DOC
git add 开发日报.md
git commit -m "日报: <一句话改动>"
git push
```

> 陷阱：两个仓库各要 `git add` 一次。第 ③ 步必须先 `cd` 到 `DOC/`，否则会留在 fpga 目录里提交，`notes` 仓库「nothing to commit」，改动只上传到 fpga 一个仓库。

## 日期块结构

```markdown
# 2026-08-XX

> 状态: 任务A ✅ | 任务B ✅ | 任务C 🔄   ← 一行说清当天进度

## 任务 N: 一句话标题（可带 ✅/❌/🔄 状态）

### 目标
（为什么做这个任务 / 要解决什么问题）

### 进展和结果
（做了什么、结果如何。中间可用加粗小标签 **① ② ③** 分阶段，或用表格）

### 结论
（可选，最终结论）

### 下一步
（可选，后续计划）

### 遇到的问题
（可选，卡点/bug/坑）

### 收获到的经验
（可选，学到的经验教训，编号列出）

### 修改的文件
（可选，文件清单，**固定用表格**）

---
```

> 状态行补充：最后一段可写「下一阶段: ...」表示当天收尾后的下一步计划，例如 `> 状态: MQTT 扩展（应用层）✅ | 下一阶段: 手写 UART + I2C 驱动`。

## 固定小节说明

| 小节 | 何时用 | 内容 |
|------|--------|------|
| `### 目标` | 每个任务必有 | 背景 + 要解决的问题 |
| `### 进展和结果` | 每个任务必有 | 做了什么、结果、表格、代码块 |
| `### 结论` | 有明确结论时 | 一句话结论 |
| `### 下一步` | 有后续计划时 | 下一步动作 |
| `### 待验证` | 有需上板验证项时 | 待验证动作 |
| `### 遇到的问题` | 有卡点时 | 阻塞、bug、坑 |
| `### 收获到的经验` | 有经验教训时 | 编号列表 |
| `### 修改的文件` | 改了文件时 | 文件清单（表格） |

## 合并规则（重要）

**同一个问题的多次迭代，合并成一个任务**，用 `### 进展和结果` 里的 `**① ② ③**` 分阶段叙述，而不是拆成多个任务。

正例：
- 「丢包根因定位」合并：多智能体分析 → 接入信号 → 计数器判定（一条线，一个任务）
- 「UART 门控修复」合并：初版门控 → DTR 重开门根因 → survive-reset 修复

反例（不要这样）：
- 每个阶段拆一个任务（「分析」「接入信号」「判定」拆成 3 个任务）

## 表格和代码块

- 判断类结论用表格：`| 信号 | 结果 | 判定 |`
- 时序/测量用表格：`| 参数 | 测量值 | 结论 |`
- 关键 RTL/脚本改动用代码块（标注「修复前/修复后」）
- 命令用 bash 代码块

## 一个完整示例

```markdown
# 2026-08-13

> 状态: 固件重传 ✅ | 丢包根因定位 ✅ 站点 A | UART 门控修复 ✅ survive-reset

## 任务 2: 丢包根因定位 ✅ 站点 A 确认

### 目标
ping 不通，定位 MAC→FIFO 数据通路上的丢包点。

### 进展和结果

**① 多智能体并行分析（排除 3 个假设）**

| 假设 | 结论 | 关键依据 |
|------|------|---------|
| mac_rx 因 CRC 错丢弃帧 | ❌ 排除 | crc_err 不 gate sop/en/eop |

**② 决定性信号接入 ILA (26→32 探针)**

**③ 计数器判定结果**

| 计数器 | 值 | 判定 |
|---|---|---|
| rx_afifo_full_cnt | 219 | ★ 站点 A 溢出 |

### 结论
**根因 = 站点 A：`gmii2mac` 内部异步 FIFO(16 深)满溢出。**

### 下一步
读 `rtl/gmii2mac.v` → 加深 FIFO(16→64/128)。

### 收获到的经验
1. 计数器要接出来才算数。

### 修改的文件
| 文件 | 修改 |
|------|------|
| `rtl/webserver_cpu_top.v` | NUM_PROBES 26→32 |

---
```
