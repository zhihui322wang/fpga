# 网络协议栈知识点（学到了什么）

> 项目: RISC-V WebSoC (PicoRV32 + XC7A35T + RTL8211F)
> 顺序: 自底向上，跟代码解析顺序一致 —— 以太网 → ARP → ICMP → IP → TCP → HTTP
> 每层讲清「学到了什么」，最后有一节「跨层精髓」把贯穿始终的套路串起来。

---

## 1. 以太网层（c/eth.c）

干两件事：① 读以太网帧的「类型字段」（字节 12-13），看是 0x0800(IP) 还是 0x0806(ARP)，决定这个包丢给谁；② 判断是发给本机的包后，顺手把回包的 MAC 头先写进 TX FIFO（写本机 MAC 当源地址、把对方的 MAC 拷过来当目的地址），这样上层 ip/arp 构造回包时就不用再管 MAC 头了——**"边收边发，省一步"**。

具体看 `eth_proc()`：
- 读 `OFF_ETH_TYPE`（字节 12-13）拼出 16 位类型 → 0x0806 走 ARP，0x0800 走 IP，其他丢弃。
- 走 IP 前先比目的 MAC 是不是本机（`Local_MAC`），不是就 `NO_PROC` 丢弃。
- 匹配通过就调 `eth_write_tx_header()`：源 MAC 填本机、目的 MAC 抄收到的源 MAC、EtherType 照抄。

### 以太网帧结构（图示，一目了然）

软件看到的以太网帧 = 目的MAC + 源MAC + 类型 + 数据。前导码(8B) 和 FCS(4B) 由硬件自动收发，代码不碰：

```
 字节偏移: 0          6          12   14
          ┌──────────┬──────────┬──────┬──────────────────────────────┐
          │  目的MAC  │  源MAC   │ 类型  │         数据 (46~1500)        │
          │   6 字节  │  6 字节  │ 2 字节│                              │
          └──────────┴──────────┴──────┴──────────────────────────────┘
                                        │
                        EtherType 决定「数据」里装的是谁：
                        ├─ 0x0806 → ① ARP 报文 (28 字节)
                        └─ 0x0800 → ②③ IP头(20) + 上层(ICMP/TCP)
```

**① 承载 ARP**（发数据前先问 MAC）：

```
 0          6          12   14                                    42
┌──────────┬──────────┬──────┬──────────────────────────────────────┐
│  目的MAC  │  源MAC   │0x0806│           ARP 报文 (28 字节)           │
└──────────┴──────────┴──────┴──────────────────────────────────────┘
                          ┌─────┬─────┬────┬────┬──────┬────────┬────────┬────────┬────────┐
                          │htype│ptype│hlen│plen│opcode│ sendMAC│ sendIP │ tgtMAC │ tgtIP  │
                          │ 2B  │ 2B  │ 1B │ 1B │  2B  │  6B    │  4B    │  6B    │  4B    │
                          └─────┴─────┴────┴────┴──────┴────────┴────────┴────────┴────────┘
                          14-15 16-17 18   19   20-21  22-27    28-31    32-37    38-41
```

**② 承载 ICMP**（ping 的 Echo 请求/应答）：

```
 0          6          12   14                       34
┌──────────┬──────────┬──────┬────────────────────────┬────────────────────┐
│  目的MAC  │  源MAC   │0x0800│      IP头 (20 字节)     │  ICMP (8 + payload)│
└──────────┴──────────┴──────┴────────────────────────┴────────────────────┘
                          ┌────────┬─────────┬─────────┬──────────────────┐
                          │ver/IHL │protocol │checksum │ srcIP / dstIP    │
                          └────────┴─────────┴─────────┴──────────────────┘
                          14-15    23        24-25     26-29 / 30-33
                                                     ┌────┬────┬────────┬─────┬─────────┐
                                                     │type│code│checksum│id/sq│ payload │
                                                     │ 1B │ 1B │  2B    │ 4B  │   ...   │
                                                     └────┴────┴────────┴─────┴─────────┘
                                                     34   35   36-37    38-41 42-...
```

**③ 承载 TCP**（HTTP 网页）：

```
 0          6          12   14                       34                      54
┌──────────┬──────────┬──────┬────────────────────────┬─────────────────────┬──────────┐
│  目的MAC  │  源MAC   │0x0800│      IP头 (20 字节)     │   TCP头 (20 字节)    │ HTTP数据 │
└──────────┴──────────┴──────┴────────────────────────┴─────────────────────┴──────────┘
                                                        ┌──────┬──────┬───────┐
                                                        │src/dst│  seq │ flags │
                                                        └──────┴──────┴───────┘
                                                        34-35  38-41  47
```

**关键**：三种封装都是「以太网头(14B) 打底，中间按 EtherType 嵌套」，代码里的偏移量 `OFF_IP_SRC_IP=26`、`OFF_TCP_SEQ_NUM=38` 就是一层层头长累加出来的：
- ARP 字段偏移 = **14** + 相对偏移
- ICMP/TCP 字段偏移 = **14 + 20(IP头)** + 相对偏移

---

## 2. ARP 层（c/arp.c）

干一件事：回答「这个 IP 的 MAC 是谁」。收到 ARP 请求（对方在问「169.254.1.1 的 MAC 是啥？」），把请求里「问话人 / 被问人」的身份字段对调重填，凑出一个 ARP 应答原路送回。

前置过滤（arp.c:22-41），三个条件都满足才处理：
1. 目的 MAC 是本机 **或** 全 FF（广播）——请求是广播来的，必须认广播；
2. target IP 必须等于本机 IP——人家问的不是我就不答；
3. opcode 必须是 REQUEST(1)——只应答请求。

构造应答分 9 段（arp.c:44-105），本质是「对调重填」：

| 段 | TX 字段 | 来源 |
|----|---------|------|
| 1 | 目的 MAC（0-5） | RX 的源 MAC（6-11） |
| 2 | 源 MAC（6-11） | 本机 `Local_MAC` |
| 3 | EtherType（12-13） | 照抄 0x0806 |
| 4 | htype/ptype/hlen/plen（14-19） | 照抄 |
| 5 | opcode（20-21） | 改成 REPLY(2) |
| 6 | sender MAC（22-27） | 本机 MAC |
| 7 | sender IP（28-31） | 本机 IP |
| 8 | target MAC（32-37） | RX 的 sender MAC |
| 9 | target IP（38-41） | RX 的 sender IP |

最后补 0 到 64 字节 push。

**精髓：广播问、单播答**——问的时候不知道对方 MAC，只能广播；答的时候已经知道问话人的 MAC 了，直接单播回去。

### 为什么需要 ARP？

IP 和 MAC 是**两个不同层的地址**：

- **IP 是逻辑地址**：跨网络寻址用，路由器按 IP 逐跳转发，是「你在哪」的抽象；
- **MAC 是物理地址**：局域网内投递用，以太网帧头只认 MAC，是「你的网卡是谁」。

发数据时，上层只知道对方的 **IP**，但以太网帧头必须填对方的 **MAC**。中间缺一环「IP → MAC」的翻译，ARP 就是补这一环的。

一句话：**ARP 是「IP → MAC」的翻译官，工作在同一个局域网（广播域）内**。跨网段的请求不走 ARP，走路由器转发。

### ARP 保证网络通畅的基本原理

核心四步：**广播问、单播答、缓存学**。

1. A 要发数据给 B，只知道 B 的 IP、不知道 B 的 MAC；
2. A **广播**一个 ARP 请求：「谁的 IP 是 X.X.X.X？请告诉我你的 MAC」——因为不知道 B 的 MAC，没法单播，只能全网广播吼一嗓子；
3. 链路上所有人都收到，只有 IP = X 的 **B 应答**，别人一看「问的不是我」就丢弃；
4. B **单播**回 ARP 应答：「X.X.X.X 的 MAC 是 Y」——B 已经从请求的 sender MAC 读到 A 的 MAC，所以能直接单播回去。

之后 A 学到 B 的 MAC，填进帧头正常发包。**学到的东西缓存起来，下次发数据不用再广播。**

### ARP 表如何操作

ARP 表（缓存）= 一堆「IP → MAC」映射，每项带一个老化计时器。三种操作：

| 操作 | 干什么 | 触发时机 |
|------|--------|----------|
| **查表** (lookup) | 发数据前先查缓存，命中直接拿 MAC | 每次要发包前 |
| **学习** (learn) | 把包里的 sender IP + sender MAC 记进表 | 收到**任何** ARP 包时（请求或应答都算） |
| **老化** (aging) | 表项超时删除，下次重新 ARP | 计时器到点 |

两个关键细节：
- **免费学习**：收到 ARP 包（哪怕不是发给我的），里面的 sender IP/MAC 也是有效的——对方既然发了 ARP，它的映射就是真的，顺手记下来。
- **为什么要老化**：对方可能换网卡 / DHCP 重分 IP，还拿着旧映射就发错地方了。所以每项设 TTL（几十秒到几十分钟），超时作废，下次重新解析。

**本项目的差异**：WebSoC 是「纯应答服务器」——只被动回 ARP reply，从不主动发 ARP request，所以固件里**没有 ARP 表**。但它回 ARP reply 时填的那堆字段，本质就是「别人要存进自己 ARP 表的那一条表项」。

---

## 3. ICMP 层（c/icmp.c）

干一件事：把收到的 ping（Echo 请求）变成 Echo 应答原样弹回去。**零拷贝流水线**——不把 payload 搬出来再搬回去，直接对着 RX 缓冲区读、往 TX 缓冲区写。

三步（icmp.c:55-97）：
1. 先写 IP 头（`ip_header_update` 交换 src/dst IP 并重算 IP 校验和）；
2. 写 ICMP 头：type 改成 0（ECHO_REPLY）、code 填 0，id/seq 和 payload 原样抄；
3. 算 ICMP 校验和写回，不足 64 字节补 0。

校验和细节（`icmp_body_checksum`）：跳过 type/code/checksum 这 4 字节，从 id/seq 开始逐字节累加；payload 是奇数长度时末尾补 0 凑成偶数再算。

**精髓：ICMP 校验和覆盖「整个报文（头 + 数据）」**，和 IP 头校验和「只校验 20 字节头」是两种覆盖范围，别混。

### 为什么需要 ICMP？

IP 是「尽力投递」（best-effort）：只负责把包送出去，**不保证到达、也不报告失败**。网络出问题（目的不可达、TTL 超时、要换路）时，IP 自己是不会吭声的。

ICMP 补的就是这个「报错 + 诊断」缺口：
- **报错**：目标不可达、超时、重定向……由路由器/主机用 ICMP 回给源主机；
- **诊断**：ping 用的 Echo（回显），探「对方活没活着、往返多快」。

一句话：**IP 只负责送，ICMP 负责「喊一声」——报错和探活**。注意 ping ≠ 整个 ICMP，它只是 ICMP 里最常用的 Echo 功能。

### ping 的原理（Echo 请求 → 应答）

1. 源主机发 **Echo Request**（type=8），payload 塞一串数据 + id/seq 编号；
2. 目标收到后 type 改成 **Echo Reply**（type=0），**payload 原样弹回**（不解析、零拷贝）；
3. 源主机靠 id/seq 匹配「哪个进程的第几个包」，再算 RTT 判断通不通、快不快。

**本项目是纯应答端**：收到 type=8 就回 type=0（icmp.c:69），id/seq/payload 逐字节抄回。

### ICMP 校验和如何算（覆盖范围最关键）

- 覆盖「**整个 ICMP 报文**」（8 字节头 + payload），和 IP 头校验和「只校验 20 字节头」是两种范围，别混。
- 计算时**跳过 type/code/checksum 这 4 字节**，而且 type 用 **ECHO_REPLY(0)** 参与算（不是收到的 8）——因为回包 type 已经改了（icmp.c:34）。
- payload 奇数长度时**末尾补 0** 凑成偶数（icmp.c:47-51）。
- 最后取反 `~sum`（icmp.c:52）。

> 项目细节：`icmp_reply` 里 `tx_pkt_len = eth(14) + ip_total_len + 4`，那个 `+4` 是 FCS（帧尾校验），MAC 会自动加，但 push 长度得先把它算进去。

---

## 4. IP 层（c/ip.c）

干两件事：① 验证这个包是不是发给本机的合法 IPv4 包；② 读出「源 IP」「协议号」「总长度」，决定丢给 ICMP 还是 TCP。

校验（ip.c:10-28）：
- ver/IHL 高 4 位必须 `0x40`（IPv4 + 头长 20 字节，`0x45` 最常见）；
- IHL 低 4 位 >= 5；
- 目的 IP 必须等于本机 `169.254.1.1`。

通过后读三个全局量：`src_ip`（给 TCP 回包用）、`ip_protocol_type`（1=ICMP / 6=TCP / 17=UDP 分派）、`ip_total_len`（给 TCP 算 data_len 用）。

回包用 `ip_header_update`（ip.c:79-106）：复制整个 20 字节 IP 头，但原地替换 4 段——total_len、src_ip（换本机）、dst_ip（换对方 src_ip）、checksum（重算），其余字段照抄。

头校验和（`ip_header_checksum`，ip.c:57-76）：只遍历 20 字节，跳过 checksum 字段本身、替换 total_len，逐字节累加后取反。

**精髓：IP 头 20 字节，校验和「只校验头不校验数据」**（数据交给 TCP/ICMP 各自的校验和）。回包 = 交换 src/dst IP + 重算校验和，其余照抄——和 ARP 的「对调重填」一个套路。

### 为什么 IP 要分片？

IP 要跑在**各种异构链路**上（以太网、PPP、Wi-Fi……），每条链路的 MTU（最大传输单元）不一样——以太网 1500、PPP 可能更小。IP 不能假设沿途所有链路都装得下同一个包，所以设计成：**包太大装不下时切成几片，到目的地再拼回来**。

三个字段配合完成「切片 + 重组」：

| 字段 | 长度 | 作用 |
|------|------|------|
| Identification | 16 bit | 同一原始包的所有分片共用，靠它归组 |
| Flags (DF/MF) | 3 bit | DF=不许分片；MF=后面还有分片 |
| Fragment Offset | 13 bit | 本片数据在原始包里的位置（**单位 8 字节**） |

三个「为什么」：
- **Offset 为什么按 8 字节？** 13 bit 按 1 字节编址最多只能到 8191 字节，装不下 65535 的 IP 包；按 8 字节编址能覆盖 8191×8 = 65528，够用。这是「字段宽度 vs 覆盖范围」的折中。
- **为什么有 DF（Don't Fragment）？** 分片有代价——丢一片整包重传、重组缓冲占资源、还是攻击面。TCP 常设 DF=1 让路由器「别分片，装不下就丢 + 回 ICMP 需要分片」，这正是「路径 MTU 发现」的机制。
- **为什么现代网络尽量不分片？** 分片在源端或中间路由器都能做，但**重组只在目的地做**——中间路由器反复切是纯负担；IPv6 干脆废掉路由分片，只允许源端分。

**本项目**：ip.c 不做分片重组，收到分片包会当成完整包处理（错）。局域网内 ping/HTTP 都是小包、MTU 1500 够用，跑得通；「分片不处理」是它作为极简嵌入式栈的边界。

### 为什么要有 TTL？

核心问题：**路由环路**。路由表出 bug 形成环（A→B→A），一个包会永远在网络里打转、白占带宽。TTL 给每个包设一个「最多跳几跳」的上限，每过一路由器减 1，减到 0 就丢，并回 ICMP「Time Exceeded」给源——**环路里的包转几圈就死，不会永动**。

四个「为什么」：
- **名字叫 Time 实为跳数？** 最早设想按「秒」减（路由器按缓存时长减、至少减 1），但实际路由器不校时、缓存就微秒级，按秒减既难实现又不精确；RFC 1812 规定「每跳至少减 1」，于是 TTL 事实上成了 hop limit，名字沿用没改。
- **为什么减到 0 丢、而不是检查时间？** 跳数判断是确定性的、几乎零成本（一个字节减一），比「过期时间」靠谱得多。
- **TTL 养活了 traceroute**：故意发 TTL=1、2、3…… 的包，靠沿途回传的 ICMP Time Exceeded 一程程描出路径——这正是 ICMP「报错」功能的用途之一。
- **为什么初始 TTL 取 64？** Linux 默认 64、Windows 128——够穿过绝大多数路径（公网最多 ~30 跳），又是 2 的幂好记；副作用是能靠「收到包的 TTL」被动猜对方 OS（64→Linux、128→Windows），成了被动指纹的一种。

**本项目**：WebSoC 是端主机不是路由器，不减 TTL；回包 `ip_header_update` 直接照抄收到的 TTL（不是重设 64）。直连单跳场景 TTL 到不了 0，无碍；但严格说端主机回包应自己重设 TTL。

### 协议号分派为什么这样设计？

IP 头里 8 bit 的「协议号」（偏移 23）回答一件事：**payload 里装的是谁**——1=ICMP、6=TCP、17=UDP…… IP 按它把数据分派给正确的上层处理函数。

这其实和以太网的 EtherType 是**同一个套路**：每层都留一个「类型/选择子」字段指向上层，一层只做一步分派：

| 层 | 字段 | 值 → 分派到 |
|----|------|-------------|
| 以太网 (L2) | EtherType (2B) | 0x0800→IP, 0x0806→ARP |
| IP (L3) | Protocol (1B) | 1→ICMP, 6→TCP, 17→UDP |
| TCP/UDP (L4) | 端口 (2B) | 80→HTTP, 443→HTTPS, 53→DNS |

三个「为什么」：
- **为什么每层都要有分派字段？** 因为分层——以太网不该懂「TCP 还是 UDP」（那是 L4 的事），IP 也不该懂「哪个应用」（那是端口的事）。每层只往上一层看一步，职责单一。
- **为什么是这些数字？** 纯历史：ICMP(1)、TCP(6) 出自最早的 RFC 790(1981)，UDP(17) 稍后，由 IANA 统一登记，没特殊含义。
- **为什么 8 bit？** 256 个协议号够用，比 EtherType 的 16 bit 窄一档——L3 只需区分「协议」这一级，不用管应用。

**本项目**：ip.c 读 `ip_protocol_type`（偏移 23）分派 1→icmp.c、6→tcp.c（17=UDP 没实现）。这和 eth.c 读 EtherType 分派 arp/ip 是同一招——「读类型字段，交给对应上层」。

### 为什么 IP 校验和只算头、还要「清零自己 + 先替换 total_len」？

这是 ip.c 里最「讲究」的一处（`ip_header_checksum`，ip.c:57-76），三个操作各对应一个设计理由：

1. **为什么只算 20 字节头、不算数据？** TTL 每过一路由器减 1；若校验和覆盖数据，每个路由器都得把整个包（可能几 KB）重新累加一遍，代价太高。只算头，路由器改完 TTL 后做 O(1) 增量更新即可。数据完整性不靠 IP——TCP/UDP/ICMP 各自有「伪首部 + 头 + 数据」的校验和，IP 只需保证「路由决策用到的头」没坏。
2. **为什么先把 checksum 字段自己清零再算？**（ip.c:66）校验和定义是「两两拼 16 bit → 二进制反码求和 → 取反」。算的时候 checksum 那两字节还没值（鸡生蛋），先当 0 参与求和；验证端把「含 checksum 字段在内的 20 字节」再求一次和，应得 0xFFFF——「自己加自己的反码恒等于全 1」，这就是「取反」的妙处。
3. **为什么回包时要「替换 total_len」再算？**（ip.c:67-68）TCP 应答的 payload 长度常和请求不同，total_len 变了；而 total_len 在 IP 头里、被校验和覆盖，所以必须按「新 total_len」注入再求和，不能照抄收到的值。`ip_header_checksum(total_len, 0)` 就是把新值塞进对应偏移再算。

**本项目**：`ip_header_checksum` 里三个 if 分支（`i==OFF_IP_CHECKSUM` 清零、`i==OFF_IP_TOTAL_LEN` 替换、其余逐字节求和）就是上面三点的直接落地——嵌入式栈「手算校验和」的标准写法。

---

## 5. TCP 层（c/tcp.c）—— 最复杂的一层

干三件事：① 解析 TCP 头；② 用一张「连接表」记住每条连接的状态；③ 按状态机走完握手 → 传数据 → 挥手。

**头解析**（`tcp_parse_header`）：源/目的端口、seq、ack、DataOffset（高 4 位 × 4 = 头长）、flags、window，再算 `data_len = IP总长 - IP头 - TCP头`。

**连接表**（`conn_table[4]`）：每条连接记 state / remote_ip / remote_port / local_seq / remote_seq / retry_count / timeout。这是「面向连接」的落地——同一个 socket 的多个包靠这张表关联起来。

**三次握手**（`tcp_handle_listen` + `tcp_handle_syn_received`）：
1. 收 SYN → 分配空槽位，记 remote_ip/port/seq → 回 SYN+ACK（ACK = 对方 seq+1）；
2. 收 ACK → 转 ESTABLISHED，本机 `local_seq+1`（自己发的 SYN 也占一个序号）。

**校验和伪首部**（`tcp_calc_checksum_payload`）：12 字节伪首部（源IP + 目的IP + 0 + 协议号6 + TCP长度）虽然不实际发送，但算校验和时要一起算——这样 TCP 能顺带校验 IP 地址对不对，防止 IP 层投错包。

**状态机 7 状态**：CLOSED / LISTEN / SYN_RECEIVED / ESTABLISHED / CLOSE_WAIT / LAST_ACK / TIME_WAIT，`tcp_proc` 按状态 switch 分发。

**四次挥手**：收 FIN → 回 FIN+ACK → 进 LAST_ACK → 收 ACK → 释放槽位。

**超时重传**（`tcp_timer_check`）：用 `rdcycle` 读硬件计时器，SYN+ACK 发出后超时没收到 ACK 就重发，最多 3 次，还不行就释放槽位。

**精髓**：
- **序号 = 字节流的位置指针**，ACK = 期望收到的下一个字节；SYN/FIN 没数据也各占 1 个序号，所以 ACK = 对方 seq+1。
- **三次握手是为了双向同步初始序号**——双方都要告诉对方「我从多少号开始发」。
- 三种校验和覆盖范围要分清：**IP 只头 / ICMP 头+数据 / TCP 伪首部+头+数据**。

### 为什么要有滑动窗口？

TCP 在不可靠网络上送「字节流」。如果发一个字节等一个 ACK（停等协议 stop-and-wait），吞吐率会被 RTT 卡死——发一段等一个来回，带宽再高也白搭。滑动窗口让发送方**不必等每个 ACK**，可以一口气把「窗口内」的字节都发出去（流水线 pipelining），ACK 一到窗口就往前滑。

| 概念 | 是什么 |
|------|--------|
| 接收窗口 rwnd | 接收方还剩多少缓冲，由 TCP 头的 Window 字段(16bit) 通告 |
| 在途字节 | 已发未确认的字节数，不能超过 rwnd |
| 滑动 | ACK 到达 → 左边界右移 → 又能发新数据 |

两个「为什么」：
- **为什么窗口是 16 bit？** 65535 字节 ≈ 64KB；高带宽延迟积链路（带宽×RTT）需要更大，于是有窗口缩放选项（RFC 1323），最大扩到 ~1GB。这是「字段宽度 vs 大带宽」的又一次折中。
- **为什么 ACK 号 = 期望的下一个字节？** ACK = 左边界 = 已经按序收到的字节数，天然的「滑动指针」；这也解释了 SYN 占一个序号（否则握手序号和 ACK 对不上）。

**滑动窗口 = 流量控制**：保护的是**接收方**（别把对方缓冲灌满）。它只回答「你还能收多少」，不回答「网络堵不堵」——那是拥塞控制的事。

### 为什么要有拥塞控制？

流量控制只保护接收方，但**网络本身（路由器队列）也有上限**。很多发送方一起猛发，路由器队列溢出 → 大量丢包 → 全网瘫痪（1986 年互联网「拥塞崩溃」就是这么来的）。拥塞控制让每个发送方**根据网络状况自我限速**，用「拥塞窗口 cwnd」表示「网络现在容得下我发多少」。

有效发送窗口 = `min(接收窗口 rwnd, 拥塞窗口 cwnd)`。

经典算法（TCP Tahoe/Reno）：
- **慢启动 Slow Start**：cwnd 从 1 MSS 起，每 RTT **翻倍**（指数增长）到阈值；
- **拥塞避免 Congestion Avoidance**：过阈值后每 RTT **+1 MSS**（线性增长）；
- **丢包反应**：超时 → cwnd 归 1 重新慢启动；3 个重复 ACK（快重传）→ cwnd 减半、进快恢复（Reno）。

三个「为什么」：
- **为什么叫「慢启动」却指数涨？** 它从很小(1 MSS)「慢慢起步」，但每 RTT 翻倍其实涨得飞快——名字说的是起点低，不是说涨得慢。这是个经典的名不副实。
- **为什么是 AIMD（加性增、乘性减）？** 加性增(每 RTT +1) 慢慢试探、乘性减(丢包减半) 快速退让，全网所有发送方都这么做，就能收敛到**公平共享**瓶颈带宽——不需要中央调度，这是分布式算法的优雅之处。
- **为什么有效窗口取 min(rwnd, cwnd)？** 两个约束都要满足：既不能灌爆接收方，也不能堵死网络。

**本项目**：tcp.c 没有拥塞控制——局域网小服务器发一个 HTTP 响应就 FIN，永远触发不了拥塞。但和它对打的主机 Linux 协议栈**会**做拥塞控制，抓包能看到主机的慢启动行为。

### 为什么序号会回绕（wraparound）？

TCP 序号是 32 bit（0 ~ 2³²-1 ≈ 42.9 亿），标记字节流位置。但 2³² ≈ 4GB，在 10Gbps 链路上 **~3.4 秒就转一圈**，序号空间用完重头来——这就是「回绕」。

问题与对策：
- **老段混淆**：网络里滞留的旧分片可能和新段撞号 → 用 PAWS（RFC 1323，时间戳）判「这个段是不是太老了」，太老直接丢。
- **ISN 要随机**：初始序号固定(如 0) 会让攻击者预测序号注入伪造段（TCP 劫持），现代栈用随机 ISN（RFC 6528）。

**本项目**：tcp.c 用固定 ISN=0x10000000——开发调试阶段固定值的好处是 Wireshark 一眼认出自己的包；真实栈才需要随机 ISN 防回绕混淆 + 防劫持。

---

## 6. HTTP 层（c/http.c）

最薄的一层——前面四层（eth/ip/tcp）已经把「可靠的字节流」铺好了，HTTP 只需要关心「这些字节是什么文本」。

**判 GET**（http.c:20-31）：读 TCP payload 前 3 字节，是 `"GET"` 就回网页，不是就只回 FIN 关连接。

**响应**（http.c:6-14）：一段写死的字符串——状态行 `HTTP/1.1 200 OK` + 头 `Content-Type`/`Connection: close` + 空行 + HTML body。

**关键点**（http.c:34-38）：FIN 和 DATA 合并成一个包发（piggyback），flags = ACK|PSH|FIN，避免 DATA 和 FIN 背靠背两个包导致丢包。

**精髓：HTTP 是文本协议，人能直接读**；头与 body 用空行 `\r\n\r\n` 分隔；`Connection: close` 表示短连接——响应完就关（所以挥手流程会完整走一遍）。

### 为什么 HTTP 是文本协议？

HTTP/1.x 的状态行、头都是 ASCII 文本，好处与代价各一个：

- **好处——可调试**：人用 telnet/nc 能直接手敲请求，抓包里一眼读出内容，不用对着十六进制猜。这对开发调试价值巨大。
- **代价——费带宽 + 解析慢**：全文本比二进制多花字节；解析要逐字节比字符串（http.c 里 `a=='G' && b=='E' && c=='T'`）。

取舍：HTTP/1.x 选「好调试」、牺牲「省带宽」；HTTP/2 改回二进制帧（保留 1.1 语义）说明「文本 vs 二进制」是不同阶段的取舍，不是谁对谁错。

### 为什么极简服务器只要 GET 就够？

HTTP 方法有 8 个（GET/POST/PUT/DELETE/HEAD/OPTIONS/PATCH/CONNECT），方法字段本质是「对资源的**动作**」：GET=读、POST=提交、DELETE=删。

本项目服务器只有一个职责——把写死的 HTML 页面吐给浏览器，没有上传、没有增删改，所以**只需要 GET**。http.c 甚至不解析完整方法名，只比前 3 字节 `GET`（http.c:28）——「只实现要用的那一小撮」，是极简嵌入式栈的典型。

### 为什么状态码按「百位」分层？

状态码 3 位，首位就告诉你「问题归谁」：

| 首位 | 含义 | 例 |
|------|------|-----|
| 2xx | 成功 | 200 OK |
| 3xx | 重定向 | 301/302 |
| 4xx | **客户端**错 | 404 Not Found |
| 5xx | **服务器**错 | 500 |

「谁的问题」这一层语义让机器看数字分流、人看 reason phrase 看懂。本项目只回 200 OK 和 FIN（拒绝），连 404 都不回——极简边界。

### 为什么 Connection: close（短连接）？

HTTP/1.0 默认短连接：**一个请求 = 一条 TCP 连接，响应完就关**（FIN 走完整挥手）。

- 好处：服务器极简——不用维护 keep-alive 连接池、不用靠 Content-Length 判断 body 在哪结束（关连接本身就是「响应结束」信号）。
- 代价：每个请求都重新三次握手 + 四次挥手；HTTP/1.1 改 keep-alive 持久连接，Content-Length/chunked 就是为「不关连接也能知道 body 结束」而生。

本项目写死 `Connection: close`（http.c:9）——正好配合「响应完就 FIN」，挥手流程完整走一遍（也是之前 HTTP 必须 FIN piggyback 的根因）。

---

## 7. 跨层精髓（贯穿始终的套路）

1. **偏移量就是协议栈的"地图"**：`OFF_ETH_TYPE=12`、`OFF_IP_SRC_IP=26`、`OFF_TCP_SEQ_NUM=38`…… 每个字段在帧里的位置都用宏定义好了，解析就是"按图索骥"，代码里几乎看不到魔数。

2. **分层思想**：eth 只认"以太类型"这一件事，具体是 IP 还是 ARP 它不关心，丢给上层。每层职责单一，改哪层不影响别的层。

3. **回包套路统一 =「对调重填」**：ARP 对调问话人/被问人，IP 对调 src/dst，TCP 对调端口和 seq/ack——都是「把请求里的身份字段换位、其余照抄、重算校验和」。

4. **网络字节序 = 大端**：发多字节字段时先发高位字节（`tcp_wr32` 先 `>>24` 写第一个字节），收时先读高位左移拼接。

5. **三种校验和覆盖范围要分清**：IP 只校验 20 字节头；ICMP 校验整个报文（头+数据）；TCP 校验伪首部 + 头 + 数据。

---

## 8. 收尾：六层「为什么这样设计」一张表

从下到上，每层解决一个「没有它就不行」的问题，也各留一个「选择子」字段指向上层：

| 层 | 解决的核心问题 | 关键设计 | 分派字段 |
|----|---------------|---------|---------|
| 以太网 eth.c | 同网段帧怎么送到目标 MAC | 6B 源/目 MAC + CRC | EtherType (0x0800/0x0806) |
| ARP arp.c | 已知 IP 怎么拿 MAC | 广播问、单播答、缓存表 | 操作码 (1问/2答) |
| ICMP icmp.c | 网络出问题谁报错 | Echo/Reply + 校验全报文 | Type/Code |
| IP ip.c | 跨网段路由 + 防环路 | 分片 / TTL / 只校验头 | Protocol (1/6/17) |
| TCP tcp.c | 不可靠 IP 上造可靠字节流 | 滑动窗口 / 拥塞控制 / 序号 | 端口 (80/443/53) |
| HTTP http.c | 字节流怎么表达「要什么」 | 文本 + 方法 + 状态码 | 方法 (GET/POST...) |

**一条贯穿主线**：每层「只做一步、往上交一步」——eth 认 EtherType、ip 认 Protocol、tcp 认端口、http 认方法，职责单一、可替换。这就是「分层」能撑起整个互联网的原因。

**本项目边界（极简嵌入式栈的取舍）**：ip 不分片、tcp 不拥塞控制、http 只 GET——「直连局域网 + 小包 + 只读网页」前提下，把每层砍到只剩要用的那一小撮；跨网、大包、并发、持久连接才需要补全。

**下一步**：网络协议栈学习收尾 ✅，开始手写 **UART** 和 **I2C** 驱动代码。

---

## 9. 扩展：如果再往上加 MQTT（对应哪一层）

MQTT（Message Queuing Telemetry Transport）和 HTTP 是**同一层的兄弟协议**——都跑在 TCP 之上的**应用层（L7）**，不是新的一层。当前工程里对应 `http.c` 这一层。

分派方式和 HTTP 完全同一个套路——TCP 按「端口」这个选择子字段往上交：

| 端口 | 分派到 |
|------|--------|
| 80 | HTTP |
| 443 | HTTPS |
| 53 | DNS |
| **1883** | **MQTT** |

所以 `ip.c` 不变（还是 `Protocol=6` → tcp.c），只改 TCP 收包处分派：`tcp.c:399` 现在 `tcp_dst_port != HTTP_PORT` 就 RST，改成加一个 `MQTT_PORT(1883)` 分支，`1883 → mqtt.c`、其余才 RST；再加个 `mqtt.c` 实现 CONNECT/SUBSCRIBE/PUBLISH 控制包即可。

**和 HTTP 的取舍对比**（正好是 HTTP 那几个「为什么」的反面）：

| | HTTP | MQTT |
|--|------|------|
| 通信模型 | 请求/响应（一问一答） | 发布/订阅（broker 中转，一对多） |
| 协议形态 | 文本 | 二进制 |
| 连接 | 短连接（或 keep-alive） | 长连接（持久保持） |
| 定位 | 网页/API | IoT 低带宽、小代码设备 |

MQTT 天生给受限设备（传感器、单片机）设计，跟 WebSoC 这种小 SoC 的定位很搭——只是当前工程「网页」需求被 HTTP 满足，还没到需要 pub/sub 的场景。
