# RISC-V WebSoC — 项目当前状态

> 最后更新: 2026-08-13
> 目标: 上板用 fpga_ila 抓 ILA 波形，定位 ping 丢包根因

---

## 一、项目概述

RISC-V WebSoC: PicoRV32 + XC7A35T-FGG484-2 + RTL8211F PHY

- **仿真工具**: iverilog + gtkwave
- **综合工具**: Vivado 2024.1 (本机 `/home/zhihuiw/vivado/Vivado/2024.1`)
- **调试工具**: fpga_ila (软逻辑分析仪, fcapz_ela_enhance 分支)
- **开发板**: ACX750 (XC7A35T), 板载 UART L21/M21, FT232H USB 串口

---

## 二、当前进度总览

| 任务 | 状态 |
|------|------|
| TCP SYN→SYN+ACK 仿真验证 | ✅ PASS |
| ILA 探针扩展 (26 探针, 150bit, 50MHz, 2048 depth) | ✅ 完成 |
| ILA 探针再扩展 (32 探针, 184bit) — 丢包根因探针 | ✅ 完成 2026-08-13 |
| 丢包根因并行分析 (排除 CRC / 位序假设) | ✅ 完成 2026-08-13 |
| ILA 标准模式重构 (soft_ila_top + ila_hub_top) | ✅ 完成 |
| 仿真 VCD 生成修复 (内嵌 TB 冲突) | ✅ 完成 |
| Web GUI 环境搭建 + Bug 修复 | ✅ 完成 |
| Vivado build.tcl 适配 (fpga_ila IP + 引脚) | ✅ 完成 |
| 时序约束修复 (WNS: -1.760→+3.321ns) | ✅ 完成 2026-08-12 |
| Vivado 重编译 (fix timing + ILA) | ✅ 完成 2026-08-12 |
| FPGA 烧录 (2026-08-12) | ✅ 手动烧录成功 |
| ILA Qt GUI 启动 | ✅ 已启动, 端口用 ttyACM0 @ 921600 |
| SIMPLE_INFER 参数 (dual_clock_true_dual_port_ram) | ✅ 完成 |
| updatemem 固件合并 | ⚠️ 弃用 — 改用 UART 上传固件 |
| 固件 UART 上传 (upload_fw.py --offset 0) | ✅ 完成 2026-08-12 |
| CPU 启动运行验证 (ILA 确认) | ✅ 完成 2026-08-12 |
| ILA 抓波 + 分析 (analyze_capture.py) | ✅ 完成 2026-08-12 |
| ping 回包 (ARP 应答) | ❌ 未通 — 包在 MAC→CPU FIFO 之间被丢弃 |
| MAC TX 发包时序验证 | ✅ PASS — 仿真验证通过 |

---

## 三、ILA 当前配置 — 2026-08-12 最终版本

文件: `rtl/webserver_cpu_top.v`

```verilog
// ① 核总线 — 50MHz CPU 时钟域
localparam ILA_NUM_CORES     = 1;
localparam ILA_TRANSPORT_EN  = 3'b001;   // UART only
localparam ILA_BAUD          = 921600;
localparam ILA_CLK_HZ        = 50_000_000;

wire [ILA_NUM_CORES-1:0]      ila_core_we;
wire [ILA_NUM_CORES-1:0]      ila_core_re;
wire [15:0]                   ila_core_addr;
wire [31:0]                   ila_core_wdata;
wire [ILA_NUM_CORES*32-1:0]   ila_core_rdata;

// ② ILA 核 — 32 探针, 184 bit, clk_50m, DEPTH=2048
soft_ila_top #(
    .CORE_EN(1), .DATA_DEPTH(2048), .MAX_WINDOWS(1),
    .SAMPLE_HZ(50000000), .RST_ACTIVE_LOW(1), .NUM_PROBES(32),
    .PROBE0_WIDTH(1), .PROBE1_WIDTH(8), ... .PROBE25_WIDTH(4),
    .PROBE26_WIDTH(8), .PROBE27_WIDTH(8), .PROBE28_WIDTH(8),
    .PROBE29_WIDTH(8), .PROBE30_WIDTH(1), .PROBE31_WIDTH(1),
    .SIMPLE_INFER(1),   // 关键: 绕过 XPM 多块分配, 用简单推断
    .EXT_TRIG_EN(1)
) u_ila_core0 (
    .sample_clk(clk_50m), .rst_in(sys_rst_n), .jtag_clk(ila_jtag_clk),
    .probe0(gmii_rx_dv), .probe1(gmii_rxd), ... .probe25(led_o),
    .probe26(rx_afifo_full_cnt[7:0]), .probe27(rx_correct_pkt_cnt[7:0]),
    .probe28(rx_crc_err_pkt_cnt[7:0]), .probe29(recv_pkt_drop_cnt[7:0]),
    .probe30(mac_in_full), .probe31(gmii_rx_er),
    .trigger_in(1'b0), .trigger_out(), .armed_out(),
    .reg_we(ila_core_we), .reg_re(ila_core_re),
    .reg_addr(ila_core_addr), .reg_wdata(ila_core_wdata),
    .reg_rdata(ila_core_rdata)
);

// ③ ILA Hub — UART 传输, 板载串口 L21/M21
ila_hub_top #(
    .TRANSPORT_EN(3'b001), .NUM_CORES(ILA_NUM_CORES),
    .ILA_CLK_HZ(ILA_CLK_HZ), .ILA_BAUD(ILA_BAUD), .REG_HOLD(4)
) u_ila_debug (
    .clk(clk_50m), .rst(~sys_rst_n),
    .uart_rxd(uart_rx), .uart_txd(uart_tx),
    .gmii_rx_clk(1'b0), .gmii_rxd(8'd0), .gmii_rx_dv(1'b0),
    .gmii_txd(), .gmii_tx_en(),
    .core_reg_we(ila_core_we), .core_reg_re(ila_core_re),
    .core_reg_addr(ila_core_addr), .core_reg_wdata(ila_core_wdata),
    .core_reg_rdata(ila_core_rdata),
    .core_jtag_clk(ila_jtag_clk), .core_jtag_rst()
);
```

**关键参数:**

| 参数 | 值 | 说明 |
|------|-----|------|
| 采样时钟 | clk_50m (50MHz) | 20ns 粒度, 无时序违规 |
| Hub 时钟 | clk_50m (50MHz) | REG_HOLD=4 |
| 传输 | UART, 921600 baud | 板载 CH340 → /dev/ttyACM0 |
| 采样深度 | 2048 | 环形缓冲 |
| 窗口数 | 1 | 单次触发 |
| 复位 | sys_rst_n (低有效) | = reset_l & pll_locked |
| SIMPLE_INFER | 1 | 绕过 XPM 多块分配 |

> ⚠️ **UART 物理接口**: ACX750 板载 CH340 (USB-Serial), Linux 设备为 `/dev/ttyACM0`，不是 `/dev/ttyUSB1`。FT232H 仅提供 JTAG 编程功能。

---

## 四、32 探针详情 (signals.json @ 50MHz)

| # | 信号 | 位宽 | bit_lo | 说明 |
|---|------|------|--------|------|
| 0 | gmii_rx_dv | 1 | 0 | GMII RX valid |
| 1 | gmii_rxd | 8 | 1 | GMII RX data |
| 2 | gmii_tx_en | 1 | 9 | GMII TX enable |
| 3 | gmii_txd | 8 | 10 | GMII TX data |
| 4 | mac_rx_sop | 1 | 18 | MAC RX start |
| 5 | mac_rx_en | 1 | 19 | MAC RX enable |
| 6 | mac_rx_data | 8 | 20 | MAC RX data |
| 7 | mac_rx_eop | 1 | 28 | MAC RX end |
| 8 | mac_tx_sop | 1 | 29 | MAC TX start |
| 9 | mac_tx_en | 1 | 30 | MAC TX enable |
| 10 | mac_tx_data | 8 | 31 | MAC TX data |
| 11 | mac_tx_eop | 1 | 39 | MAC TX end |
| 12 | mac_tx_err | 1 | 40 | MAC TX error |
| 13 | bus_req | 1 | 41 | CPU bus request |
| 14 | bus_rhwl | 1 | 42 | CPU bus R=1/W=0 |
| 15 | bus_address | 32 | 43 | CPU bus address |
| 16 | bus_rdata | 32 | 75 | CPU bus read data |
| 17 | bus_ack | 1 | 107 | CPU bus ack |
| 18 | bus_wdata | 32 | 108 | CPU bus write data |
| 19 | cpu_rd_empty | 1 | 140 | CPU RX FIFO empty |
| 20 | cpu_wr_full | 1 | 141 | CPU TX FIFO full |
| 21 | cpu_rd_rpkt_pop_ind | 1 | 142 | RX packet pop pulse |
| 22 | cpu_wr_wpkt_push_ind | 1 | 143 | TX packet push pulse |
| 23 | cpu_wr_wen_ind | 1 | 144 | TX write enable pulse |
| 24 | cpu_rd_ren | 1 | 145 | RX read enable |
| 25 | led_o | 4 | 146 | LED output |
| 26 | rx_afifo_full_cnt | 8 | 150 | gmii2mac 内部异步 FIFO 溢出计数 |
| 27 | rx_correct_pkt_cnt | 8 | 158 | MAC 好 FCS 帧计数 |
| 28 | rx_crc_err_pkt_cnt | 8 | 166 | MAC 坏 FCS 帧计数 |
| 29 | recv_pkt_drop_cnt | 8 | 174 | cpu_channel RX FIFO 满丢包计数 |
| 30 | mac_in_full | 1 | 182 | package_fifo_v2 RX full 标志 |
| 31 | gmii_rx_er | 1 | 183 | GMII RX 错误标志 (PHY) |

总宽: 184 bit, 深度: 2048, 时钟: 50MHz, 窗口: 1, 签名: `0x00000800200100B8`

---

## 五、关键文件清单

### RTL (项目)
| 文件 | 说明 |
|------|------|
| `rtl/webserver_cpu_top.v` | 顶层模块，含 ILA 标准模式集成 |
| `rtl/dual_clock_simple_dual_port_ram.v` | 含 `ifndef IVL_SIM` guard |
| `rtl/package_fifo_v2.v` | 含 `ifndef IVL_SIM` guard |
| `rtl/gmii2mac.v` | 含 `ifndef IVL_SIM` guard |
| `rtl/riscv32_top.v` | 含 `ifndef IVL_SIM` guard |
| `rtl/uart.v` | 含 `ifndef IVL_SIM` guard |
| `rtl/single_clock_true_dual_port_ram.v` | 含 `ifndef IVL_SIM` guard |
| `rtl/single_clock_simple_dual_port_ram.v` | 含 `ifndef IVL_SIM` guard |

### 仿真
| 文件 | 说明 |
|------|------|
| `sim/tb_fast.v` | Testbench，用 `force dut.ila_*` 访问 ILA 寄存器 |
| `sim/Makefile` | iverilog 编译，`ILA_HOME = fpga_ila_local` |

### 配置
| 文件 | 说明 |
|------|------|
| `signals.json` | 1 核, 32 探针, 184bit, 2048 depth |
| `ila_gui.sh` | GUI 启动脚本 (Qt + Web)，指向 `fpga_ila_local` |
| `fpga_ila_files.f` | fpga_ila RTL 文件列表 (${FPGA_ILA_HOME} 变量) |

### Vivado 构建
| 文件 | 说明 |
|------|------|
| `build_xilinx/build.tcl` | 自动包含 `fpga_ila_local` 全部 RTL 文件 |
| `build_xilinx/pins.xdc` | ILA UART 占用板载 L21(ila_uart_rxd)/M21(ila_uart_txd) |
| `build_xilinx/add_ila.tcl` | ⚠️ Vivado 硬件 ILA (Xilinx IP)，不要运行，会冲突 |

---

## 六、fpga_ila 安装位置

| 路径 | 用途 |
|------|------|
| `/home/zhihuiw/fpga_work/fpga_ila_local/` | **仿真 + Vivado 构建使用** (Makefile ILA_HOME) |
| `/home/zhihuiw/fpga_work/ip库/fpga_ila-fcapz_ela_enhance/` | ip库只读参考 (Web GUI venv 在这里，代码也在这里) |
| `/home/zhihuiw/fpga_work/ip copy/fpga_ila/` | ip copy (允许修改) |

> ⚠️ `fpga_ila_local` 和 `ip库/fpga_ila-fcapz_ela_enhance` 是**两份独立拷贝**，结构相同。仿真用 local，Web GUI 的代码修改在 ip库版本。

---

## 七、已修复的 Bug/问题

### 1. ILA BURST_PTR 回读 (已解决)
data_byte_off 整数除法截断 → 改用扁平偏移 `FCAPZ_DATA_BASE + i*20 + word_offset`

### 2. TRIG_MASK 默认全 1 (已解决)
上电默认所有位 mask=1，必须显式清零 word0-4

### 3. 16-bit 地址溢出 (已解决)
151bit × 4096 = 20B/sample，最大地址 0x140FC > 0xFFFF → DATA_DEPTH 降到 2048

### 4. VCD 不生成 — 内嵌 TB 冲突 (已解决)
7 个 RTL 文件加 `ifndef IVL_SIM` guard

### 5. Web GUI 加载 JSON 无反应 (已解决)
`_loadSignals()` 没拷贝 `data_depth`/`num_probes` → `_populateCoreSelect()` 当死核过滤 → 修改 `index.html`

### 6. Vivado `module not found` (已解决)
build.tcl 只加 `../rtl/*.v`，没加 fpga_ila 文件 → 在 build.tcl 里加了 glob

### 7. Vivado part-select 常量错误 (已解决)
`ila_rdata[0*32 +: 32]` → 改为直接 `ila_rdata`

### 8. 标准指南不含 reg_re 端口 (文档坑)
快速入门示例省略了 `reg_re`/`core_reg_re`，但实际 RTL 端口必须连接

### 9. Web GUI 默认波特率 115200 vs FPGA 921600 (已解决)
连接时手动改为 921600

### 10. Vivado DRC: uart_rx/tx 无引脚约束 (已修复)
顶层有 `uart_rx`/`uart_tx` 端口但 `pins.xdc` 缺约束。ACX750 板仅一组 UART 引脚 L21/M21 被 ILA 占用 → CPU UART 分配到闲置 GPIO0 C18/C19 (LVCMOS33)，仅满足 DRC 要求。

### 11. ILA BRAM 时序违规 (已知限制)
125MHz 时钟下 ILA BRAM 路径 WNS=-4.006ns，151bit 宽总线关键路径 ~12ns > 8ns period。不影响功能验证但可能丢采样点。

---

## 八、常用命令

```bash
# === 仿真 ===
cd /home/zhihuiw/fpga_work/Prj/RiscV_WebSoC_3/sim
make clean && make      # 编译
make run                # 仿真 → tb_fast.vcd
make wave               # gtkwave 看波形

# === GUI (需要 FPGA 上板烧录) ===
cd /home/zhihuiw/fpga_work/Prj/RiscV_WebSoC_3
./ila_gui.sh web        # Web 版 → http://localhost:9527
./ila_gui.sh            # Qt 桌面版

# === Vivado (本机) ===
source /home/zhihuiw/vivado/Vivado/2024.1/settings64.sh
cd /home/zhihuiw/fpga_work/Prj/RiscV_WebSoC_3/build_xilinx
vivado -mode batch -source build.tcl    # 综合 + 实现 + 生成 bitstream

# === 烧录 (本机) ===
vivado -mode batch -source program.tcl  # 烧录 bit 到 FPGA
```

---

## 九、2026-08-12 时序修复 + 上板调试

### 时序约束修复

**问题**: Vivado 布局后 Setup WNS=-1.760ns, TNS=-123.154ns, 81 条 failing endpoints。跨时钟域路径 (clk_125m → clk_50m_cpu) 没有时钟组约束。

**修复** (`build_xilinx/timing.xdc`):
```tcl
# 声明 4 路 MMCM 输出时钟为异步组
set_clock_groups -asynchronous \
    -group [get_clocks clk_125m_unbuf] \
    -group [get_clocks clk_125m_tx_unbuf] \
    -group [get_clocks clk_200m_unbuf] \
    -group [get_clocks -include_generated clk_50m_cpu_unbuf]
```

**结果**:
| 指标 | 修复前 | 修复后 |
|------|--------|--------|
| WNS (Setup) | -1.760 ns | **+3.321 ns** |
| TNS (Setup) | -123.154 ns | **0.000** |
| WHS (Hold) | — | **+0.031 ns** |
| Failing endpoints | 81 | 0 |

### SIMPLE_INFER 参数修复

**问题**: `ila_ela.v` 向 `dual_clock_true_dual_port_ram.v` 传入 `.SIMPLE_INFER(1)`，但该参数不存在。

**修复**:
- `dual_clock_true_dual_port_ram.v`: 新增 `parameter SIMPLE_INFER = 0`，添加 `generate if (SIMPLE_INFER)` 简单推断分支
- 移除内层嵌套 `generate`/`endgenerate` 关键字（SystemVerilog 禁止嵌套 generate）

### ila_hub_top 端口修复

**问题**: `webserver_cpu_top.v` 例化了不存在的 `.jtag_tms()` 端口。

**修复**: 移除 `.jtag_tms()` 连接（该版本 ila_hub_top 无此端口）。

### UART 物理接口发现

ACX750 板的 USB-Blaster (FT232H, 0403:6014) 仅提供 JTAG 编程功能。板载 UART 通过 CH340 芯片 (USB-Serial)，Linux 设备为 `/dev/ttyACM0`（非 `/dev/ttyUSB1`）。

### 当前状态

- ✅ 固件已通过 UART 上传 (1455 words)，CPU 已启动运行
- ✅ ILA 抓波正常，`analyze_capture.py` 可触发并回读 2048 样本
- ⚠️ 串口设备: `/dev/ttyACM0` @ 921600 baud（GUI 默认 /dev/ttyUSB1 需手动改）
- ⚠️ `updatemem` 固件合并方案已弃用（无 `-mem` 选项，MMI 无 Processor 块）
- ❌ ping 未通 — ARP 请求到达 MAC 层但包在 MAC→CPU FIFO 之间被丢弃（见第十二节）

---

## 十、MAC TX 发包时序验证 (2026-08-11 仿真)

### 验证方法

1. 修改 testbench ILA 触发条件: `mac_rx_sop` → `mac_tx_sop` (probe14, bit_lo=97, word3 bit1)
2. 配置 POSTTRIG=1535 (之前默认为 0，导致只捕获 1 个后触发样本)
3. PRETRIG=512 → 总采集 = 512 + 1535 + 1 = 2048 样本
4. 分析脚本: `sim/analyze_mac_tx.py`, `sim/extract_mac_tx_detail.py`

### 关键时序测量 (50MHz ILA 采样 = 20ns/样本)

| 参数 | 测量值 | 预期 | 状态 |
|------|--------|------|------|
| SOP 脉冲宽度 | 1 cycle (20ns) | 1 cycle | ✅ |
| EOP 脉冲宽度 | 1 cycle (20ns) | 1 cycle | ✅ |
| mac_tx_en 持续时间 | 64 cycles (1280ns) | 64 bytes | ✅ |
| mac_tx_err | 始终为 0 | 0 | ✅ |
| TX PUSH → MAC_TX_SOP | 10 samples (200ns) | — | ✅ |
| MAC_TX_SOP → GMII_TX_SOP | 7 samples (140ns) | — | ✅ |
| GMII preamble 插入 | 8 bytes (0x55×7+0xD5) | 8 bytes | ✅ |

### 数据包内容

```
MAC TX Data (64 bytes):
  11 22 33 44 55 66  ← Dst MAC (PC)
  00 00 01 02 04 05  ← Src MAC (FPGA)
  08 00              ← EtherType IPv4
  45 00 00 28 ...    ← IP: Ver4, Len40, Proto6
  a9 fe 01 01        ← Src IP = 169.254.1.1
  a9 fe 01 64        ← Dst IP = 169.254.1.100
  00 50 00 50        ← TCP: SrcPort=80, DstPort=80 ⚠ (见下方)
  10 00 00 00        ← SEQ = 0x10000000
  20 00 00 01        ← ACK = 0x20000001
  50 12              ← Flags = SYN|ACK ✓
  05 b4 79 c5        ← Window=1460, Checksum
  Padding: 00...     ← 补零到 64B 最小帧
```

### ⚠ TCP DstPort 问题 (已查明根因 — 2026-08-12 更新)

**现象**: ILA dump 显示 DstPort=80 (0x0050)，但 PC 从端口 4660 (0x1234) 发起连接。
固件 C 代码和反汇编逻辑正确。

**根因 (2026-08-12 确认)**: `sim/xpm_memory_tdpram_sim.v` 仿真模型缺少 byte-enable 支持。

~~**旧根因 (2026-08-11, 已推翻)**~~: 原分析错误认定 Port A 缺少 byte-enable。
推翻理由: CPU 数据写入全部走 Port B（Port B 有 `wren_byte_b`），Port A 仅用于 BFM 固件加载。

**确认的故障链路**:

1. **Byte-enable 硬件 pipeline 验证通过** (`tb_byte_en_test.v`, vendor="" → gen_inferred_ram):
   - 精确复现两个 `sh` 半字写操作，监控全链路信号
   - 结果: PASS — `word[0x5D6] = 0x12340050` ✓
   - Byte-enable 硬件 pipeline (PicoRV32 → riscv32intfbridge → wr_byte_en_d → SRAM wren_byte_b) 完全正确

2. **Bug 复现** (`tb_byte_en_test_xpm.v`, vendor="xilinx" → gen_xilinx_xpm_tdpram):
   - 使用与完整 SoC 仿真相同的 XPM 路径
   - 结果: BUG REPRODUCED — `word[0x5D6] = 0x00500050`

3. **XPM 仿真模型缺陷** (`sim/xpm_memory_tdpram_sim.v`):
   ```verilog
   // 修复前: 全部 32 bit 写入，忽略 byte-enable
   if (enb && |web)            // |web = ANY bit set
       mem[addrb] <= dinb;     // 写入全字
   ```
   - PicoRV32 的 `sh` 复制半字数据到 mem_wdata 两半 (0x0050 → 0x00500050)
   - XPM 模型虽有正确 byte-enable 信号 (`web = 0011`)，但写入时忽略 → 全字覆盖
   - 上半字 (tcp_src_port = 0x1234) 被 0x0050 覆盖

4. **修复** (`sim/xpm_memory_tdpram_sim.v`):
   - 端口宽度: `wea`/`web` 从 `[BYTE_WRITE_WIDTH/8-1:0]` (1bit) 修复为 `[WRITE_DATA_WIDTH/BYTE_WRITE_WIDTH-1:0]` (4bit)
   - 写入逻辑: 添加逐字节 `for` 循环，每字节独立判断 `web[i]`
   - 修复后验证: `word[0x5D6] = 0x12340050` ✓

**结论:**
- **RTL 硬件代码完全正确，无需修改**
- Bug 仅存在仿真模型中，不影响上板行为
- `vendor_stubs.v` 中 `altsyncram` 模型已同步修复 (byteena_a/byteena_b per-byte 写入)，不影响当前 Xilinx 路径
- Byte-enable 全链路 (PicoRV32 → riscv32intfbridge → riscv32_top → single_clock_tdpram → SRAM) 共 7 级，全部验证通过

---

## 十一、下一步计划

1. ⏳ **ILA GUI 连 FPGA**: 在 Qt GUI 中选择 `/dev/ttyACM0` @ 921600 → PING 验证 ILA Hub 通信
2. ⏳ **固件合并方案**: 探索 updatemem 替代方案 (BMM/data2mem/Vivado Tcl)
3. ⏳ **加载固件**: 通过 RISC-V 编程接口 (program_wr/waddr/wdata) 或修复 updatemem
4. ⏳ **抓 ILA 波形**: 设触发条件 → 抓 MAC TX / GMII 信号 → 验证硬件时序
5. ⏳ **对接 http_proc**: 实现 HTTP GET 响应

---

## 十二、固件加载成功 + ILA 抓波诊断 (2026-08-12)

### 固件加载 (UART 方式)

`lcpu_type = "uart"` 的固件加载路径: `uart_lcpu_top` (115200) → SUBBUS → `lcpu_fpga_test` 解码 → 写 program RAM + reset。

**关键 Bug 发现** (`c_build/upload_fw.py`，未修改源码):

`upload_fw.py` 默认 `--offset 0x100`，会错误地剥离固件前 256 字节（即 `reset_entry`），导致 CPU 在 RAM word 0 执行全零。正确用法是 `--offset 0`:

```bash
python3 c_build/upload_fw.py c_build/out/firmware.bin --offset 0 -p /dev/ttyACM0 -b 115200
# 结果: 1455 words 上传成功
```

地址映射 (lcpu_fpga_test.v):
| 地址 | 功能 |
|------|------|
| 0x100 | `riscv_reset_l` (写 0=复位, 1=释放) |
| 0x10 | LED |
| 0x10000~0x1ffff | program RAM (word 地址 = address[15:0]) |

### CPU 运行验证 (ILA 抓波)

`build_xilinx/analyze_capture.py` 触发 `gmii_rx_dv=1`，后台持续 ping 169.254.1.1，回读 2048 样本分析:

| 信号 | 结果 | 含义 |
|------|------|------|
| `bus_req` @ 0x6000 | 3 次读 | CPU 轮询 RX FIFO 空标志 (`_RD(0)`) |
| `led_o` | = 1 | 心跳状态 (CPU 已进入主循环) |
| `gmii_rx_dv` | 28 样本 (560ns) | ARP 请求到达 FPGA |
| `mac_rx_en` | 25 样本 (500ns) | MAC 层收到帧 |

**结论**: 固件加载成功，CPU 运行正常。✅

### ping 不通根因定位

ILA 抓包诊断 (40.96µs 窗口) 定位到丢包点:

| 信号 | 结果 | 结论 |
|------|------|------|
| `gmii_rx_dv` | 28 样本 | ✅ ARP 到达 GMII RX |
| `mac_rx_en` | 25 样本 | ✅ MAC 层收到帧 |
| `mac_rx_sop` / `mac_rx_eop` | 0 | ⚠️ (125MHz 单拍脉冲，50MHz ILA 采不到，不能下结论) |
| `cpu_rd_empty` | 始终高 (空) | ❌ 包没进 CPU RX FIFO |
| `cpu_rd_ren` | 始终高 (2048) | ⚠️ 可疑 |
| `gmii_tx_en` / `mac_tx_en` | 0 | ❌ 无 TX 回包 |

**根因**: 帧到达 MAC 层，但在 `gmii2mac → package_fifo_v2` 之间被丢弃，未进入 CPU RX FIFO。

> ⚠️ 本节「最可能是 CRC 错误」的初步判断已被 2026-08-13 多智能体分析**推翻**——`mac_rx` 从不丢弃坏帧（CRC 错只置 `mac_rx_err` 标志，不 gate 数据），RGMII nibble 位序也验证正确。真正的丢包点在两个 CDC FIFO 之一，详见 [第十三节](#十三丢包根因并行分析--决定性信号接入-ila-2026-08-13)。

**两个候选丢包点**:
1. **站点 A**: `gmii2mac` 内部异步 FIFO (Eth_RXC→clk_125m, 16 深) — full 时静默丢字节，无背压
2. **站点 B**: `cpu_channel` 的 `package_fifo_v2` (clk_125m→clk_50m) — `wpkt_push && full` 时丢整包

**下一步**: 重编译烧录后，用新接入的 6 个决定性探针 (probe26-31) 一锤定音。

---

## 十三、丢包根因并行分析 + 决定性信号接入 ILA (2026-08-13)

### 13.1 多智能体分析结论 (排除 3 个假设)

| 假设 | 结论 | 关键依据 |
|------|------|---------|
| mac_rx 因 CRC 错丢弃帧 | ❌ 排除 | mac_rx 不丢弃坏帧，crc_err 只置 `mac_rx_err` 标志，不 gate sop/en/eop |
| RGMII nibble 位序错 | ❌ 排除 | posedge→低 nibble、negedge→高 nibble 正确 (RGMII v2.0) |
| cpu_rd_ren=1 固件 bug 排空 FIFO | ❌ 部分 | ren 在 package_fifo_v2 里未被使用，不影响 empty |
| wpkt_push 需 eop 才触发 | ❌ 排除 | push 由 `mac_rx_en` 下降沿触发 (`~ram_wen & wen`)，不需 eop |
| dual_clock_fifo CDC 丢字节 | ⚠️ 候选 | 全写全读 + gray 指针过采样 + full 静默丢字节 |

**关键发现**: 空标志 `cpu_rd_empty` 拉低条件 = `(mac_rx_en 干净下降沿) & frame_hit=1 & !full_reg`。`frame_hit` 恒 1（包过滤关闭），所以最可能是 **`full_reg` 卡高（数据 RAM 满）阻塞写**，或 FIFO 指针 CDC 问题。

### 13.2 两个丢包点 + 决定性信号

| 站点 | 模块 | 时钟域 | 丢包条件 | 决定性信号 |
|------|------|--------|---------|-----------|
| A | gmii2mac 内部异步 FIFO (16 深) | Eth_RXC→clk_125m | full 静默丢字节 | `rx_afifo_full_cnt` |
| B | cpu_channel package_fifo_v2 | clk_125m→clk_50m | `wpkt_push && full` 丢整包 | `recv_pkt_drop_cnt` + `mac_in_full` |

物理层健康度判据: `rx_correct_pkt_cnt`（好 FCS 帧）、`rx_crc_err_pkt_cnt`（坏 FCS 帧）、`gmii_rx_er`（PHY 错误标志）。

### 13.3 新增 6 探针 (probe26-31)

| # | 信号 | 位宽 | bit_lo | 来源 |
|---|------|------|--------|------|
| 26 | rx_afifo_full_cnt | 8 | 150 | gmii2mac 计数器 (原悬空 `.rx_afifo_full_cnt()`) |
| 27 | rx_correct_pkt_cnt | 8 | 158 | gmii2mac 计数器 (原悬空) |
| 28 | rx_crc_err_pkt_cnt | 8 | 166 | gmii2mac 计数器 (原悬空) |
| 29 | recv_pkt_drop_cnt | 8 | 174 | cpu_channel 新增 `output` |
| 30 | mac_in_full | 1 | 182 | cpu_channel 新增 `output` (package_fifo_v2 `.full`) |
| 31 | gmii_rx_er | 1 | 183 | 顶层 GMII RX 错误 |

**修改的文件** (仅项目 RTL，未动 fpga_ila IP):
- `rtl/webserver_cpu_top.v` — NUM_PROBES 26→32, 新增 probe26-31 连线, 接通 gmii2mac 计数器
- `rtl/cpu_channel.v` — 新增 `mac_in_full` output 端口
- `signals.json` — 32 探针, 184bit, signature `0x00000800200100B8`
- `build_xilinx/analyze_capture.py` + `ila_capture.py` — 触发字 5→6 字, P 表加 6 探针

### 13.4 判定表 (重烧录后读 analyze_capture.py 输出)

| 现象 | 判定 |
|------|------|
| `rx_crc_err_pkt_cnt > 0` | 物理层损坏 → 调 IDELAY / 查 RGMII 位序 / 时钟 |
| `rx_correct_pkt_cnt == 0` | 物理层根本没收到好帧 |
| `rx_afifo_full_cnt > 0` | 站点 A: Eth_RXC→125m 异步 FIFO 溢出 |
| `recv_pkt_drop_cnt > 0` 或 `mac_in_full==1` | 站点 B: 125m→50m FIFO 满丢包 / full 卡高 |

### 13.5 重编译 + 重烧录 + 重触发流程

```bash
# ① Vivado 重编译 (本机, 因为 RTL 改了 NUM_PROBES/探针连线)
source /home/zhihuiw/vivado/Vivado/2024.1/settings64.sh
cd /home/zhihuiw/fpga_work/Prj/RiscV_WebSoC_3/build_xilinx
vivado -mode batch -source build.tcl          # 综合 + 实现 + bitstream

# ② 烧录 (本机)
vivado -mode batch -source program.tcl        # 烧录到 xc7a35t_0

# ③ 上传固件 (本机, ~39 分钟, 见开发日报 2026-08-13 任务1)
cd /home/zhihuiw/fpga_work/Prj/RiscV_WebSoC_3
python3 c_build/upload_fw.py c_build/out/firmware.bin --offset 0 -p /dev/ttyACM0 -b 115200

# ④ 采集 + 分析 (自动触发 gmii_rx_dv, 后台 ping, 回读 2048 样本)
python3 build_xilinx/analyze_capture.py
#   关键看新增的「=== 丢包根因判定 (新增 6 探针) ===」段落

# ⑤ (可选) 波形查看
./ila_gui.sh              # Qt GUI, 端口 /dev/ttyACM0 @ 921600
```

### 13.6 UART 门控修复 (阻断 ILA 流量污染固件上传解码器)

板载 UART RX(L21) 同时接固件上传 UART(115200) 与 ILA Hub(921600)。上传结束后 always-on 的 `uart_lcpu` 解码器仍扫描 `'s'(0x73)` 起始字节, 会把 ILA 二进制包误判成 `s{w|r}` 命令, 进而误写指令 RAM / reset(0x100) / LED(0x10)。

**修复 (仅 `rtl/webserver_cpu_top.v`)**: 新增粘性「上传完成」门控 —— 见 CPU 复位拉低(进入上传窗口)后再释放(0→1)才把 CPU 的 `uart_rx` 门控到 1'b1, 只留 ILA Hub 独享 RX。

最终版 (survive-reset, 见 13.8) — 粘性标志 **不随 `reset_l` 复位**, 只在配置(上电/烧录)时 INIT=0:

```verilog
reg  uart_seen_reset_lo = 1'b0;  // 见过 CPU 复位被拉低(=进入上传窗口)
reg  uart_upload_done   = 1'b0;  // 上传完成标志(粘性, 跨 reset_l 保持)
always @(posedge clk_50m) begin
  if (riscv_reset_l == 1'b0)
    uart_seen_reset_lo <= 1'b1;
  if (uart_seen_reset_lo && riscv_reset_l == 1'b1)
    uart_upload_done <= 1'b1;
end
wire cpu_uart_rx = uart_upload_done ? 1'b1 : uart_rx;   // u_riscv.uart_rx 改用此信号
```

**要点**:
- 用粘性标志而非 `reset_l==1` 直接关门 —— 上电时 `riscv_reset_l` 默认=1(`lcpu_fpga_test`), 直接关门会挡住上传第一步 `reset=0` 命令。
- 副作用: 上传最后一步 `LED(0x10)=5` 被丢弃, 纯装饰(固件心跳接管 LED)。
- **这是真实冲突但不是 ping 失败根因** —— ping 失败是纯硬件 MAC→FIFO 通路问题, 判定仍靠 [13.4 判定表](#134-判定表-重烧录后读-analyze_capturepy-输出)。此修复已并入 13.5 重编译流程(重编译即含此修复)。

### 13.7 计数器判定结果 (2026-08-13): 丢包在站点 A ✅

烧录 + 上传 + 读计数器后:

| 计数器 | 值 | 判定 |
|---|---|---|
| rx_correct_pkt_cnt | 133 | 物理层正常 |
| **rx_afifo_full_cnt** | **219** | **站点 A: gmii2mac 内部异步 FIFO 溢出** |
| recv_pkt_drop_cnt | 0 | 站点 B 无丢 |
| mac_in_full | 0 | full 未卡高 |

**结论**: `gmii2mac` 内部异步 FIFO(Eth_RXC→clk_125m, 16 深)溢出丢字节, 是 ping 不通的根因。修复方向: 加深 FIFO(16→64/128)或对齐 Eth_RXC/clk_125m 时钟域。
```

> ⚠️ ILA 采样时钟仍是 50MHz，125MHz 的单拍 SOP/EOP 脉冲采不到；但计数器是累加的 level 信号，可可靠读。

### 13.8 门控失效根因: DTR 复位重开门 (2026-08-13)

13.6 的初版门控带 `negedge reset_l` 复位, 实测「GUI 打开串口后 ping 又不通 / 断开 GUI 仍不通」。根因链:

1. 上传固件后门已关 (`uart_upload_done=1`), 固件本应受保护。
2. **GUI/脚本打开 `/dev/ttyACM0` 时 assert DTR**, 若 DTR 接线连到 `reset_l`(D21), 则触发 FPGA 复位。
3. 初版门控在 `negedge reset_l` 时把 `uart_upload_done` 清 0 → 门重开 → ILA 二进制流量再次流入 `uart_lcpu` 解码器 → 误写指令 RAM / reset(0x100) / LED(0x10), 固件被污染 → ping 失败。

**修复 (survive-reset)**: 去掉 `negedge reset_l`, 粘性标志用 `= 1'b0` 的 INIT(仅配置时清 0)。这样 DTR/按键复位都不会重开门, 已上传固件(仍在 BRAM)持续受保护, ILA 抓波不再污染固件。

**代价 (已记录在代码注释)**: 重新上传固件需重新烧录(配置时门重开)。如需快速重传, 后续可加 UART break 检测等解锁机制。

**重编译状态**: 本修改已重编译, Vivado `BUILD COMPLETE`, bitstream 重新生成 (`build_xilinx/RiscV_WebSoC.bit`, 2026-08-13 19:17)。待重烧录 + 上传固件 + ping 验证。

> ⚠️ DTR→reset_l 接线未从原理图确认; 但 survive-reset 修复对「任何 reset_l 来源重开门」都鲁棒, 方向正确。
