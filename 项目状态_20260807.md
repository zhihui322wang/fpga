# RISC-V WebSoC — 项目当前状态

> 最后更新: 2026-08-07
> 目标: 上板用 fpga_ila 抓 ILA 波形，验证 MAC TX 发包时序

---

## 一、项目概述

RISC-V WebSoC: PicoRV32 + XC7A35T-FGG484-2 + RTL8211F PHY

- **仿真工具**: iverilog + gtkwave
- **综合工具**: Vivado (另一台机器)
- **调试工具**: fpga_ila (软逻辑分析仪, fcapz_ela_enhance 分支)
- **开发板**: ACX750 (XC7A35T), 板载 UART L21/M21, FT232H USB 串口

---

## 二、当前进度总览

| 任务 | 状态 |
|------|------|
| TCP SYN→SYN+ACK 仿真验证 | ✅ PASS |
| ILA 探针扩展 (13→27, 151bit, 2048 depth) | ✅ 完成 |
| ILA 标准模式重构 | ✅ 完成 |
| 仿真 VCD 生成修复 (内嵌 TB 冲突) | ✅ 完成 |
| Web GUI 环境搭建 + Bug 修复 | ✅ 完成 |
| Vivado build.tcl 适配 (fpga_ila 文件 + UART 引脚) | ✅ 完成 |
| Qt GUI 环境准备 | ✅ 完成 (venv 已创建，全部依赖已安装，GUI 启动验证通过) |
| Vivado 综合生成 bitstream | ✅ 完成 (2.1MB, WNS=-3.714ns ILA BRAM 时序违规已知) |
| 上板烧录 + GUI 抓波形 | ✅ FPGA 已烧录 (Digilent/E306A991ABCD → xc7a35t_0) |
| MAC TX 发包时序验证 | ⏳ 待做 |

---

## 三、ILA 标准模式 — 当前 RTL 结构

文件: `rtl/webserver_cpu_top.v`

```verilog
// ① 核总线
localparam ILA_NUM_CORES    = 1;
localparam ILA_CLK_HZ       = 125_000_000;
localparam ILA_BAUD         = 921600;
localparam ILA_REG_HOLD     = 6;

wire [ILA_NUM_CORES-1:0]     ila_we;
wire [ILA_NUM_CORES-1:0]     ila_re;
wire [15:0]                  ila_addr;
wire [31:0]                  ila_wdata;
wire [ILA_NUM_CORES*32-1:0]  ila_rdata;

// ② ILA 核 — 27 探针, 151 bit
soft_ila_top_fcapz #(
    .CORE_EN(1), .DATA_DEPTH(2048), .MAX_WINDOWS(1),
    .SAMPLE_HZ(125000000), .RST_ACTIVE_LOW(1), .NUM_PROBES(27),
    .PROBE0_WIDTH(1), .PROBE1_WIDTH(8), ... .PROBE26_WIDTH(4),
    .EXT_TRIG_EN(1)
) u_ila_core0 (
    .sample_clk(clk_125m), .rst_in(sys_rst_n), .jtag_clk(ila_jtag_clk),
    .probe0(gmii_rx_dv), .probe1(gmii_rxd), ... .probe26(led_val),
    .trigger_in(1'b0), .trigger_out(), .armed_out(),
    .reg_we(ila_we[0]), .reg_re(ila_re[0]),
    .reg_addr(ila_addr), .reg_wdata(ila_wdata), .reg_rdata(ila_rdata)
);

// ③ ILA Hub — UART 传输
ila_hub_top #(
    .TRANSPORT_EN(3'b001), .NUM_CORES(ILA_NUM_CORES),
    .ILA_CLK_HZ(ILA_CLK_HZ), .ILA_BAUD(ILA_BAUD),
    .REG_HOLD(ILA_REG_HOLD), .USE_FCAPZ_CORE(1)
) u_ila_debug (
    .clk(clk_125m), .rst(~sys_rst_n),
    .uart_rxd(ila_uart_rxd), .uart_txd(ila_uart_txd),
    .gmii_rx_clk(1'b0), .gmii_rxd(8'd0), .gmii_rx_dv(1'b0),
    .gmii_txd(), .gmii_tx_en(),
    .core_reg_we(ila_we), .core_reg_re(ila_re),
    .core_reg_addr(ila_addr), .core_reg_wdata(ila_wdata),
    .core_reg_rdata(ila_rdata),
    .jtag_tms(1'b0), .core_jtag_clk(ila_jtag_clk), .core_jtag_rst(ila_jtag_rst)
);
```

**顶层端口**: 只保留 `ila_uart_rxd`(input) / `ila_uart_txd`(output)，删除了旧的 6 个非标准端口。

---

## 四、27 探针详情 (signals.json)

| # | 信号 | 位宽 | bit_lo | 说明 |
|---|------|------|--------|------|
| 0 | gmii_rx_dv | 1 | 0 | GMII RX valid |
| 1 | gmii_rxd | 8 | 1 | GMII RX data |
| 2 | gmii_tx_en | 1 | 9 | GMII TX enable |
| 3 | gmii_txd | 8 | 10 | GMII TX data |
| 4 | mac_rx_sop | 1 | 18 | MAC RX start |
| 5 | mac_rx_en | 1 | 19 | MAC RX enable |
| 6 | mac_rx_data | 8 | 20 | MAC RX data |
| 7 | mac_rx_eop | 1 | 28 | MAC RX end |
| 8 | bus_req | 1 | 29 | CPU bus request |
| 9 | bus_rhwl | 1 | 30 | CPU bus read=1/write=0 |
| 10 | bus_address | 32 | 31 | CPU bus address |
| 11 | bus_rdata | 32 | 63 | CPU bus read data |
| 12 | bus_ack | 1 | 95 | CPU bus ack |
| 13 | gmii_tx_er | 1 | 96 | GMII TX error |
| 14 | mac_tx_sop | 1 | 97 | MAC TX start |
| 15 | mac_tx_en | 1 | 98 | MAC TX enable |
| 16 | mac_tx_data | 8 | 99 | MAC TX data |
| 17 | mac_tx_eop | 1 | 107 | MAC TX end |
| 18 | mac_tx_err | 1 | 108 | MAC TX error |
| 19 | cpu_rd_empty | 1 | 109 | CPU RX FIFO empty |
| 20 | cpu_wr_full | 1 | 110 | CPU TX FIFO full |
| 21 | cpu_rd_rpkt_pop_ind | 1 | 111 | RX packet pop pulse |
| 22 | cpu_wr_wpkt_push_ind | 1 | 112 | TX packet push pulse |
| 23 | cpu_wr_wen_ind | 1 | 113 | TX write enable pulse |
| 24 | cpu_rd_ren | 1 | 114 | RX read enable |
| 25 | bus_wdata | 32 | 115 | CPU bus write data |
| 26 | led_val | 4 | 147 | LED output |

总宽: 151 bit, 深度: 2048, 时钟: 125MHz, 窗口: 1

---

## 五、关键文件清单

### RTL (项目)
| 文件 | 说明 |
|------|------|
| `rtl/webserver_cpu_top.v` | 顶层模块，含 ILA 标准模式集成 |
| `rtl/dual_clock_simple_dual_port_ram.v` | 含 `ifndef IVL_SIM` guard |
| `rtl/package_fifo_v2.v` | 含 `ifndef IVL_SIM` guard |
| `rtl/gmii2mac.v` | 含 `ifndef IVL_SIM` guard |
| `rtl/riscv32_top.v` | 含 `ifndef IVL_SIM` guard |
| `rtl/uart.v` | 含 `ifndef IVL_SIM` guard |
| `rtl/single_clock_true_dual_port_ram.v` | 含 `ifndef IVL_SIM` guard |
| `rtl/single_clock_simple_dual_port_ram.v` | 含 `ifndef IVL_SIM` guard |

### 仿真
| 文件 | 说明 |
|------|------|
| `sim/tb_fast.v` | Testbench，用 `force dut.ila_*` 访问 ILA 寄存器 |
| `sim/Makefile` | iverilog 编译，`ILA_HOME = fpga_ila_local` |

### 配置
| 文件 | 说明 |
|------|------|
| `signals.json` | 1 核, 27 探针, 151bit, 2048 depth |
| `ila_gui.sh` | GUI 启动脚本 (Qt + Web)，指向 `fpga_ila_local` |
| `fpga_ila_files.f` | fpga_ila RTL 文件列表 (${FPGA_ILA_HOME} 变量) |

### Vivado 构建
| 文件 | 说明 |
|------|------|
| `build_xilinx/build.tcl` | 自动包含 `fpga_ila_local` 全部 RTL 文件 |
| `build_xilinx/pins.xdc` | ILA UART 占用板载 L21(ila_uart_rxd)/M21(ila_uart_txd) |
| `build_xilinx/add_ila.tcl` | ⚠️ Vivado 硬件 ILA (Xilinx IP)，不要运行，会冲突 |

---

## 六、fpga_ila 安装位置

| 路径 | 用途 |
|------|------|
| `/home/zhihuiw/fpga_work/fpga_ila_local/` | **仿真 + Vivado 构建使用** (Makefile ILA_HOME) |
| `/home/zhihuiw/fpga_work/ip库/fpga_ila-fcapz_ela_enhance/` | ip库只读参考 (Web GUI venv 在这里，代码也在这里) |
| `/home/zhihuiw/fpga_work/ip copy/fpga_ila/` | ip copy (允许修改) |

> ⚠️ `fpga_ila_local` 和 `ip库/fpga_ila-fcapz_ela_enhance` 是**两份独立拷贝**，结构相同。仿真用 local，Web GUI 的代码修改在 ip库版本。

---

## 七、已修复的 Bug/问题

### 1. ILA BURST_PTR 回读 (已解决)
data_byte_off 整数除法截断 → 改用扁平偏移 `FCAPZ_DATA_BASE + i*20 + word_offset`

### 2. TRIG_MASK 默认全 1 (已解决)
上电默认所有位 mask=1，必须显式清零 word0-4

### 3. 16-bit 地址溢出 (已解决)
151bit × 4096 = 20B/sample，最大地址 0x140FC > 0xFFFF → DATA_DEPTH 降到 2048

### 4. VCD 不生成 — 内嵌 TB 冲突 (已解决)
7 个 RTL 文件加 `ifndef IVL_SIM` guard

### 5. Web GUI 加载 JSON 无反应 (已解决)
`_loadSignals()` 没拷贝 `data_depth`/`num_probes` → `_populateCoreSelect()` 当死核过滤 → 修改 `index.html`

### 6. Vivado `module not found` (已解决)
build.tcl 只加 `../rtl/*.v`，没加 fpga_ila 文件 → 在 build.tcl 里加了 glob

### 7. Vivado part-select 常量错误 (已解决)
`ila_rdata[0*32 +: 32]` → 改为直接 `ila_rdata`

### 8. 标准指南不含 reg_re 端口 (文档坑)
快速入门示例省略了 `reg_re`/`core_reg_re`，但实际 RTL 端口必须连接

### 9. Web GUI 默认波特率 115200 vs FPGA 921600 (已解决)
连接时手动改为 921600

### 10. Vivado DRC: uart_rx/tx 无引脚约束 (已修复)
顶层有 `uart_rx`/`uart_tx` 端口但 `pins.xdc` 缺约束。ACX750 板仅一组 UART 引脚 L21/M21 被 ILA 占用 → CPU UART 分配到闲置 GPIO0 C18/C19 (LVCMOS33)，仅满足 DRC 要求。

### 11. ILA BRAM 时序违规 (已知限制)
125MHz 时钟下 ILA BRAM 路径 WNS=-4.006ns，151bit 宽总线关键路径 ~12ns > 8ns period。不影响功能验证但可能丢采样点。

---

## 八、常用命令

```bash
# === 仿真 ===
cd /home/zhihuiw/fpga_work/Prj/RiscV_WebSoC_3/sim
make clean && make      # 编译
make run                # 仿真 → tb_fast.vcd
make wave               # gtkwave 看波形

# === GUI (需要 FPGA 上板烧录) ===
cd /home/zhihuiw/fpga_work/Prj/RiscV_WebSoC_3
./ila_gui.sh web        # Web 版 → http://localhost:9527
./ila_gui.sh            # Qt 桌面版

# === Vivado (另一台机器) ===
cd /home/zhihuiw/fpga_work/Prj/RiscV_WebSoC_3/build_xilinx
source build.tcl        # 综合 + 实现 + 生成 bitstream

# === 烧录 (另一台机器) ===
source program.tcl      # 烧录 RiscV_WebSoC.bit 到 FPGA
```

---

## 九、下一步计划

1. ⏳ **Vivado bitstream 生成** (DRC 修复后重跑中)
2. ⏳ **烧录 FPGA** → 用 Qt GUI 连接 `/dev/ttyUSB1` (波特率 921600)
3. ⏳ **加载 signals.json** → 配置触发 → 抓 ILA 波形
4. ⏳ **验证 MAC TX 发包时序**
5. ⏳ **对接 http_proc** → 实现 HTTP GET 响应
