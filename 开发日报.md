# RISC-V WebSoC 开发日报

> 项目: RISC-V WebSoC (PicoRV32 + XC7A35T + RTL8211F)<br>
> 目标: 三步渐进 — Ping通 → TCP握手 → HTTP网页

---

## 目录

- [2026-08-27](#2026-08-27)
  - [任务 1: 通读项目文档（重点 README）+ 确定项目下一步 + 生成完整流程学习文档](#任务-1-通读项目文档重点-readme-确定项目下一步-生成完整流程学习文档)
- [2026-08-26](#2026-08-26)
  - [任务 1: 复习 ip/tcp/http](#任务-1-复习-iptcphttp)
- [2026-08-25](#2026-08-25)
  - [任务 1: 复习 eth/arp/icmp](#任务-1-复习-etharpicmp)
- [2026-08-24](#2026-08-24)
  - [任务 1: RGMII/GMII 接口实现原理](#任务-1-rgmiigmii-接口实现原理)
  - [任务 2: 协议交互原理 — ping 全链路](#任务-2-协议交互原理--ping-全链路)

- [2026-08-20](#2026-08-20)
  - [任务 1: MQTT 扩展](#任务-1-mqtt-扩展)
  - [任务 2: 其他协议 — UDP](#任务-2-其他协议--udp)
  - [任务 3: 其他协议 — DHCP、DNS、NTP](#任务-3-其他协议--dhcpdnsntp)
  - [任务 4: 其他协议 — HTTPS、SSH、WebSocket、CoAP](#任务-4-其他协议--httpssshwebsocketcoap)
  - [任务 5: 其他协议 — MQTT 同类协议](#任务-5-其他协议--mqtt-同类协议)
- [2026-08-19](#2026-08-19)
  - [任务 1: IP 深入（续）](#任务-1-ip-深入续)
  - [任务 2: 网络协议栈收尾](#任务-2-网络协议栈收尾)
- [2026-08-18](#2026-08-18)
  - [任务 1: IP 深入](#任务-1-ip-深入)
  - [任务 2: TCP 深入](#任务-2-tcp-深入)
- [2026-08-17](#2026-08-17)
  - [任务 1: ARP 深入](#任务-1-arp-深入)
  - [任务 2: ICMP 深入](#任务-2-icmp-深入)
- [2026-08-16](#2026-08-16)
  - [任务 1: 网络协议栈知识点系统梳理](#任务-1-网络协议栈知识点系统梳理)
- [2026-08-15](#2026-08-15)
  - [任务 1：理解 TCP 代码 — 知识点总结](#任务-1理解-tcp-代码--知识点总结)
- [2026-08-14](#2026-08-14)
  - [任务 1：ping 通了 ✅](#任务-1ping-通了)
  - [任务 2：TCP 握手通了 ✅](#任务-2tcp-握手通了)
  - [任务 3：HTTP 网页通了 ✅](#任务-3http-网页通了)
  - [任务 4：ILA 调通了 ✅](#任务-4ila-调通了)
- [2026-08-13](#2026-08-13)
  - [任务 1: 固件重传 + CPU 运行验证](#任务-1-固件重传--cpu-运行验证)
  - [任务 2: 丢包根因定位 ✅ 站点 A 确认](#任务-2-丢包根因定位--站点-a-确认)
  - [任务 3: package_fifo_v2 深度仿真 — 双 pop 读指针污染](#任务-3-package_fifo_v2-深度仿真--双-pop-读指针污染)
  - [任务 4: JTAG 烧固件 + UART 永久归 ILA](#任务-4-jtag-烧固件--uart-永久归-ila)
- [2026-08-12](#2026-08-12)
  - [任务 1: TCP DstPort=80 根因 — byte-enable 仿真模型缺陷](#任务-1-tcp-dstport80-根因--byte-enable-仿真模型缺陷)
  - [任务 2: fpga_ila 集成 — IP 快照 + 插桩](#任务-2-fpga_ila-集成--ip-快照--插桩)
  - [任务 3: Vivado 时序约束修复 (WNS -1.76→+3.32ns)](#任务-3-vivado-时序约束修复-wns--176332ns)
  - [任务 4: 烧录 + 固件上传 + CPU 运行验证](#任务-4-烧录--固件上传--cpu-运行验证)
- [2026-08-11](#2026-08-11)
  - [任务 1: FPGA 烧录 + ILA 通信调试](#任务-1-fpga-烧录--ila-通信调试)
  - [任务 2: MAC TX 发包时序仿真验证](#任务-2-mac-tx-发包时序仿真验证)
  - [任务 3: TCP DstPort=80 根因分析 ❌ 已推翻](#任务-3-tcp-dstport80-根因分析--已推翻)
- [2026-08-07](#2026-08-07)
  - [任务 1: fpga_ila_local 虚拟环境搭建](#任务-1-fpga_ila_local-虚拟环境搭建)
  - [任务 2: 仿真标准模式验证](#任务-2-仿真标准模式验证)
  - [任务 3: GUI 启动脚本确认](#任务-3-gui-启动脚本确认)
  - [任务 4: Qt GUI 启动验证](#任务-4-qt-gui-启动验证)
  - [任务 5: ila_dump_to_vcd.py 探针更新 (13→27)](#任务-5-ila_dump_to_vcdpy-探针更新-1327)
  - [任务 6: Vivado bitstream 生成 — DRC 修复 + FPGA 烧录](#任务-6-vivado-bitstream-生成--drc-修复--fpga-烧录)
  - [任务 7: Qt GUI 端口修复 (COM4 → /dev/ttyUSB1)](#任务-7-qt-gui-端口修复-com4--devttyusb1)
  - [任务 8: ILA 串口通信调试 — 发现时序违规导致 fcapz 核不工作](#任务-8-ila-串口通信调试--发现时序违规导致-fcapz-核不工作)
  - [任务 9: ILA 时钟降频修复 (125MHz → 50MHz) 🔄](#任务-9-ila-时钟降频修复-125mhz--50mhz)
  - [ILA 标准模式使用问题总结](#ila-标准模式使用问题总结)
- [2026-08-06](#2026-08-06)
  - [任务 1: ILA BURST_PTR 数据回读修复](#任务-1-ila-burst_ptr-数据回读修复)
  - [任务 2: 硬件触发调试 — TRIG_MASK 默认值问题](#任务-2-硬件触发调试--trig_mask-默认值问题)
  - [任务 3: TCP SYN 帧注入 → SYN+ACK 验证](#任务-3-tcp-syn-帧注入--synack-验证)
  - [任务 4: ILA 探针全面扩展 (13 → 27 探针)](#任务-4-ila-探针全面扩展-13--27-探针)
  - [任务 5: 修复仿真 VCD 生成 — 内嵌 testbench 冲突](#任务-5-修复仿真-vcd-生成--内嵌-testbench-冲突)
  - [任务 6: ILA 标准模式重构](#任务-6-ila-标准模式重构)
  - [任务 7: fpga_ila Web GUI 环境搭建](#任务-7-fpga_ila-web-gui-环境搭建)
  - [任务 8: Vivado 上板编译配置](#任务-8-vivado-上板编译配置)
  - [ILA 标准模式使用问题总结](#ila-标准模式使用问题总结-1)
- [2026-08-05](#2026-08-05)
  - [第一阶段: Ping 通网络 ✅ 已完成](#第一阶段-ping-通网络--已完成)
  - [第二阶段: TCP 协议实现](#第二阶段-tcp-协议实现)
- [2026-08-04](#2026-08-04)
  - [任务 6: 学习 TCP 协议基础](#任务-6-学习-tcp-协议基础)

---

# 2026-08-27

> 状态: 通读项目文档（重点 README）✅ | 确定项目下一步 ✅（实现 fpga_webserver-main 项目）| 完整流程学习文档生成 ✅ | 先讲后考学习会话 🔄

## 任务 1: 通读项目文档（重点 README）+ 确定项目下一步 + 生成完整流程学习文档 ✅

### 目标
协议栈复习收尾后，通读参考工程 fpga_webserver-main README，梳理项目全局；确定项目下一步方向（实现 fpga_webserver-main 项目）；把完整流程沉淀成学习文档，供对照学习与问答自测。

### 进展和结果

**① 通读 fpga_webserver-main README（重点）**

`fpga_webserver-main` 是**双平台 FPGA Web 服务器参考工程**（PicoRV32 软核 + 裸机 C 协议栈），README 要点：

| 项 | 内容 |
|----|------|
| 定位 | 基于 PicoRV32 RISC-V 软核 + 裸机 TCP/IP 协议栈的轻量 FPGA Web 服务器 |
| 目标器件 | Xilinx XC7A35T-FGG484（Artix-7，RGMII 接口，Vivado）；Altera EP4CE10F17C6（Cyclone IV E，GMII 接口，Quartus II 13.1） |
| 系统架构 | `webserver_top`（平台相关）→ `webserver_wrapper`（平台无关）→ riscv32_top(PicoRV32) / lcpu_merge(总线仲裁) / reg_webserver(寄存器堆) + cpu_channel(包FIFO+DMA) + gmii2mac(以太网MAC) |
| PHY 接口 | Xilinx：RGMII 4bit DDR → `rgmii2gmii` 桥 → 内部 GMII；Altera：GMII 8bit SDR 直连 |
| 协议栈（C 固件） | eth（MAC 过滤）→ arp（请求/应答）→ ip（头解析/校验和）→ icmp（ping 应答）→ tcp（SYN/SYN-ACK/ACK/FIN/RST 状态机）→ http（GET/POST，网页读写寄存器） |
| 寄存器映射 | 版本 / 复位 / 时间(64bit) / 调试 / LED / 以太网统计 / MDIO 子总线 / CPU 包 FIFO / 调试 RAM(4KB) / 指令 RAM(64KB) / 外部总线 |
| Web 服务 | TCP/80，默认 IP 192.168.1.88 |
| 系统时钟 | 50MHz 输入 → 50/125/200MHz；PicoRV32 RV32IC；指令 RAM 48KB |
| 开发流程 | c_build（C 编译→InstructRAM）→ sim（icarus/verilator）→ 双平台 build_fpga.sh |

关键理解：
- **双平台共用核心**：`webserver_wrapper` 平台无关，仅顶层 + PLL + rgmii2gmii 桥平台相关。
- **协议栈全在 C 固件**：裸机无 OS，每层一个 .c（eth.c / arp.c / ip.c / icmp.c / tcp.c / http.c）。
- **与 RiscV_WebSoC_3 同源**：同为「PicoRV32 软核 + 裸机 C 协议栈」路线；Xilinx 路径（rgmii2gmii → gmii2mac → cpu_channel）即本项目硬件通路，可直接对照定位实现差异。

**② 项目全貌梳理**

| 维度 | 结论 |
|------|------|
| 项目定位 | PicoRV32 软核 + XC7A35T + RTL8211F 的 WebSoC，软核跑裸机 C 协议栈——「软核软件协议栈（路线①）」的极简手写版 |
| 里程碑 | 三步渐进 08-14 全部达成：ping 5/5（avg 1.778ms）、TCP 握手 5/5、HTTP 200 OK 3/3 |
| 学习进度 | 六层深挖（08-15~08-19）+ 扩展协议补齐（08-20）+ 六层复习（08-25~08-26），全部完成 |
| 当前状态 | 完整流程学习文档已生成，先讲后考学习会话进行中 |
| 下一步 | 实现 fpga_webserver-main 项目 |
| 开发约束 | RTL 核心通路不动（仅 ILA 实例段/探针可改）；C 固件可自由改；fpga_ila 只使用不修改源码 |

**③ 确定项目下一步**

| 项 | 结论 |
|----|------|
| 下一步 | **实现 fpga_webserver-main 项目**（通读 README 后确立的方向） |
| 再下一步 | 手写 UART / I2C 驱动（原计划顺延） |

**④ 完整流程素材提取（Workflow 并行 5 路 + 第一手精读）**

| 来源 | 覆盖 |
|------|------|
| 固件源码 | main/eth/arp/ip/icmp/tcp/http.c + inc 头文件（第一手逐行） |
| RTL RX 路径 | rgmii_to_gmii → gmii2mac → mac_rx → package_fifo_v2 → cpu_channel → lcpu_fpga_test |
| RTL TX + 时钟 | webserver_cpu_top（MMCM 四路时钟 + ILA 双核 + UART 归属）、mac_tx、gmii_to_rgmii |
| 开发工作流 | run_sim.sh（iverilog+BFM）、c_build（交叉编译→InstructRAM.tcl）、build_xilinx（Vivado+updatemem+JTAG）、ila_gui |
| 寄存器映射 | lcpu_general.h + lcpu_fpga_test.v 全量 `_RD`/`_WR` 地址 |

**⑤ 学习文档生成**

生成 [RiscV_WebSoC_完整流程学习](RiscV_WebSoC_完整流程学习.md)（已加入 Obsidian 镜像）：
- **第一部分**：完整流程总结——系统架构总图、收包/发包全链路、固件分发树、TCP 状态机、寄存器映射、三层封包与校验和、开发工作流、扩展知识（RGMII / CDC / CRC32 / ILA 双核 / rdcycle / 零拷贝）、下一步 UART 学习要点。
- **第二部分**：对应问答——A~G 七组 26 题，覆盖架构 / 收包 / 固件 / 发包 / 寄存器 / 开发流程 / 边界。

> 全程对照实际源码，修正了设计文档的出入：当前 sim 用 `run_sim.sh`（无 Makefile/tb_fast.v）、固件无 `web_app.c/designApp.c`、`MAX_TCP_CONN=4`（非 16）。

### 收获到的经验
1. fpga_webserver-main README 给出参考工程全貌：双平台共用平台无关核心 + C 固件分层协议栈，与 RiscV_WebSoC_3 同源（PicoRV32 + 裸机协议栈 + GMII MAC），对照可快速定位实现差异。
2. 学习文档要「对照实际代码而非设计文档」——设计文档与实现有出入（文件结构、连接数、仿真入口）。
3. 先讲后考：文档是底稿，讲解串主线，质询逼自查每个分支是否真懂。
4. 项目文档体系互补：日报记「做了什么」、学习计划沉淀「学到了什么」、项目状态汇总「现在在哪」，维护好即是长期资产。
5. 通读历史日报可还原每一步决策链（JTAG 烧固件 vs UART 上传、FIN piggyback 单包等），后续遇同类问题不必重新踩坑。

### 下一步
先讲后考学习会话进行中：先完整讲解流程，再按 grill-me 逐段质询 → 实现 fpga_webserver-main 项目。

---

# 2026-08-26

> 状态: 复习 ip/tcp/http ✅（对照 [网络协议学习计划](网络协议学习计划.md)）| 下一步: 手写 UART + I2C 驱动

## 任务 1: 复习 ip/tcp/http

### 目标
昨日已完成 eth/arp/icmp 底层三层的复习，今日补全上层三层（IP / TCP / HTTP），六层协议栈至此全部过完。仍对照 [网络协议学习计划](网络协议学习计划.md) 与 `c/ip.c`、`c/tcp.c`、`c/http.c` 源码，自底向上逐层梳理。

### 进展和结果

**① IP 层（ip.c）— 校验、分派、对调重填**

`ip_proc()` 的职责分收包校验与分派两步：

| 环节 | 关键动作 |
|------|---------|
| 收包校验 | ver/IHL 高 4 位 = `0x40`（IPv4 + 头长 20B）、IHL ≥ 5、目的 IP = 本机 `169.254.1.1` |
| 读取分派 | 读 `src_ip` / `ip_protocol_type` / `ip_total_len` 三个全局量，按协议号分派：1→ICMP、6→TCP |
| 回包构造 | `ip_header_update()`：复制 20B IP 头，仅替换 total_len / src_ip / dst_ip / checksum，其余照抄——与 ARP「对调重填」同一套路 |
| 头校验和 | `ip_header_checksum()`：只遍历 20B 头，跳过 checksum 字段、替换 total_len，逐字节累加后取反 |

头校验和三个 if 分支，各对应一个设计理由：

| if 分支 | 对应设计理由 |
|---------|-------------|
| `i==OFF_IP_CHECKSUM` 清零自身 | 校验和定义为「反码求和再取反」，计算时自身 2 字节尚无值，先当 0 参与求和 |
| `i==OFF_IP_TOTAL_LEN` 替换 total_len | 回包 payload 长度与请求不同，total_len 变化且被校验和覆盖，必须按新值注入再算 |
| 其余逐字节求和（仅 20B 头） | TTL 每跳减 1，若校验和覆盖数据则每个路由器均需整包重算；只算头，改完 TTL 可做 O(1) 增量更新 |

> 项目边界：ip.c 不做分片重组、不减 TTL（回包直接照抄收到的 TTL）。局域网小包 + 直连单跳场景无影响；跨网段、大包才需补全。

**② TCP 层（tcp.c）— 连接表 + 状态机**

本层为六层中最复杂的一层。`tcp_parse_header()` 解析端口、seq、ack、DataOffset（高 4 位 × 4 = 头长）、flags、window，并计算 `data_len = IP总长 - IP头 - TCP头`。

连接表 `conn_table[4]` 是「面向连接」的代码落地：

| 字段 | 含义 |
|------|------|
| state | 连接当前状态 |
| remote_ip / remote_port | 对端地址（连接定位依据） |
| local_seq / remote_seq | 双方序号（字节流位置指针） |
| retry_count / timeout | 超时重传计数与期限 |

三次握手流程：

| 步骤 | 动作 | 状态变化 |
|------|------|---------|
| 1 | 收 SYN → 分配空槽位、记录 remote_ip/port/seq | LISTEN → SYN_RECEIVED |
| 2 | 回 SYN+ACK（ACK = 对方 seq+1） | — |
| 3 | 收 ACK → 本地 `local_seq+1`（自身 SYN 亦占一序号） | SYN_RECEIVED → ESTABLISHED |

两个易混淆、需反复确认的要点：

| 要点 | 说明 |
|------|------|
| 校验和伪首部 | 12B 伪首部（源IP + 目的IP + 0 + 协议号6 + TCP长度）不实际发送，但计算校验和必须计入——使 TCP 可顺带校验 IP 地址，防止 IP 层投错包 |
| 校验和覆盖范围 | **IP 仅算 20B 头 / ICMP 算整个报文 / TCP 为伪首部+头+数据**，三种范围各不相同 |

状态机 7 状态（CLOSED / LISTEN / SYN_RECEIVED / ESTABLISHED / CLOSE_WAIT / LAST_ACK / TIME_WAIT），`tcp_proc` 按状态 switch 分发。四次挥手：收 FIN → 回 FIN+ACK → LAST_ACK → 收 ACK → 释放槽位。超时重传由 `tcp_timer_check` 借助 `rdcycle` 硬件计时器实现，SYN+ACK 超时重发最多 3 次。

流量控制与拥塞控制对比：

| 项 | 滑动窗口（rwnd） | 拥塞控制（cwnd） |
|----|------------------|------------------|
| 保护对象 | 接收方缓冲 | 网络（路由器队列） |
| 有效窗口 | min(rwnd, cwnd) | 同上 |
| 本项目 | 无拥塞控制（响应一发即 FIN，触发不了） | — |

**③ HTTP 层（http.c）— 最薄的一层**

底层已铺好可靠字节流，HTTP 仅关心字节内容。`http_proc()` 处理逻辑：

| 输入 | 动作 |
|------|------|
| payload 前 3 字节 = "GET" | 返回写死响应：状态行 `HTTP/1.1 200 OK` + `Content-Type` + `Connection: close` + 空行 + HTML body |
| 其他 | 仅回 FIN 关闭连接 |

关键设计：**FIN 与 DATA 合并单包发送（piggyback）**，flags = ACK|PSH|FIN，避免背靠背两包导致 DATA 丢包——即 08-14 HTTP 网页打通之根因。

四个设计「为什么」汇总（详见 [网络协议学习计划](网络协议学习计划.md) §6）：

| 设计选择 | 理由 |
|----------|------|
| 文本协议 | 人可直接读、可 telnet/nc 手敲调试；代价为费带宽、逐字节解析 |
| 只实现 GET | 服务器职责单一（吐固定网页），仅比较前 3 字节即可 |
| 状态码按百位分层 | 首位即标识问题归属（2xx 成功 / 4xx 客户端 / 5xx 服务端） |
| Connection: close | 短连接免维护 keep-alive 池，关连接即响应结束信号 |

**④ 三种校验和覆盖范围（复习要点）**

| 层 | 校验和覆盖范围 |
|----|----------------|
| IP | 仅 20 字节头（`ip_header_checksum`） |
| ICMP | 整个报文：8B 头 + payload（`icmp_body_checksum`） |
| TCP | 12B 伪首部 + 头 + payload（`tcp_calc_checksum_payload`） |

### 遇到的问题
无，纯复习。唯一反复确认之处为三种校验和覆盖范围——属最易混淆点，每次复习均需重述。

### 收获到的经验
1. 回包套路六层统一为「对调重填」：ARP 对调身份、IP 对调 src/dst、TCP 对调端口与 seq/ack，均为换位 + 照抄 + 重算校验和。
2. 字段偏移即协议栈地图：`OFF_IP_SRC_IP=26`、`OFF_TCP_SEQ_NUM=38` 由 14(eth)+20(ip)+相对偏移逐层累加而来，代码中几乎无魔数。
3. 校验和覆盖范围：IP 只头 / ICMP 头+数据 / TCP 伪首部+头+数据，三者不可混用。

### 下一步
六层复习完毕，跨层总纲收尾待后续补。

---

# 2026-08-25

> 状态: 复习 eth/arp/icmp ✅（对照 [网络协议学习计划](网络协议学习计划.md)）| CRC32 LFSR 并行实现 ✅ | 下一阶段: 手写 UART + I2C 驱动

## 任务 1: 复习 eth/arp/icmp

### 目标
对照 [网络协议学习计划](网络协议学习计划.md) 和 `c/eth.c`、`c/arp.c`、`c/icmp.c` 源码，把三层最底层协议从头到尾复习一遍。

### 进展和结果

**① 以太网层（eth.c）—「边收边发，省一步」**

`eth_proc()` 干两件事（`c/eth.c:36`）：
1. 读 EtherType（字节 12-13）→ 0x0806 走 ARP、0x0800 走 IP、其他丢弃；
2. 走 IP 前先比目的 MAC 是不是本机（`Local_MAC`），不是就 `NO_PROC` 丢弃；
3. 匹配通过就 `eth_write_tx_header()` 预写 14 字节以太网头：源 MAC 填本机、目的 MAC 抄收到的源 MAC、EtherType 照抄。

> 关键设计：**回包的 MAC 头提前写好**，上层 ip/tcp 构造时不用再管 MAC 层，只往上堆 IP/TCP 头。每 PUSH 一个包是独立 block，所以每发一个包都要重写一次 eth 头（`eth.c:6` 注释）。

**② ARP 层（arp.c）—「广播问、单播答」**

`arp_reply()`（`c/arp.c:4`）前置过滤三条件全满足才处理：
- 目的 MAC 是本机 **或** 全 FF（广播）；
- target IP（字节 38-41）== 本机 `Local_IP_ADDR`；
- opcode == REQUEST(1)。

构造应答分 9 段「对调重填」：目的 MAC ← RX 源 MAC、源 MAC ← 本机、EtherType 照抄、opcode 改 REPLY(2)、sender MAC/IP ← 本机、target MAC/IP ← RX 的 sender。最后补 0 到 64 字节 push（`arp.c:100-105`）。

> 本项目是「纯应答服务器」，固件里没有 ARP 表——只被动回，从不主动发 ARP request（对照学习计划 §2「本项目的差异」）。

**③ ICMP 层（icmp.c）—「零拷贝流水线」**

`icmp_reply()`（`c/icmp.c:55`）三步：
1. `ip_header_update()` 交换 src/dst IP 并重算 IP 校验和；
2. 写 ICMP 头：type 改 0（ECHO_REPLY）、code 填 0，id/seq 和 payload 原样抄回（不搬出来再搬回去，直接 RX→TX 逐字节）；
3. `icmp_body_checksum()` 算校验和写回。

> 校验和覆盖「整个 ICMP 报文」（8B 头 + payload），**不是** IP 头那种只算 20B；type 用改完的 ECHO_REPLY(0) 参与算，payload 奇数长度末尾补 0（`icmp.c:47-51`）。

**④ 三层关系**

```
以太帧(14B) 打底 → 中间按 EtherType 嵌套：
  0x0806 → ARP 报文 (28B)
  0x0800 → IP头(20B) + ICMP/TCP
```

偏移累加规律：ARP 字段 = 14 + 相对偏移；ICMP/TCP 字段 = 14 + 20(IP头) + 相对偏移。`tx_pkt_len = eth(14) + ip_total_len + 4`，那个 `+4` 是 FCS（`icmp.c:62`）。

**⑤ FCS / CRC32 的 LFSR 并行实现（对照 `rtl/crc.v` + `rtl/gmii2mac.v`）**

以太网帧尾的 FCS(4B) 就是 CRC32。两种实现方式：

| 方式 | 代码 | 原理 | 特点 |
|------|------|------|------|
| 串行 LFSR | `gmii2mac.v:137` `crc32_eth` | 每 bit 进一次移位寄存器，`if (crc[0]) crc = (crc>>1) ^ 0xEDB88320` | 用反射多项式 `0xEDB88320`（= `0x04C11DB7` 位反转），逐字节循环 8 次 |
| 并行 LFSR | `crc.v:73-274` `fun_crc32_d8/d16/d32` | 把串行 8/16/32 拍**展开成一条组合逻辑表达式**，一个周期算完 | 每 bit 输出的 `+` 就是异或；一次进 8bit（GMII 一个时钟），千兆 125MHz 每拍算一个字节 |

关键理解：
- **多项式本质**：`G(x)=x³²+x²⁶+x²³+x²²+x¹⁶+x¹²+x¹¹+x¹⁰+x⁸+x⁷+x⁵+x⁴+x²+x+1` → `0x04C11DB7`；按位 LFSR 就是拿这个多项式做模 2 除法。
- **为什么能并行**：串行 LFSR 是「上一拍 crc 影响下一拍」，但移位+异或全是线性的——8 拍的传播路径可以**代数展开**成 `crc_out = f(crc_in, data_in[7:0])`，每位的 XOR 树就是展开结果（看 `fun_crc32_d8`：`crc_out_tmp[0] = crc_t[24]+crc_t[30]+data_in[0]+data_in[6]` 这种一项项加起来）。
- **反射多项式**：以太网按位反转输入字节 + 反转多项式 + 输出取反，`0x04C11DB7` → 反转 → `0xEDB88320`。`gmii2mac` 用的就是反射版（直接对帧字节流算），`crc.v` 并行版同理 `data_in_rev` 位反转输入。
- **GMII 一拍一字节**：GMII 数据 8bit @125MHz，正好每拍算一个字节，用 `fun_crc32_d8` 即可；需要更宽可上 d16/d32（32bit 域）提高吞吐。

---

# 2026-08-24

> 状态: RGMII/GMII 接口原理 ✅ | 协议交互原理（ping 全链路）✅ | 下一阶段: 手写 UART + I2C 驱动

## 任务 1: RGMII/GMII 接口实现原理

### 目标
搞清 RGMII 和 GMII 的实现原理：为什么 RGMII 4bit DDR 能等效 GMII 8bit SDR。

### 进展和结果

**① 接口对比**

| 项 | GMII | RGMII |
|----|------|-------|
| 数据线 | 8bit SDR（单沿采样） | 4bit DDR（上下沿各传 4bit） |
| 时钟 | 125MHz（千兆） | 125MHz（双沿等效 250M 传输率） |
| 引脚数 | ~24 根 | ~12 根（减半） |
| 用途 | 芯片间/板内 MAC↔PHY | 板级 MAC↔PHY（省引脚） |

**② 实现原理（为什么 4bit DDR 等效 8bit）**

- RGMII 在时钟上升沿传低 4 位、下降沿传高 4 位，一个时钟周期凑齐 8bit → 等效 GMII 8bit @125MHz。
- FPGA 侧 `rgmii_gmii_bridge`（`rtl/webserver_cpu_top.v:233`，`idelay_refclk=clk_200m`）拆两个方向：
  - **TX**（`rtl/gmii_to_rgmii.v`）：`ODDR`(OPPOSITE_EDGE)，`D1=gmii_txd[3:0]`/`D2=gmii_txd[7:4]`；`TXC` 由 ODDR(D1=1,D2=0) 产生 125MHz；`TX_CTL` D1=TX_EN/D2=TX_EN^TX_ER。
  - **RX**（`rtl/rgmii_to_gmii.v`）：`RXC` 经 `BUFG` → 全局时钟；`RXD[3:0]+RX_CTL` 各接 `IDELAYE2`(FIXED 20tap×78ps≈1.56ns) 移到采样窗口中央消 hold 违例；`IDDR`(SAME_EDGE_PIPELINED) Q1(posedge)=低4位、Q2(negedge)=高4位 → `gmii_rxd[3:0]=Q1, [7:4]=Q2`；`gmii_rx_dv=Q1, gmii_rx_er=Q1^Q2`。
- `IDELAYCTRL` 必须例化（REFCLK=200MHz），否则 `IDELAYE2` 不工作。

**③ 本项目时钟域划分**

- RGMII DDR 引脚在 125MHz 域，靠 IDDR/ODDR 转成内部 GMII SDR。
- `gmii2mac` 只有 `clk`(125m) 与 `Eth_RXC` 两个时钟，TX 走 `clk`、RX 走 `Eth_RXC`；`clk_125m_tx`(90°移相) **只**服务 bridge 的 DDR 输出引脚，soft ILA 不采。
- ILA 核1 @125MHz 抓 GMII/MAC 单拍 SOP/EOP 脉冲；核0 @50MHz 抓 CPU 域。

### 结论
**RGMII = GMII 引脚减半版**：用 4bit + DDR（双沿采样）达到 GMII 8bit 的吞吐，代价是 125MHz 双沿时序（IDDR/ODDR/IDELAYE2）更讲究。本板 RTL8211F 走 RGMII 接 FPGA。

### 收获到的经验
1. **DDR 是「一个时钟干两件事」**：RGMII 靠上下沿各传 4bit 凑 8bit，引脚减半、时序复杂度加倍。
2. **接口转换在 PHY 边界一次性完成**：板外是 RGMII，FPGA 内部统一转 GMII，后面 MAC/CPU 全不用感知 DDR。

---

## 任务 2: 协议交互原理 — ping 全链路

### 目标
把 ping 从 PC 发起 FPGA 回复的**完整协议交互过程**串起来：谁先谁后、每层靠什么字段决定下一步。

### 进展和结果

**① ping 触发 ARP 的完整时序**

| 步骤 | 方向 | 协议 | 关键字段/动作 |
|------|------|------|----------------|
| 1 | PC→广播 | ARP Request | 查 ARP 缓存无 169.254.1.1 → 广播 `目标 IP=169.254.1.1`，目的 MAC=`FF:FF:FF:FF:FF:FF` |
| 2 | FPGA→PC | ARP Reply | 单播回包，告诉 PC「169.254.1.1 = 我的 MAC」 |
| 3 | PC→FPGA | ICMP Echo Request | PC 已有映射，发 `type=8`，IP 协议号=1 |
| 4 | FPGA→PC | ICMP Echo Reply | FPGA 回 `type=0`，校验通过即回 |

**② 每层靠什么字段分派（选择子逐层传递）**

| 层 | 字段 | 值→动作 |
|----|------|---------|
| 以太网层 | EtherType | `0x0800`=交给 IP、`0x0806`=交给 ARP |
| IP 层 | 协议号 | `1`=ICMP、`6`=TCP、`17`=UDP |
| TCP 层 | DstPort | `80`=HTTP、`1883`=MQTT |

**③ 与项目实现的对应**

- `c/eth.c`：剥以太头，按 EtherType 分发。
- `c/ip.c` `ip_proc()`：校验 ver/IHL、目的 IP==本机、缓存源 IP、按协议号分发。
- `c/icmp.c`：回 Echo Reply（校验和 + 交换 IP/ICMP 字段）。
- `c/arp.c`：校验目标 IP（帧字节 38-41，`target_ip=(target_ip<<8)|byte[i]`）== `0xA9FE0101`(169.254.1.1 大端)、广播 MAC 判断、opcode 判断 → 全过才回 ARP Reply。
- 硬件链路：PHY(RTL8211F) → `rgmii_gmii_bridge` → `gmii2mac` → `cpu_channel` FIFO → CPU 固件解析 → 回包反向往外发。
- **实测**：`ping -c5 169.254.1.1` → **5/5 包、0% 丢、avg 1.778ms**，ARP+ICMP 全链路跑通。

### 结论
**一次 ping = ARP 建映射 + ICMP 应答**：先「问 MAC」（广播请求/单播应答），再「echo 探测」（请求/应答），每层靠一个选择子字段（EtherType / 协议号 / 端口）决定往上交给谁。这也是为什么 HTTP 网页能通的前提是 ping 先通——链路每一级都是下一级的前置。

### 收获到的经验
1. **分层 = 每层一个选择子**：EtherType 分 ARP/IP，协议号分 ICMP/TCP/UDP，端口分 HTTP/MQTT。往上加协议只加分支，下面几层不动。
2. **ping 通了 ≠ 协议栈通了**：ping 只覆盖 ARP+ICMP，TCP 握手才验证到传输层，HTTP 才到应用层——三步渐进（ping→握手→网页）正好对应一层层往上验证。

---

# 2026-08-20

> 状态: MQTT 扩展 ✅ | 其他协议补齐（UDP、DHCP、DNS、NTP、HTTPS、SSH、WebSocket、CoAP）✅ | MQTT 同类协议（AMQP、DDS、XMPP、STOMP、Kafka、Matter）✅ | 下一阶段: 手写 UART + I2C 驱动

## 任务 1: MQTT 扩展

### 目标
补一个「如果再往上加 MQTT，对应哪一层」的知识点，回答 MQTT 在协议栈里的定位。

### 进展和结果
在 [网络协议学习计划](网络协议学习计划.md) 补上 MQTT 扩展：**MQTT 和 HTTP 是同一层的协议，都属应用层（L7）**，不是新的一层，对应现在的 `http.c` 这一层。

- 分派同套路：TCP 按端口往上交——80→HTTP、**1883→MQTT**。`ip.c` 不变，只改 `tcp.c:399` 的 DstPort 判断加个 1883 分支，再加个 `mqtt.c`。
- 和 HTTP 的取舍对比：请求/响应 vs 发布/订阅、文本 vs 二进制、短连接 vs 长连接——正好是 HTTP 那几个「为什么」的反面，MQTT 天生给 IoT 受限设备设计。

### 收获到的经验
1. **MQTT 不是新层，是 L7 的另一个协议**：和 HTTP 平行，都靠 TCP 端口分派。
2. **「端口」这个选择子字段能挂任意多个应用**：80=HTTP、1883=MQTT，加协议只加端口分支，下面四层（eth/ip/tcp）不动。

### 下一步
（仅知识扩展，不影响当前主线：手写 UART + I2C 驱动。）

---

## 任务 2: 其他协议 — UDP

### 目标
补齐 TCP 的「无连接版」UDP，理解为什么可靠传输之外还需要一个「快但不可靠」的协议。

### 进展和结果
在 [网络协议学习计划](网络协议学习计划.md) 补上 UDP：IP 协议号 17 = UDP。

**UDP 与 TCP 相反的四点**
- 无连接（不握手直接发）、不可靠（不确认不重传不排序）、无拥塞控制、头仅 8 字节（比 TCP 20 字节轻一半）。
- 为什么还要 UDP？「可靠」有代价——握手/重传带来延迟与开销；音视频/游戏/直播要低延迟、丢几帧无所谓；DNS 一问一答、DHCP 广播这类简单交互用 UDP 省三次握手。
- 一句话：**TCP 要「可靠」，UDP 要「快」**。

### 收获到的经验
1. **可靠 vs 快是取舍**：UDP 不是「差劲的 TCP」，而是「要快就牺牲可靠」的另一个选择。
2. **本项目边界**：ip.c 只实现 1=ICMP、6=TCP，17=UDP 已认识但没实现。

---

## 任务 3: 其他协议 — DHCP、DNS、NTP

### 目标
补三个「让网络跑起来」的基础设施协议，说清本项目为什么用 169.254.1.1、为什么直接敲 IP。

### 进展和结果
在 [网络协议学习计划](网络协议学习计划.md) 补上 DHCP/DNS/NTP，三个都跑在 UDP 上。

| 协议 | 端口 | 干什么 | 为什么需要 |
|------|------|--------|-----------|
| DHCP | 67/68 UDP | 自动发 IP/掩码/网关/DNS | 手动配 IP 易冲突 |
| DNS | 53 UDP/TCP | 域名 → IP | 人记不住 IP |
| NTP | 123 UDP | 同步时间 | 日志/证书靠统一时间 |

- **DHCP 流程 DORA**：Discover→Offer→Request→Ack。本项目不用 DHCP，写死 169.254.1.1——「链路本地地址」是无 DHCP 时的自动 fallback，只能局域网内通信。
- **DNS**：递归+迭代从根域问下来。本项目直接敲 IP、跳过 DNS——DNS 是「给人类记名字」，机器之间还是靠 IP。
- **NTP**：stratum 分层对时。

### 收获到的经验
1. **169.254.1.1 是链路本地地址**：没有 DHCP 时的自动 fallback，解释了为什么直连 PC 能 ping 通、但上不了公网。
2. **DNS 是「给人用的」**：机器最终都靠 IP 通信，域名只是人类友好的别名。

---

## 任务 4: 其他协议 — HTTPS、SSH、WebSocket、CoAP

### 目标
补齐四个 L7 应用层协议，看它们和 HTTP/MQTT 的取舍对比。

### 进展和结果
在 [网络协议学习计划](网络协议学习计划.md) 补上四个 L7 协议，都靠端口分派。

| 协议 | 端口 | 场景 | 一句话 |
|------|------|------|--------|
| HTTPS | 443 | 加密网页 | HTTP + TLS 加密，防偷看篡改 |
| SSH | 22 | 加密远程登录 | 替代明文 telnet |
| WebSocket | 80/443 | 双向实时 | HTTP 握手后升级全双工 |
| CoAP | 5683 UDP | IoT | UDP 版的 HTTP，给受限设备 |

- **HTTPS/TLS**：HTTP 明文可被中间人偷看/篡改，TLS 加一层加密（证书验身份 + 对称加密传数据）。本项目 HTTP 明文，局域网够用、上公网要 HTTPS。
- **SSH**：加密远程登录，替代明文 telnet。
- **WebSocket**：HTTP 带 `Upgrade: websocket` 头升级成双向长连，聊天/推送用。
- **CoAP**：给「连 MQTT 都嫌重」的设备，跑 UDP、二进制、极简，是 UDP 版 HTTP。

### 收获到的经验
1. **L7 协议按「场景」选型**：网页 HTTPS、远程 SSH、实时 WebSocket、IoT CoAP/MQTT，都是「可靠字节流之上解决不同问题」。
2. **加密是公网刚需**：局域网明文够用，一旦出局域网就得 TLS/SSH。

### 下一步
「其他协议」补齐。回到主线：手写 UART + I2C 驱动。

---

## 任务 5: 其他协议 — MQTT 同类协议

### 目标
补「和 MQTT 类似」的消息 / 物联网应用层协议族，重点是看清：对这块 RISC-V 软核 + 千兆 PHY 来说，哪些真会跑、哪些只是行业背景。

### 进展和结果
在 [网络协议学习计划](网络协议学习计划.md) 补上第 11 节：AMQP、DDS、XMPP、STOMP、Kafka、Matter（CoAP 已在第 10 节）。

| 协议 | 端口 | 模型 | 一句话 | 对这块 FPGA |
|------|------|------|--------|-------------|
| AMQP | 5672 | broker + exchange/queue | 企业消息队列，功能全但重 | 软核不实现，认识即可 |
| DDS | 动态 UDP | 无 broker 点对点 | 实时确定性 QoS，机器人/军工 | 确定性控制类 FPGA 常当 DDS 数据节点 |
| XMPP | 5222 | 联邦 client-server | XML 即时通讯 + presence | 太重，presence 用不上，认识即可 |
| STOMP | 61613 | broker 队列/订阅 | 纯文本，简单 | 省流量不如 MQTT，边缘不划算 |
| Kafka | 9092 | 分布式日志 | 高吞吐事件流，可回放 | FPGA 当 producer 往上游推数据（DAQ） |
| Matter | 5540 | 无 broker 请求/订阅 | 智能家居 over IP 标准 | FPGA 更像以太网桥/网关，非端点 |

三条主线对比：**有没有 broker**（MQTT/AMQP/STOMP/Kafka 要，DDS/Matter 不要）、**重量**（MQTT/CoAP 轻，XML/文本重）、**可靠 vs 实时**（Kafka/AMQP 偏可靠可回放，DDS 偏确定性实时）。

### 收获到的经验
1. **对这块软核，真正会落地的只有 MQTT**：已有 TCP 栈，加 1883 端口分支 + mqtt.c 就成 IoT 边缘节点；CoAP 要等 UDP 层（ip.c 还没做 17=UDP），优先级低。
2. **Kafka 和 MQTT 几乎正交**：一个「事后可查的日志」、一个「实时推送」；FPGA 在 Kafka 体系里是 producer（数据采集出口），不是跑 Kafka 本身。
3. **DDS 最贴合 FPGA 的「确定性时序」本性**：机器人/军工里 FPGA 常是 DDS 实时数据节点；Matter 则更像以太网桥/网关，不是这块软核的活。

### 下一步
消息 / 物联网协议族补齐。回到主线：手写 UART + I2C 驱动。

---

# 2026-08-19

> 状态: IP 深入（续）✅（校验和设计 / TTL 默认值）| 网络协议栈收尾 ✅（HTTP 深入 + 六层总纲）| 下一阶段: 手写 UART + I2C 驱动

## 任务 1: IP 深入（续）

### 目标
接着 08-18 的「IP 层为什么这样设计」，继续深挖到代码级：`c/ip.c` 里 `ip_header_checksum` 的三处 if 分支各对应哪个设计理由；顺带把 TTL 加深到「初始值为什么取 64」。

### 进展和结果
在 [网络协议学习计划](网络协议学习计划.md) 补上 IP 层第 4 个「为什么」（校验和设计），并把 TTL 一节加深到「四个为什么」。

**为什么 IP 校验和只算头、还要「清零自己 + 先替换 total_len」？**（对应 `ip_header_checksum`，ip.c:57-76）
- 只算 20 字节头、不算数据：TTL 每跳减 1，若覆盖数据每个路由器都得重算整个包；只算头，改完 TTL 做 O(1) 增量更新即可。数据完整性交给 L4（TCP/UDP/ICMP 各自的校验和）。
- 清零 checksum 自己：校验和是「反码求和再取反」，算时自己那 2 字节还没值（鸡生蛋），先当 0 参与；验证端含它再求和应得 0xFFFF。
- 先替换 total_len：TCP 应答 payload 长度常变，total_len 字段被校验和覆盖，必须按「新值」注入再算，不能照抄收到值。
- 三处对应三个 if：`i==OFF_IP_CHECKSUM` 清零、`i==OFF_IP_TOTAL_LEN` 替换、其余逐字节求和。

**为什么初始 TTL 取 64？**
- Linux 默认 64、Windows 128——够穿过绝大多数路径（公网最多 ~30 跳），又是 2 的幂好记。
- 副作用：能靠「收到包的 TTL」被动猜对方 OS（64→Linux、128→Windows），成了被动指纹的一种。

### 收获到的经验
1. **IP 校验和是「头完整性」不是「数据完整性」**：只保路由决策用到的 20 字节头，数据交给 L4。
2. **「清零自己再求和」是校验和的通用套路**：自己加自己的反码恒等于全 1（0xFFFF），验证端统一。
3. **回包算校验和要按「新 total_len」**：字段变了必须先替换再算，不能照抄旧值。

### 下一步
网络协议栈代码（IP/TCP/HTTP）学完收尾后，开始手写 **UART** 和 **I2C** 驱动代码。

---

## 任务 2: 网络协议栈收尾

### 目标
补上「每层为什么这样设计」系列的最后一层 HTTP（方法 / 状态码 / 文本协议取舍），再做一张六层总纲表收尾，正式结束网络代码学习。

### 进展和结果
在 [网络协议学习计划](网络协议学习计划.md) 补上 HTTP 层「为什么这样设计」四个点，并新增「六层一张表」收尾总纲。

**为什么 HTTP 是文本协议？** 人能直接读（telnet 手敲、抓包一眼看懂）→ 好调试；代价是费带宽、解析要逐字节比字符串。HTTP/2 改二进制帧说明这是「调试 vs 带宽」的阶段取舍。

**为什么极简服务器只要 GET？** 方法 = 对资源的动作（GET 读 / POST 提交 / DELETE 删），本服务器只「吐写死的页面」，只要 GET；http.c 只比前 3 字节 `GET`，够用就好。

**为什么状态码按百位分层？** 首位告诉「问题归谁」：2xx 成功 / 4xx 客户端错 / 5xx 服务器错，机器看数字、人看 reason phrase。本项目只回 200 和 FIN，404 都不回。

**为什么 Connection: close（短连接）？** 一个请求一条连接、响应完关（FIN 走完整挥手）→ 服务器极简；代价是每请求都重新握手。HTTP/1.1 改 keep-alive，Content-Length/chunked 就是为「不关连接也能知道 body 结束」而生。

### 收获到的经验
1. **HTTP 是文本协议的极致取舍**：拿带宽换可调试性；二进制协议（HTTP/2）是相反的取舍。
2. **极简 = 只实现要用的那一小撮**：http 只要 GET、状态码只回 200，砍到边界为止。
3. **短连接让服务器免管 body 边界**：关连接本身就是「响应结束」信号，是 keep-alive 前的老办法。

### 下一步
网络协议栈（eth / arp / icmp / ip / tcp / http）学习正式收尾 ✅。下一阶段：手写 **UART** 和 **I2C** 驱动代码。

---

# 2026-08-18

> 状态: IP 深入 ✅ | TCP 深入 ✅（滑动窗口 / 拥塞控制 / 序号回绕）

## 任务 1: IP 深入

### 目标
深挖 IP 层「为什么这样设计」三个关键点（分片、TTL、协议号分派），接上「每层为什么这样设计」系列。

### 进展和结果
在 [网络协议学习计划](网络协议学习计划.md) 补上 IP 层「为什么这样设计」三个关键点，结合 `c/ip.c` 梳理。

**为什么 IP 要分片？**
- IP 跑在异构链路上，每条链路 MTU 不同（以太网 1500、PPP 更小），不能假设沿途都装得下同一个包 → 太大就切成几片、目的地再拼回。
- 三字段配合：`Identification`(16bit 归组) + `Fragment Offset`(13bit 定位, **单位 8 字节**) + `MF`(标记是否还有后续)。
- 单位 8 字节的巧思：13bit 按 1 字节只到 8191，按 8 字节能覆盖 8191×8=65528，正好装下 65535 的 IP 包——字段宽度与覆盖范围的折中。
- DF 位（不许分片）：分片有代价（丢一片整包重传 + 重组缓冲 + 攻击面），TCP 靠 DF=1 + ICMP「需要分片」做路径 MTU 发现。

**为什么要有 TTL？**
- 核心问题：路由环路（A→B→A）会让包永动、白占带宽。TTL 设「最多跳几跳」上限，每过一路由器减 1，减到 0 丢 + 回 ICMP「Time Exceeded」→ 环路里的包转几圈就死。
- 名字叫 Time 实为跳数：最早按秒减，但路由器不校时、缓存微秒级，RFC 1812 定为「每跳减 1」。
- 副产品：traceroute 靠 TTL=1/2/3… + ICMP Time Exceeded 一程程描路径（接昨天的 ICMP「报错」用途）。

**协议号分派为什么这样设计？**
- IP 头 8bit「协议号」（偏移 23）回答：payload 里装的是谁——1=ICMP、6=TCP、17=UDP。
- 和以太网 EtherType 同一套路：每层留一个「类型/选择子」字段指向上层，一层只做一步分派。

| 层 | 字段 | 值 → 分派到 |
|----|------|-------------|
| 以太网 L2 | EtherType (2B) | 0x0800→IP, 0x0806→ARP |
| IP L3 | Protocol (1B) | 1→ICMP, 6→TCP, 17→UDP |
| TCP/UDP L4 | 端口 (2B) | 80→HTTP, 443→HTTPS, 53→DNS |

- 为什么每层都要分派字段：分层——以太网不该懂 TCP/UDP，IP 不该懂应用，每层只看上一层一步。
- 为什么是这些数字：历史登记，ICMP(1)/TCP(6) 出自 RFC 790(1981)，UDP(17) 稍后，IANA 统一管。
- 本项目 ip.c 读 `ip_protocol_type`(偏移23) 分派 1→icmp、6→tcp，和 eth.c 读 EtherType 分派 arp/ip 是同一招。

### 收获到的经验
1. **分片三字段分工**：Identification 归组、Offset 定位、MF 标记结束，缺一个都拼不回来。
2. **TTL 是防环路的跳数上限**，不是真时间；副产品养活了 traceroute。
3. **分派字段是每层标配**：L2 EtherType → L3 Protocol → L4 端口，一层一步、职责单一，是「分层」的具体体现。
4. **本项目边界**：ip.c 不分片重组、TTL 照抄不重设——「直连局域网 + 小包」前提下的合理取舍，跨网/大包才需要补全。

### 下一步
继续深挖 TCP 层「为什么这样设计」（滑动窗口 / 拥塞控制 / 序号回绕），边学边补进 [网络协议学习计划](网络协议学习计划.md)。

---

## 任务 2: TCP 深入

### 目标
深挖 TCP 层「为什么这样设计」三个关键点（滑动窗口 / 拥塞控制 / 序号回绕），接上「每层为什么这样设计」系列。

### 进展和结果
在 [网络协议学习计划](网络协议学习计划.md) 补上 TCP 层「为什么这样设计」三个关键点，结合 `c/tcp.c` 梳理。

**为什么要有滑动窗口？**
- 停等协议（发一个字节等一个 ACK）吞吐率被 RTT 卡死；滑动窗口让发送方不必等每个 ACK，一口气发「窗口内」的字节（流水线）。
- 接收窗口 rwnd = 接收方剩余缓冲，由 TCP 头 Window 字段(16bit) 通告；在途字节不能超过它。
- 16bit 只到 64KB，大带宽延迟积链路靠窗口缩放(RFC 1323) 扩到 ~1GB。
- 滑动窗口 = 流量控制，保护**接收方**；只回答「你还能收多少」，不回答「网络堵不堵」。

**为什么要有拥塞控制？**
- 流量控制只保护接收方，但路由器队列也有上限；大家猛发 → 队列溢出 → 全网拥塞崩溃（1986 互联网事故）。
- 拥塞控制让每个发送方用 cwnd 自我限速，有效窗口 = `min(rwnd, cwnd)`。
- 慢启动(cwnd 1 MSS 起、每 RTT 翻倍) → 拥塞避免(每 RTT +1 MSS) → 丢包减半/归 1。
- 核心是 AIMD（加性增、乘性减），无中央调度也能收敛到公平共享——分布式算法的优雅。

**为什么序号会回绕？**
- 序号 32bit ≈ 42.9 亿，标记字节流位置；但 4GB 在 10Gbps 链路 ~3.4 秒转一圈，空间用完重头来。
- 老段混淆 → PAWS(RFC 1323 时间戳) 判「太老就丢」；ISN 固定可被预测注入 → 现代栈用随机 ISN(RFC 6528)。
- 本项目 tcp.c 用固定 ISN=0x10000000：调试阶段一眼认出自己的包；真实栈才需随机 ISN。

### 收获到的经验
1. **滑动窗口是流水线**：用「在途字节 ≤ 窗口」换掉「停等」，吞吐率不再被 RTT 卡死。
2. **流量控制 vs 拥塞控制别混**：rwnd 保护接收方、cwnd 保护网络，有效窗口取两者小值。
3. **序号回绕是 32bit 的代价**：PAWS + 随机 ISN 两招兜底；本项目固定 ISN 只适合调试。
4. **AIMD 是「无中心也能公平」的范例**：每个发送方独立探路、丢包退让，全网自发收敛。

### 下一步
继续深挖 HTTP 层「为什么这样设计」（方法 / 状态码 / 文本协议取舍），边学边补进 [网络协议学习计划](网络协议学习计划.md)。

---

# 2026-08-17

> 状态: ARP 深入 ✅ | ICMP 深入 ✅

## 任务 1: ARP 深入

### 目标
深挖 ARP 三个关键问题（为什么需要 / 保证通畅的原理 / ARP 表操作），补上以太网帧结构图示。

### 进展和结果
在 [网络协议学习计划](网络协议学习计划.md) 补上以太网帧结构 + ARP/ICMP 封装图示（三层嵌套、字节偏移一目了然），并深挖 ARP 三个关键点。

**为什么需要 ARP？**
- IP 是逻辑地址（跨网寻址用），MAC 是物理地址（局域网投递用），是两个不同层的地址。
- 发数据时上层只知道对方 **IP**，但以太网帧头必须填对方 **MAC**，中间缺一环「IP → MAC」翻译。
- 一句话：**ARP 是「IP → MAC」的翻译官，工作在同一个广播域**；跨网段不走 ARP 走路由器。

**ARP 保证网络通畅的基本原理（四步）**：广播问、单播答、缓存学。
1. A 只知道 B 的 IP、不知道 B 的 MAC；
2. A **广播** ARP 请求「谁的 IP 是 X？告诉我 MAC」——不知道 MAC 没法单播；
3. 只有 IP=X 的 **B 应答**，别人一看「问的不是我」就丢；
4. B **单播**回「MAC 是 Y」——B 已从请求的 sender MAC 读到 A 的 MAC。

**ARP 表如何操作（查 / 学 / 老化）**：
| 操作 | 干什么 | 触发时机 |
|------|--------|----------|
| 查表 lookup | 发数据前查缓存，命中直接拿 MAC | 每次发包前 |
| 学习 learn | 记 sender IP+MAC（请求/应答都记，**免费学习**） | 收到任何 ARP 包 |
| 老化 aging | 表项 TTL 超时删除，下次重新解析 | 计时器到点 |

**本项目 ARP 具体实现（arp.c）**：
- **前置过滤**（arp.c:22-41）：目的 MAC 是本机**或** FF(广播) + target IP = 本机 + opcode = REQUEST(1)。
- **构造应答 9 段「对调重填」**（arp.c:44-105）：目的 MAC=RX 源 MAC、源 MAC=本机、opcode 改 REPLY(2)、sender MAC/IP 填本机、target MAC/IP 抄 RX 的 sender MAC/IP。
- 补 0 到 64 字节 push（以太网最小帧）。
- **纯应答服务器**：只被动回 ARP reply，从不主动发 request，固件里没有 ARP 表。

### 收获到的经验
1. **广播问单播答**：问时不知 MAC 只能广播、答时已知 MAC 直接单播。
2. **免费学习**：收到任何 ARP 包都记 sender 映射。
3. **老化防失效**：对方换网卡/DHCP 后旧映射作废。

### 下一步
继续深挖 ICMP 层「为什么这样设计」（ping 的作用、ICMP 类型码、校验和覆盖范围 vs IP/TCP），边学边补进 [网络协议学习计划](网络协议学习计划.md)。

---

## 任务 2: ICMP 深入

### 目标
深挖 ICMP 三个关键点（为什么需要 / ping 的原理 / 校验和覆盖范围），补齐 ICMP 层知识。

### 进展和结果
在 [网络协议学习计划](网络协议学习计划.md) 深挖 ICMP 层，结合 `c/icmp.c`（97 行）梳理。

**为什么需要 ICMP？**
- IP 是尽力投递（best-effort），不保证到达、不报告失败。
- ICMP 补「报错 + 诊断」缺口：报错（不可达/超时/重定向）+ 诊断（ping 的 Echo）。
- 一句话：IP 只负责送，ICMP 负责「喊一声」。ping ≠ ICMP 全部，只是 Echo 子功能。

**ping 的原理（Echo 请求 → 应答）**
- 源发 Echo Request(type=8) → 目标回 Echo Reply(type=0)，payload 原样弹回（零拷贝）。
- id/seq 匹配进程和包序号，RTT 判断通不通、快不快。
- 本项目纯应答端：收到 type=8 回 type=0（icmp.c:69）。

**ICMP 校验和覆盖范围**
- 覆盖整个 ICMP 报文（8 字节头 + payload），区别于 IP 头校验和只校验 20 字节头。
- 跳过 type/code/checksum 4 字节，type 用 ECHO_REPLY(0) 参与算（icmp.c:34）。
- payload 奇数长度末尾补 0（icmp.c:47-51），最后 `~sum` 取反。

**本项目 ICMP 实现（icmp.c）**
- `icmp_reply`：先写 IP 头（交换 src/dst + 重算校验和）→ 写 ICMP 头(type=0/code=0) → 抄 id/seq/payload → 算校验和写回 → 补 0 到 64 字节 → push。
- `tx_pkt_len = eth(14) + ip_total_len + 4`：`+4` 是 FCS，push 长度要预先算进去。

### 收获到的经验
1. **校验和两种覆盖范围别混**：ICMP 覆盖「头+数据」，IP 头校验和只覆盖「20 字节头」。
2. **回包 type 要先改再算校验和**：校验和用 ECHO_REPLY(0) 算，不是收到的 8。
3. **零拷贝弹回**：payload 不搬进变量，RX 读一字节、TX 写一字节。

### 下一步
继续深挖 IP 层「为什么这样设计」（分片、TTL、协议号分派），边学边补进 [网络协议学习计划](网络协议学习计划.md)。

---

# 2026-08-16

> 状态: 网络协议栈知识点系统梳理 ✅（按「学到了什么」逐层整理）

## 任务 1: 网络协议栈知识点系统梳理

### 目标
按自底向上顺序整理每层「学到了什么」，形成可长期维护的协议栈知识点笔记。

### 进展和结果
生成 [网络协议学习计划](网络协议学习计划.md)（以太网 → ARP → ICMP → IP → TCP → HTTP + 跨层精髓）。

**学到了什么（逐层一句话）**：

- **以太网 eth.c**：读类型字段(0x0800/0x0806)分派 + 顺手预写回包 MAC 头，"边收边发省一步"。
- **ARP arp.c**：把请求的「问话人/被问人」字段对调重填凑出应答，广播问、单播答。
- **ICMP icmp.c**：Echo 请求→应答零拷贝弹回，校验和覆盖整个报文（头+数据）。
- **IP ip.c**：校验 ver/IHL/目的IP，读 src_ip/protocol/total_len 分派；回包=交换 src/dst IP + 重算头校验和。
- **TCP tcp.c**：连接表 + 七状态机走完握手/传数据/挥手；序号=字节流位置指针，SYN/FIN 各占 1 序号；校验和用 12 字节伪首部。
- **HTTP http.c**：文本协议，判 GET 回 HTML，FIN 和 DATA 合并单包发。

**跨层精髓**：偏移量=协议栈地图、分层思想、回包套路统一「对调重填」、网络字节序大端、三种校验和覆盖范围。

### 下一步
继续深挖每层「为什么这样设计」——如以太网前导码/FCS/RGMII、TCP 滑动窗口与拥塞控制、HTTP 方法/状态码，边学边补进 [网络协议学习计划](网络协议学习计划.md)。

---

# 2026-08-15

> 状态: 理解 TCP 代码 ✅ (tcp.c 知识点梳理)

## 任务 1：理解 TCP 代码 — 知识点总结

### 进展和结果通读 `c/tcp.c` (538 行) + `c/inc/tcp.h`，梳理 TCP 协议栈代码里的核心知识点。

#### 1. TCP 头部结构（固定 20 字节）

| 字段 | 偏移 | 长度 |
|------|------|------|
| SrcPort / DstPort | +0 / +2 | 各 2 |
| SEQ / ACK_Num | +4 / +8 | 各 4 |
| DataOffset | +12 | 1（高 4 位 × 4 = 头长） |
| Flags | +13 | 1 |
| Window | +14 | 2 |
| Checksum / Urgent | +16 / +18 | 各 2 |

- 偏移从 eth(14) + ip(20) = 34 起（`OFF_TCP_SRC_PORT = 34`）。
- 头长 = `((data_ofs_raw >> 4) & 0x0F) * 4`，`0x50` 即 20 字节。

#### 2. 网络字节序 = 大端

- 写：`tcp_wr16` 先 `val>>8` 写高字节、再写低字节；`tcp_wr32` 同理逐字节。
- 读：`tcp_rd16/rd32` 先读高字节左移 8，再读低字节拼上。

#### 3. 六个标志位

FIN(0x01) SYN(0x02) RST(0x04) PSH(0x08) ACK(0x10) URG(0x20)

#### 4. 三次握手 + 序号规则

- SYN → alloc_slot 记 remote_ip/port/seq → 回 SYN+ACK（state=SYN_RECEIVED）。
- SYN+ACK 的 ACK_Num = 对方 SEQ **+1**（对方 SYN 占一个序号）。
- 收到 ACK → ESTABLISHED，自己 local_seq **+1**（自己发的 SYN 也占一个序号）。

#### 5. 校验和 = 伪首部 + 头 + payload，逐字节累加

- 伪首部 12 字节：SrcIP(4) + DstIP(4) + 0x00 + Proto(0x06) + TCP 总长(2)，**不实际发送**。
- `cks_sum_cal(a,b,c)`：a、b 各一字节拼 16 位 + 累加值 c，再进位折叠（`while(sum>>16)`）。
- 坑：a/b 必须各是一字节，把 16 位字段当 8 位传就错（TCP 握手不通的根因）。
- 最后 `~sum` 取反；Checksum/Urgent 字段填 0 参与计算。

### 遇到的问题（无，纯代码理解）

### 下一步继续理解 http.c / ip.c / icmp.c 层代码。

---

# 2026-08-14

> 状态: ping 通了 ✅ | TCP 握手通了 ✅ | HTTP 网页通了 ✅ | ILA 调通了 ✅

## 任务 1：ping 通了 ✅

### 进展和结果ping 169.254.1.1 → 5/5 包、0% 丢、avg ~1.8ms，ARP + ICMP echo 全通。

### 遇到的问题之前一直 ping 不通，一度诊断为 package_fifo_v2 丢包，实际是烧录的 bitstream 过期（FPGA 上 150bit 旧布局，源码已是 184bit/32 探针），位偏移全错。

### 收获到的经验先用 get_core_cfg().total_width 回读（150 vs 184）发现探针扩展没烧进去；重编译 + updatemem 把固件 bake 进 _fw.bit + JTAG 烧录后即通。顺带排除站点 A/FIFO、固件 IP/MAC 等假设。

### 下一步TCP 握手（任务二）。

---

## 任务 2：TCP 握手通了 ✅

### 进展和结果nc -zv 169.254.1.1 80 → succeeded，5/5 全通。

### 遇到的问题SYN+ACK 被主机静默丢弃（连 RST 都没有），TCP 校验和永远算错。

### 收获到的经验tcp_calc_checksum() 伪首部源/目的 IP 按 16 位传入（内部 (a<<8|b) 需各一字节），改成逐字节调用。ICMP 通不代表 TCP 校验和也正确。

### 下一步HTTP 网页（任务三）。

---

## 任务 3：HTTP 网页通了 ✅

### 进展和结果curl -i http://169.254.1.1/ → HTTP/1.1 200 OK + 完整 HTML，3/3 全通。

### 遇到的问题TCP 握手已通，但 HTTP 的 DATA 包被丢、只有 FIN 到达，curl 收不到 body 超时。

### 收获到的经验把 FIN 合并（piggyback）进最后一个 DATA 段单包发送，push 长度 +4（FCS），只改 C 固件不动 RTL。

### 下一步ILA 调通收尾（任务四）。

---

## 任务 4：ILA 调通了 ✅

### 进展和结果使用双核抓波，50MHz 时钟和 125MHz 触发条件可以都满足。

### 遇到的问题使用 ila 的时候没办法正常 ping 通项目。

### 收获到的经验用 jtag 烧录固件，uart 完全给 ila 连接。

### 下一步项目收官。

---


# 2026-08-13

> 状态: 固件重传 ✅ | CPU 运行验证 ✅ | 丢包根因定位 ✅ 站点 A | 重编译 ✅ | 双 pop 读指针污染 ✅ | JTAG 烧固件 + UART 永久归 ILA ✅

## 任务 1: 固件重传 + CPU 运行验证

### 目标

固件写在 program RAM (BRAM) 中易失，重新上传确认固件在位、CPU 在跑。

### 进展和结果

**固件重传**

```bash
python3 c_build/upload_fw.py c_build/out/firmware.bin --offset 0 -p /dev/ttyACM0 -b 115200
# Upload complete: 1455 words, 5820 bytes ✅
```

⚠️ 本次耗时 2343s ≈ 39 分钟（约 1.6s/word），远慢于 115200 理论值——疑似脚本每 word 有 sleep 或读响应超时。

**CPU 运行验证 (ILA 抓波)**

| 信号 | 结果 | 含义 |
|------|------|------|
| `led_o` | = 4（上次=1） | 心跳 1→2→4，CPU 持续运行 |
| `bus_req` @ 0x6000 | 3 次读 | 仍轮询 RX FIFO 空标志 |

**主机链路检查**

| 项 | 结果 |
|----|------|
| `ip link` | UP, LOWER_UP, MAC 9c:69:d3:7d:47:4c |
| `ethtool` | 1000Mb/s, Duplex Unknown(255), link detected yes |
| ARP 表 | `169.254.1.1 FAILED`, `169.254.0.100 FAILED` |
| 手动 ping | 100% packet loss |

### 结论

邻居表进入 FAILED 态 → 内核按指数退避不再立即发 ARP，导致本次 ILA 触发超时。CPU 本身运行正常。

---

## 任务 2: 丢包根因定位 ✅ 站点 A 确认

### 目标

ping 不通，定位 MAC→FIFO 数据通路上的丢包点。

### 进展和结果

**① 多智能体并行分析（排除 3 个假设）**

| 假设 | 结论 | 关键依据 |
|------|------|---------|
| mac_rx 因 CRC 错丢弃帧 | ❌ 排除 | mac_rx 不丢弃坏帧，crc_err 只置 `mac_rx_err`，不 gate sop/en/eop |
| RGMII nibble 位序错 | ❌ 排除 | posedge→低 nibble、negedge→高 nibble 正确 (RGMII v2.0) |
| dual_clock_fifo CDC 丢字节 | ⚠️ 候选 | full 静默丢字节（无背压） |
| cpu_rd_ren=1 固件 bug 排空 FIFO | ❌ 部分 | ren 在 package_fifo_v2 里未被使用 |
| wpkt_push 需 eop 才触发 | ❌ 排除 | push 由 `mac_rx_en` 下降沿触发 |

**关键发现**: 空标志 `cpu_rd_empty` 拉低条件 = `(mac_rx_en 干净下降沿) & frame_hit=1 & !full_reg`；`frame_hit` 恒 1。最可能是 `full_reg` 卡高或 FIFO 指针 CDC 问题。

**诊断障碍**: `gmii2mac` 的计数器在顶层全部悬空，无法读取。

**② 决定性信号接入 ILA (26→32 探针)**

把 6 个决定性计数器 probe 进 ILA，一锤定音：

| # | 信号 | 判定什么 |
|---|------|---------|
| 26 | rx_afifo_full_cnt | 站点 A: Eth_RXC→125m FIFO 溢出 |
| 27 | rx_correct_pkt_cnt | 物理层好 FCS 帧计数 |
| 28 | rx_crc_err_pkt_cnt | 物理层坏 FCS 帧计数 |
| 29 | recv_pkt_drop_cnt | 站点 B: 125m→50m FIFO 满丢整包 |
| 30 | mac_in_full | full 是否卡高 (CDC 指针问题) |
| 31 | gmii_rx_er | PHY 错误标志 |

**③ 计数器判定结果**

| 计数器 | 值 | 判定 |
|---|---|---|
| rx_correct_pkt_cnt | 133 | 物理层正常 |
| rx_crc_err_pkt_cnt | 6 | 坏帧可忽略 |
| **rx_afifo_full_cnt** | **219** | ★ 站点 A: gmii2mac 内部异步 FIFO 溢出 |
| recv_pkt_drop_cnt | 0 | 站点 B 没丢 |
| mac_in_full | 0 | full 没卡高 |
| gmii_rx_er | 0 | PHY 无错误标志 |

### 结论

**根因 = 站点 A：`gmii2mac` 内部异步 FIFO(Eth_RXC→clk_125m, 16 深)满溢出。** Eth_RXC(RGMII 恢复时钟)与 clk_125m(MMCM 时钟)非同一时钟源，有频偏，16 深无背压 FIFO 溢出静默丢字节，完整帧到不了 package_fifo_v2，CPU RX FIFO 永远空。

### 修改的文件

| 文件 | 修改 |
|------|------|
| `rtl/webserver_cpu_top.v` | NUM_PROBES 26→32, 新增 probe26-31, 接通 gmii2mac 计数器输出 |
| `signals.json` | 32 探针, 184bit, signature `0x00000800200100B8` |
| `build_xilinx/analyze_capture.py` | 触发字 5→6, 新增丢包判定诊断 |

### 收获到的经验

1. **计数器要接出来才算数**: gmii2mac 里的 CRC/FIFO-full 计数器早就埋好，顶层空接等于白埋。
2. **签名是 bit 布局的指纹**: 改 NUM_PROBES / 位宽后 signature 必须按公式重算，否则回读样本错位。

---

## 任务 3: package_fifo_v2 深度仿真 — 双 pop 读指针污染

### 目标

第二次抓波出现与任务 2 (站点 A) 不同的现象: `mac_in_full=1` 卡高 + `cpu_rd_empty=1` 卡空 + `recv_pkt_drop_cnt=14`(站点 B 满丢整包)。对 `package_fifo_v2`(RX 包 FIFO, 站点 B)做定向仿真, 定位 full 卡死根因。

### 进展和结果

**① 定向仿真排除 3 个假设** (4 个 testbench)

| 假设 | 结论 | 依据 |
|------|------|------|
| package_fifo_v2 指针逻辑 bug | ❌ 排除 | 正确单脉冲 pop 下 `used` 稳定 100, full 从不置位 (tb_pfifo_full.v) |
| pop 是电平卡高 | ❌ 排除 | 顶层接 `cpu_rd_rpkt_pop_ind`; lcpu_merge `op_req` 是 1 周期脉冲 |
| 固件每包 pop 一次 | ❌ 双 pop | main.c `LCPU_RD_START_PACKET()`(=pop#1) + 末尾 `_RD(1)=1`(=pop#2 release) |

**② 双 pop 读指针污染机制**

release pop 在 para FIFO 已空时读 stale 数据, 污染 `nb_rptr_wclk_s2`(读指针 CDC 后的 wclk 侧副本), 使 rptr 落后 7 包。

**③ 仿真结果**

| 场景 | used | full |
|------|------|------|
| 正确单脉冲 pop | 稳定 100 | 从不置位 |
| 双 pop (mem_array=0 模拟硬件默认) | 恒定 700 | 从不置位 |

### 结论

**双 pop 是真实 bug**: release pop 读空 FIFO 污染读指针, 造成 7 包幻影占用。对 ping (100B) ≈ 700 词不卡 full, 但 **TCP/HTTP 大包 (1518B) → 7×1518=10626 > 2578 满阈值, 必卡 full**。

仿真 full=0 与硬件 full=1 未完全对齐, 剩余假设: PHY link 垃圾帧 (nb_wptr 跳变)、更大包 (>368B 平均)、突发淹没 para FIFO。

### 待验证

1. 固件去掉 release pop (`_RD(1)=1`) 后重测 —— 固件改动, 不碰核心 RTL。

### 收获到的经验

1. CDC 读指针要防空读: release pop 在 FIFO 空时读 stale 数据, 是「无 empty 保护的 pop」。
2. 仿真 mem_array 默认值敏感: registers RAM 未初始化 = X vs 硬件 = 0, 行为差异大, 仿真要显式初始化。
3. 双 pop 对 ping 无害、对 TCP 致命: 7 包幻影占用阈值取决于包长, 小包不触发、大包必触发。

### 修改的文件

| 文件 | 修改 |
|------|------|
| `/tmp/tb_pfifo_full.v` | 新建 — 正确脉冲 pop 验证 |
| `/tmp/tb_pfifo_doublepop.v` | 新建 — 复现双 pop |
| `/tmp/tb_pfifo_debug.v` | 新建 — dump para FIFO 内部指针 |
| `/tmp/tb_pfifo_confirm.v` | 新建 — mem_array=0 模拟硬件默认 |

---

## 任务 4: JTAG 烧固件 + UART 永久归 ILA

### 目标

放弃「UART 上传固件」(慢 ~39 分钟 + 与 ILA 共享串口冲突 + 固件易失)，改用标准工作流：JTAG 烧固件 + UART 永久归 ILA + GUI ILA 当纯工具。

### 进展和结果

**① JTAG 烧固件脚本** (`build_xilinx/load_fw_jtag.sh`)

updatemem 把固件 (bank0.mem..bank15.mem) bake 进 BRAM INIT，生成 `RiscV_WebSoC_fw.bit`，再 program 烧录。

**② 执行结果** (20:50)

| 步骤 | 结果 |
|------|------|
| updatemem | ✅ `update_mem completed successfully`, 0 errors |
| program | ✅ `PROGRAMMING SUCCESS` (20:50:30) |

### 结论

「JTAG 烧固件 + UART 永久归 ILA + GUI 当工具」目标架构已落地并烧录成功。固件 bake 进 BRAM INIT，上电自跑，无需 UART 上传。

> ⚠️ 注意：这是**固件加载方式**的改进，不涉及 ping 丢包根因（站点 A/B FIFO 问题）——那是硬件通路问题，与固件怎么烧进去无关。

### 修改的文件

| 文件 | 修改 |
|------|------|
| `build_xilinx/load_fw_jtag.sh` | 新建 — 一键 updatemem + program |
| `build_xilinx/program_fw.tcl` | 新建 — 烧录 _fw.bit |
| `build_xilinx/RiscV_WebSoC_fw.bit` | 生成 — 含固件 bitstream |

### 收获到的经验

1. **固件加载与丢包根因是两回事**: JTAG 烧固件让固件"进去"了，但 ping 不通的硬件 FIFO 溢出仍在。别把"加载方式"和"数据通路 bug"混为一谈。
2. **updatemem 需要有效 MMI**: 之前弃用是因为 MMI 无 Processor 块；`write_mem_info` 生成的 MMI 含 16 个 RAMB36 bank，updatemem 用 `-proc` 显式指 bank 路径即可。

---

# 2026-08-12

> 状态: TCP DstPort=80 根因确认 ✅ | ILA 集成 ✅ | 时序修复 ✅ | 烧录+固件上传+CPU 运行 ✅

## 任务 1: TCP DstPort=80 根因 — byte-enable 仿真模型缺陷

### 目标

TCP 握手回复 SYN+ACK 时 DstPort 字段为 80 而非 PC 源端口 4660 (0x1234)。定位根因。

### 进展和结果

**① 代码回退（推翻错误分析）** — 原分析错误认定 `single_clock_true_dual_port_ram` Port A 缺 byte-enable 是根因，据此改了多个 RTL 文件。实际数据写入走 Port B（Port B 有 byte-enable），Port A 仅用于 BFM 固件加载。回退：

| 文件 | 操作 |
|------|------|
| `rtl/lcpu_riscv_wrapper.v` | 移除 `.wr_byte_en()` 端口 |
| `rtl/webserver_cpu_top.v` | 移除 `bus_wr_byte_en` 连线 |
| `rtl/lcpu_fpga_test.v` | 移除 `wr_byte_en` 端口和 ramintf `.byte_en()` 连接 |

**② 根因确认：`xpm_memory_tdpram_sim.v` 仿真模型缺 byte-enable**

- **硬件 pipeline 验证** (`tb_byte_en_test.v`): 复现 firmware 两个 `sh` 半字写，监控 `wr_byte_en_m/d`、`wren_byte_b` 全链路。**PASS** —— 硬件 byte-enable 7 级 pipeline 完全正确。
- **Bug 复现** (`tb_byte_en_test_xpm.v`, vendor="xilinx"): SRAM word = 0x00500050（应为 0x12340050）。**BUG REPRODUCED** —— XPM 仿真模型忽略 byte-enable：

```verilog
// 修复前: 仅用 OR-reduce 判断"是否有写", 写全部 32 bit, 忽略 byte-enable
always @(posedge clka) begin
    if (ena && |wea)
        mem[addra] <= dina;
end
```

**故障机制**: PicoRV32 `sh` 指令把半字复制到 mem_wdata 两半 (0x0050→0x00500050)，`mem_wstrb=0011` 只写低半字；XPM 模型 `|wea` 判断任一写使能位就写全字 → 覆盖相邻变量。

**修复**: 端口宽度修正 (`wea` 1bit→4bit) + 逐字节写使能：

```verilog
always @(posedge clkb) begin
    if (enb) begin
        for (integer _b = 0; _b < WEB_WIDTH; _b = _b + 1) begin
            if (web[_b])
                mem[addrb][_b*8 +: 8] <= dinb[_b*8 +: 8];
        end
    end
end
```

修复验证 **PASS**: SRAM word = 0x12340050 ✓

**③ byte-enable 全链路追踪（含 altsyncram 修复）** — 逐级验证 PicoRV32→SRAM 的 7 级 pipeline 全部正确；`vendor_stubs.v` 的 altsyncram 模型同样缺 byteena 逐字节写入，一并修复（影响 Intel/Altera 仿真路径，当前 Xilinx 不受影响）。

### 结论

- **硬件 RTL 完全正确，零缺陷**；Bug 仅为仿真模型缺陷，不影响上板行为（Xilinx 真实 BRAM 支持 byte-enable）。
- 3 个仿真模型修复完成: `xpm_memory_tdpram_sim.v` ✅、`vendor_stubs.v` altsyncram ✅、注释的 xpm 模型(不编译)。

### 修改的文件

| 文件 | 修改 |
|------|------|
| `sim/xpm_memory_tdpram_sim.v` | 端口宽度 + byte-enable 逻辑修复 |
| `sim/vendor_stubs.v` | altsyncram 添加 byteena 逐字节写入 |
| `rtl/lcpu_riscv_wrapper.v` / `rtl/webserver_cpu_top.v` / `rtl/lcpu_fpga_test.v` | 回退（移除错误添加的 wr_byte_en 端口/连线） |

---

## 任务 2: fpga_ila 集成 — IP 快照 + 插桩

### 目标

用最新 fpga_ila 快照替换旧 IP，并在 `webserver_cpu_top.v` 中实例化 `soft_ila_top` + `ila_hub_top` 完整调试链路。

### 进展和结果

**① IP 快照部署** — `fpga_ila-snapshot-20260812133532` 替换旧 IP（flat rtl/ 结构，`soft_ila_top` 替代 `soft_ila_top_fcapz`）。补充缺失的 `crc_func.vh`/`ila_version.vh`，重写 `fpga_ila_files.f`（14 个 UART 必需文件），更新 `ila_gui.sh` 的 ILA_HOME 路径。

**② ILA 核 + Hub 实例化** (`rtl/webserver_cpu_top.v`) — 新增 localparam（`ILA_TRANSPORT_EN=3'b001`, `ILA_BAUD=921600`），实例化 `soft_ila_top`（26 探针, 150bit, DEPTH=2048）+ `ila_hub_top`（UART, 板载 L21/M21）。

| 参数 | 值 |
|------|-----|
| 采样时钟 | clk_50m (50MHz, 20ns 粒度) |
| 传输 | UART 921600 baud |
| 采样深度 | 2048 (环形缓冲) |
| 窗口数 | 1 |

**UART 引脚**: ILA Hub 独占板载串口 `uart_rx`(L21)/`uart_tx`(M21)；CPU UART TX 改为内部信号（调试期间 CPU printf 不可见）。

### 修改的文件

| 文件 | 修改 |
|------|------|
| `ip copy/fpga_ila-snapshot-20260812133532/` | 完整 IP 快照 |
| `fpga_ila_files.f` | 重写（14 个 UART 必需文件） |
| `ila_gui.sh` | ILA_HOME 路径 |
| `rtl/webserver_cpu_top.v` | 新增 ~130 行 ILA 代码 |
| `signals.json` | 重写（26 探针, 150bit, signature `0x000008001A010096`） |

---

## 任务 3: Vivado 时序约束修复 (WNS -1.76→+3.32ns)

### 目标

布局后 Setup WNS=-1.760ns, 81 条 failing endpoints。根因是跨时钟域路径 (clk_125m→clk_50m 等) 无时钟组约束。

### 进展和结果

在 `build_xilinx/timing.xdc` 声明 4 路 MMCM 输出为异步时钟组：

```tcl
set_clock_groups -asynchronous \
    -group [get_clocks clk_125m_unbuf] \
    -group [get_clocks clk_125m_tx_unbuf] \
    -group [get_clocks clk_200m_unbuf] \
    -group [get_clocks -include_generated clk_50m_cpu_unbuf]
```

| 指标 | 修复前 | 修复后 |
|------|--------|--------|
| WNS (Setup) | -1.760 ns | **+3.321 ns** |
| TNS (Setup) | -123.154 ns | **0.000** |
| Failing endpoints | 81 | **0** |

### 修改的文件

| 文件 | 修改 |
|------|------|
| `build_xilinx/timing.xdc` | 异步时钟组声明 |

---

## 任务 4: 烧录 + 固件上传 + CPU 运行验证

### 目标

烧录 bitstream，通过 UART 上传固件，验证 CPU 运行，并初步定位 ping 不通根因（最终在 08-13 任务 2 完成）。

### 进展和结果

**烧录**: 用户手动烧录成功，ILA Qt GUI 启动（signals.json 正确加载）。

**固件上传（关键发现 `--offset` Bug）** — `upload_fw.py` 默认 `--offset 0x100` 是「剥离字节数」不是「复位地址」，会静默丢掉固件前 256 字节（`reset_entry`），导致 CPU 在 word 0 执行全零。正确用法 `--offset 0`：

```bash
python3 c_build/upload_fw.py c_build/out/firmware.bin --offset 0 -p /dev/ttyACM0 -b 115200
# 结果: 1455 words 上传成功
```

**CPU 运行验证 (ILA 抓波)**

| 信号 | 结果 | 含义 |
|------|------|------|
| `bus_req` @ 0x6000 | 3 次读 | CPU 轮询 RX FIFO 空标志 |
| `led_o` | = 1 | 心跳，CPU 已进主循环 |
| `gmii_rx_dv` | 28 样本 | ARP 到达 GMII RX |
| `mac_rx_en` | 25 样本 | MAC 收到帧 |

**ping 不通根因定位**

| 信号 | 结果 | 结论 |
|------|------|------|
| `gmii_rx_dv`/`mac_rx_en` | 28/25 样本 | ✅ ARP 到达 MAC |
| `cpu_rd_empty` | 始终高(空) | ❌ 包没进 CPU RX FIFO |
| `gmii_tx_en`/`mac_tx_en` | 0 | ❌ 无 TX 回包 |

**初步根因**: 帧到达 MAC 但 `gmii2mac → package_fifo_v2` 之间被丢弃，疑 CRC 错误或跨域。→ 最终在 08-13 任务 2 定位到站点 A FIFO 溢出。

### 遇到的问题

1. `updatemem` 无 `-mem` 选项、MMI 无 Processor 块 → 无法合并固件到 bitstream
2. UART 设备是 CH340 芯片 → `/dev/ttyACM0`（非 `/dev/ttyUSB1`）
3. JTAG 扫描到垃圾数据 → 板卡未上电/线未接

### 收获到的经验

1. **`--offset` 语义要看清**: 是「剥离字节数」不是「复位地址」，默认 0x100 会静默丢掉 reset_entry。
2. **ILA 采样率 vs 信号频率**: 50MHz ILA 采 125MHz GMII 必然欠采样，单拍 SOP/EOP 脉冲采不到；看「活动有无」要看 level 信号（`cpu_rd_empty`）。

---

# 2026-08-11

> 状态: FPGA 烧录 ✅ | MAC TX 时序验证 ✅ | ILA PING 通信 ❌ (FTDI 驱动冲突)

## 任务 1: FPGA 烧录 + ILA 通信调试

### 目标

烧录 50MHz bitstream，通过 FCAPZ 协议 PING 验证 ILA Hub 通信。

### 进展和结果

1. Vivado 烧录成功 (`PROGRAMMING SUCCESS`)
2. 写 `ila_sender.py`（ping/raw/bench/loopback 四种模式）
3. CRC 验证通过: Python crc16 与 Verilog crc16_func.vh 一致 ("123456789"→0x29B1)
4. FCAPZ 帧格式确认: `55 AA <core_id> <cmd> <len_lo> <len_hi> <CRC_LO> <CRC_HI>`
5. 0xFA echo 溯源: 合成日志确认 bitstream 不含 `fcapz_ejtaguart.v`，0xFA echo 是 FPGA 未配置时 FTDI 硬件回环

### 遇到的问题

- hw_server 将 FT232H 切到 JTAG/libusb 模式，杀掉后 `ftdi_sio` 无法重新绑定，`/dev/ttyUSB1` 消失
- **解决**: 板子完全断电重上电 → FT232H 恢复 UART 模式 → `/dev/ttyUSB1` 出现

---

## 任务 2: MAC TX 发包时序仿真验证

### 目标

在仿真中通过 ILA 捕获 MAC TX 侧信号，验证发包时序。

### 进展和结果

| 参数 | 测量值 | 结论 |
|------|--------|------|
| SOP/EOP 脉冲宽度 | 1 cycle (20ns) | ✅ |
| mac_tx_en 持续时间 | 64 cycles (1280ns) | ✅ 64 字节 |
| mac_tx_err | 始终 0 | ✅ |
| GMII 前导码插入 | 8B (0x55×7+0xD5) | ✅ |
| TCP Flags | 0x12 (SYN\|ACK) | ✅ |

同时修复 POSTTRIG 默认值问题（`ADDR_POSTTRIG=0` 导致只捕获 1 个后触发样本），以及 `ila_dump_to_vcd.py` 的 X/Z 字符解析。

### 修改的文件

| 文件 | 修改 |
|------|------|
| `sim/ila_dump_to_vcd.py` | 修复 X/Z 状态字符解析 |
| `sim/analyze_mac_tx.py` | 新建，MAC TX 全包分析 |
| `sim/extract_mac_tx_detail.py` | 新建，逐周期打印 |

---

## 任务 3: TCP DstPort=80 根因分析 ❌ 已推翻

> ⚠️ 此分析在 2026-08-12 被推翻（根因见 [2026-08-12 任务 1](#任务-1-tcp-dstport80-根因--byte-enable-仿真模型缺陷)）。原始记录保留供参考。

**原根因主张**: ~~`single_clock_true_dual_port_ram` Port A 不支持 byte-enable~~

**推翻理由 (2026-08-12)**: 数据写入走 Port B（有 byte-enable），Port A 仅 BFM 固件加载；真实根因是 `wr_byte_en_d` 与 `wren_b` 两条路径 pipeline 延迟不对齐。
# 2026-08-07

> 状态: GUI 仿真环境搭建完成 ✅ | 仿真验证通过 ✅ | Vivado bitstream ✅ | FPGA 烧录 ✅ | ILA 时钟降频修复 🔄

### 📚 Skills 参考手册

整理了两套 Agent Skills 的完整参考手册，融合了通用软件工程 skills 和 FPGA 专用 skills：

> **Skills 完整参考手册**: [mattpocock_skills_参考手册](mattpocock_skills_参考手册.md)

| 来源 | 数量 | 覆盖范围 |
|------|------|----------|
| [mattpocock/skills](https://github.com/mattpocock/skills) | 24 个 | 工程: grilling/tdd/code-review/diagnosing-bugs/codebase-design 等 + 效率: handoff/teach 等 |
| HuanghmBuck/skills (本地) | 15 个 | FPGA 开发: verilog-design/doc-lld/unit-sim/debug-ila/format + 文档: md2html/md2pdf/blog-publish + Git: push/pull/list/diff/share |

手册包含：4 大设计理念、每个 skill 的功能+链接+典型场景、5 个 FPGA 开发场景的 skills 组合流程（新模块设计/硬件Bug调试/大型功能开发/文档交付/跨机器交接）。

---

## 任务 1: fpga_ila_local 虚拟环境搭建

### 进展和结果
在 `fpga_ila_local` 目录创建 Python `.venv` 虚拟环境，安装 GUI (Qt + Web) 全部依赖。系统缺少 `python3.14-venv` 且无 sudo 权限，通过 `--without-pip` + `get-pip.py` 绕过。

### 操作步骤
```bash
# 1. 创建 venv (系统缺 ensurepip)
python3 -m venv --without-pip /home/zhihuiw/fpga_work/fpga_ila_local/.venv

# 2. 通过 bootstrap 脚本安装 pip
curl -s https://bootstrap.pypa.io/get-pip.py -o /tmp/get-pip.py
/home/zhihuiw/fpga_work/fpga_ila_local/.venv/bin/python /tmp/get-pip.py

# 3. 安装依赖
.venv/bin/pip install PySide6 pyqtgraph pyserial fastapi uvicorn websockets
```

### 依赖清单

| 组件 | 包 | 用途 |
|------|-----|------|
| Qt GUI | PySide6>=6.5, pyqtgraph>=0.13 | 桌面波形查看器 |
| Web GUI | fastapi, uvicorn, websockets | 浏览器波形查看器 (localhost:9527) |
| 公共 | pyserial>=3.5 | UART 串口通信 |

### 遇到的问题
1. `python3 -m venv` 失败 — Ubuntu 24.04 的 Python 3.14 未预装 `ensurepip` 模块，且 `python3.14-venv` 包未安装
2. `sudo apt install` 不可用 — 无终端 sudo 权限
3. `pip install` 耗时较长 — PySide6 wheel 约 100MB+，下载+安装需 5-10 分钟

### 解决方法
使用 `--without-pip` 标志跳过 ensurepip 检查，然后通过 `bootstrap.pypa.io` 的 `get-pip.py` 独立安装 pip，再安装所有依赖。

### 修改的文件
- `/home/zhihuiw/fpga_work/fpga_ila_local/.venv/` — 新建虚拟环境

---

## 任务 2: 仿真标准模式验证

### 进展和结果
在标准模式重构完成后，重新编译并运行完整仿真。iverilog 编译通过 (仅 timescale 继承 warning)，vvp 仿真 PASS：固件成功处理 TCP SYN 并返回 SYN+ACK。

### 验证结果
```
=== PASS: Firmware sent TCP SYN+ACK! ===
ILA: Dump complete — 2048 samples → ila_dump.txt
VCD: tb_fast.vcd (352MB)
```

- CPU 正常启动 (riscv_reset_l 释放后 mem_valid 拉高)
- ILA 触发配置正常 (PRETRIG=2047, TRIG_MODE=1, TRIG_MASK=0x00040000)
- 仿真时间: 3,577,180,000 ps (~3.58 秒)

### 常用命令
```bash
cd sim
make clean && make      # 编译
make run                # 仿真 → tb_fast.vcd
make wave               # gtkwave 看完整波形
make ila_vcd            # ILA dump → VCD 转换
make wave_ila           # gtkwave 看 ILA 波形
```

---

## 任务 3: GUI 启动脚本确认

### 进展和结果
确认 `ila_gui.sh` 的 venv 路径与新建的 `.venv` 一致:

```bash
# ila_gui.sh 核心逻辑
ILA_HOME="/home/zhihuiw/fpga_work/fpga_ila_local"
VENV_PYTHON="$ILA_HOME/.venv/bin/python"   # ✅ 与新建 venv 一致

# Qt 桌面版
./ila_gui.sh
```

> ⚠️ GUI 需要 FPGA 已烧录 bitstream + UART 连接 (波特率 921600)，仅仿真时无法使用 GUI 连接。

### GUI vs 仿真对照

| 场景 | 工具 | 数据源 |
|------|------|--------|
| 仿真 | gtkwave + tb_fast.vcd | iverilog $dumpvars |
| 仿真 | gtkwave + ila_wave.vcd | ILA core → ila_dump.txt → VCD |
| 硬件 | Web GUI (localhost:9527) | UART → fcapz 协议 |
| 硬件 | Qt GUI | UART → fcapz 协议 |

---

## 任务 4: Qt GUI 启动验证

### 进展和结果
Qt 桌面版 GUI 启动成功 (PID 1658095)，ILA home 和 signals.json 路径正确加载。采用 Qt GUI 作为主力调试工具。

### 启动方式
```bash
cd /home/zhihuiw/fpga_work/Prj/RiscV_WebSoC_3
./ila_gui.sh            # Qt 桌面版 (推荐)
./ila_gui.sh web        # Web 版 (http://localhost:9527)
```

### 验证通过
- PySide6 6.11.1 ✅
- pyqtgraph 0.14.0 ✅
- signals.json 自动复制到 fpga_ila_local ✅
- GUI 窗口正常弹出 ✅

> ⚠️ GUI 连接需要 FPGA 已烧录 bitstream + UART 连接 (波特率 921600)，当前等待 Vivado 编译完成。

---

## 任务 5: ila_dump_to_vcd.py 探针更新 (13→27)

### 进展和结果
`ila_dump_to_vcd.py` 硬编码了旧的 13 探针定义 (96bit)，更新为当前 27 探针 (151bit)，与 `signals.json` 保持同步。重新转换 ila_wave.vcd 正确显示全部 27 个信号，包括新增的 MAC TX、CPU FIFO 脉冲、bus_wdata、led_val 等。

### 修改的文件
`sim/ila_dump_to_vcd.py` — PROBES 列表、extract_probe 注释、预览输出

---

## 任务 6: Vivado bitstream 生成 — DRC 修复 + FPGA 烧录

### 进展和结果
Vivado 综合+实现+布线全部通过，但 bitstream 生成时 DRC 检查失败。根因：`uart_rx`/`uart_tx` (CPU UART) 在顶层端口列表中但 `pins.xdc` 没有约束。ACX750 板只有一组 UART 引脚 L21/M21，已被 ILA 调试占用。

### DRC 错误
```
ERROR: [DRC NSTD-1] Unspecified I/O Standard: 2 out of 24 logical ports
ERROR: [DRC UCIO-1] Unconstrained Logical Port: 2 out of 24 logical ports
```

### 解决方法
- `uart_rx` → C18 (GPIO0[0]), `uart_tx` → C19 (GPIO0[1]), IOSTANDARD: LVCMOS33
- 修改 `pins.xdc` 添加引脚约束，新建 `gen_bitstream.tcl` 从 checkpoint 重跑
- bitstream 生成成功 (2.1MB)，但时序违规 WNS=-3.714ns (ILA BRAM @125MHz)
- FPGA 烧录成功: Vivado HW Manager 检测到 Digilent/E306A991ABCD → xc7a35t_0

### 修改的文件
`build_xilinx/pins.xdc` — 新增 uart_rx(C18)/uart_tx(C19) 约束
`build_xilinx/gen_bitstream.tcl` — 从已有综合重跑实现+bitstream

### 硬件发现
Vivado 编程后 FT232H 被 hw_server 占用 (JTAG 模式)，UART 通道 `/dev/ttyUSB1` 消失。需重新插拔 USB 线缆恢复。FT232H 在 ACX750 上同时提供 JTAG (编程) 和 UART (ILA 通信)，同一时刻只能一个功能使用。

---

## 任务 7: Qt GUI 端口修复 (COM4 → /dev/ttyUSB1)

### 问题
Qt GUI 硬编码默认串口为 Windows 格式 `COM4`，Linux 下连接失败: `No such file or directory: 'COM4'`

### 修复
修改 `fpga_ila_local/gui/main.py` (2处) 和 `gui/app/connection_panel.py` (1处)，`COM4` → `/dev/ttyUSB1`，波特率 921600 不变。

---

## 任务 8: ILA 串口通信调试 — 发现时序违规导致 fcapz 核不工作

### 进展和结果
GUI 连接 `/dev/ttyUSB1` @ 921600 超时: `serial recv_frame timeout`。Python 脚本直接测试发现:
- FPGA 收到 fcapz 帧后只回显 (FA 成帧)，不生成响应
- 回显格式: FPGA 对每个收到的字节回发 `[0xFA, received_byte]`
- 协议帧格式确认: `SYNC0=0x55, SYNC1=0xAA` + core/cmd/len/payload/CRC16
- 确认: ILA transport 层活着 (UART RX/TX 正常)，但 fcapz 核状态机不处理命令

### 根因分析
首轮 Vivado 时序报告: clk_125m 域 WNS=-3.714ns, 217 条 failing paths。ILA BRAM 151bit 宽总线 @125MHz (8ns period)，关键路径 ~11.7ns。fcapz_ela 核的寄存器状态机在时序违规时钟域下不可靠运行，BRAM 读写、状态跳转随机出错，导致不响应命令。

---

## 任务 9: ILA 时钟降频修复 (125MHz → 50MHz) 🔄

### 方案
将 ILA 时钟从 `clk_125m` (125MHz, 8ns) 切换到 `clk_50m` (50MHz, 20ns)，利用 MMCM 已有的 50MHz CPU 时钟输出。

### 修改
```verilog
// webserver_cpu_top.v
localparam ILA_CLK_HZ  = 50_000_000;   // 125_000_000 → 50_000_000
.SAMPLE_HZ(50000000),                    // 125000000 → 50000000
.sample_clk(clk_50m),                    // clk_125m → clk_50m
.clk(clk_50m),                            // ila_hub_top: clk_125m → clk_50m
```
- `signals.json`: `sample_clock_hz: 125000000 → 50000000`
- ILA 波特率自动适配: 50MHz / 921600 ≈ 54.25 → divider 54 (0.47% 偏差)
- 采样粒度 20ns → 每 GMII 字节 (8ns) 约 2.5 ILA 周期，足以观察包级时序

**当前状态:** Vivado 完整编译后台运行中 🔄

---

### ILA 标准模式使用问题总结

#### 问题 1: 标准指南省略了 `reg_re` / `core_reg_re` 端口

快速入门指南的示例代码只显示了 `.reg_we` / `.core_reg_we`，没有 `.reg_re` / `.core_reg_re`。但实际 RTL 中这两个端口是 `input`/`output` 声明，无默认值，**不连接会编译报错**。

**解决:** 标准模式也必须连接 `reg_re` 和 `core_reg_re`:
```verilog
soft_ila_top_fcapz:  .reg_re(ila_re[0])
ila_hub_top:         .core_reg_re(ila_re)
```

#### 问题 2: Web GUI 加载 signals.json 无反应

Web GUI 的 `_loadSignals()` 函数只拷贝 `name`/`probes` 字段，漏掉 `data_depth`/`num_probes`。后续 `_populateCoreSelect()` 用 `(c.data_depth || 0) > 0` 过滤死核，导致刚加载的核被当作死核丢弃，页面静默无反应。

**解决:** 修改 `index.html` 的 `_loadSignals()`，完整拷贝 `data_depth`、`num_probes`、`total_width`、`max_windows` 等字段。

#### 问题 3: Web GUI 默认波特率 115200 vs FPGA 921600

Web GUI 连接对话框默认波特率 115200，但标准模式下 `ILA_BAUD = 921600`。不匹配导致连接超时。

**解决:** 连接时手动改波特率为 921600。

#### 问题 4: 仿真中 testbench 如何访问 ILA 寄存器

标准模式下 Hub 直接驱动核总线，没有仿真 bypass 路径。testbench 需要通过 `force` 语句覆盖总线信号:

```verilog
task ila_write(input [15:0] addr, input [31:0] data);
    @(posedge ila_clk);
    force dut.ila_we   = 1'b1;
    force dut.ila_addr = addr;
    force dut.ila_wdata = data;
    @(posedge ila_clk);
    force dut.ila_we   = 1'b0;
endtask
```

#### 问题 5: Python venv 创建失败 (ensurepip 缺失)

Ubuntu 24.04 + Python 3.14 环境下，`python3 -m venv` 因缺少 `ensurepip` 模块而失败，且 `python3.14-venv` 系统包未安装。

**解决:** `python3 -m venv --without-pip` + `get-pip.py` 引导安装。

#### 问题 6: 上板前必须确认 FPGA 已烧录

Web/Qt GUI 通过 UART 发送 fcapz 协议命令，需要 FPGA 上的 ILA Hub 响应。如果 FPGA 未烧录 bitstream，串口能打开但 Hub 不响应 → 连接超时。

#### 问题 7: Vivado 编程后 FT232H JTAG/UART 冲突

Vivado hw_server 通过 libusb 直接控制 FT232H (Digilent USB Device) 进行 JTAG 编程，会 detach 内核 ftdi_sio 驱动，导致 UART 通道 `/dev/ttyUSB1` 消失。Vivado GUI 持续运行时 FT232H 保持在 JTAG 模式。

**解决:** Vivado 编程完成后，物理重新插拔 ACX750 USB 线缆，让 FT232H 重新枚举。

#### 问题 8: ILA BRAM 时序违规 (WNS=-3.714ns @ 125MHz) ⚠️ 关键

XC7A35T-2 在 125MHz (8ns period) 下，151bit 宽 ILA BRAM 总线关键路径 ~11.7ns。217 条 failing paths。fcapz_ela 核状态机不可靠 → 串口收到数据但不处理命令、不返回响应。

**解决:** 将 ILA 时钟降至 50MHz (20ns period)，利用 MMCM 已有的 clk_50m_cpu 输出。Vivado 重编译中。

#### 问题 9: GUI 默认端口 COM4 (Windows) 不兼容 Linux

Qt GUI 硬编码默认串口为 `COM4`。**解决:** 改为 `/dev/ttyUSB1` (3处)。

---

## 仿真运行

```bash
cd sim
make clean && make      # 编译
make run                # 仿真 → tb_fast.vcd
make wave               # gtkwave 看完整波形
make ila_vcd            # ILA dump → VCD 转换
make wave_ila           # gtkwave 看 ILA 波形
```

## 硬件调试

```bash
./ila_gui.sh            # Qt GUI 版
```

> ⚠️ 硬件调试前提: FPGA 已烧录 bitstream + UART 连接 + 波特率设为 921600

---

## 下一步计划

1. ✅ pip install 完成 → GUI 启动验证通过
2. ✅ Vivado bitstream 生成 (DRC 修复: uart_rx→C18, uart_tx→C19)
3. ✅ FPGA 烧录成功 (Digilent/E306A991ABCD → xc7a35t_0)
4. ✅ ILA 协议验证 (发现时序违规导致 fcapz 核不响应)
5. ✅ MAC TX 发包时序仿真验证 (ILA 捕获 mac_tx_sop→eop 全流程)
6. 🔄 ILA 时钟降频修复 (125MHz→50MHz, Vivado 编译中)
7. ⏳ 板子断电重开 → 恢复 `/dev/ttyUSB1` → ILA PING 验证
8. ⏳ Qt GUI 连接 → 加载 signals.json → 抓 MAC TX 波形 (硬件)
9. ⏳ 排查 TCP DstPort 异常 (固件回复 DstPort=80 而非 4660)
10. ✅ fpga_ila 新快照部署 (ip copy/ + fpga_ila_files.f + ila_gui.sh)
11. ✅ ILA 实例化完成 (debug-ila skill: soft_ila_top + ila_hub_top, 26 探针 150bit)
12. ⏳ Vivado 编译 → 烧录 → ILA GUI 连接 → 验证触发采集
13. ⏳ 排查 TCP DstPort 异常 (固件回复 DstPort=80 而非 4660)

---

> 更新日期: 2026-08-12

---

# 2026-08-06

> 状态: TCP SYN→SYN+ACK 仿真通过 ✅ | ILA 标准模式集成 ✅ | Vivado 编译中 🔄

---

## 任务 1: ILA BURST_PTR 数据回读修复

### 进展和结果
修复了 ILA force trigger 回读时 4096 个样本全部相同的 Bug。根因是 `soft_ila_top_fcapz` 的读地址计算中 `data_sample = data_byte_off / (WORDS*4)` 整数除法截断，改为扁平偏移编址后正常。

### 遇到的问题
ILA wrapper 的 `bram_rd_addr` 是为 JTAG 突发读设计的，逐字读时 data_byte_off 始终在 0~11 范围变化，整数除法截断导致始终读同一 BRAM 行。

### 解决方法
使用 `FCAPZ_DATA_BASE + i * 20 + word_offset` 扁平偏移，将样本索引直接编码到地址中。

---

## 任务 2: 硬件触发调试 — TRIG_MASK 默认值问题

### 进展和结果
TRIG_MASK 寄存器上电默认值 = 全 1。testbench 只写了 word0 (bits 31:0)，word1/2 保持全 1。EQ 比较时高位总线信号非零 → 整个比较失败。显式清零所有 TRIG_MASK word 后硬件触发正常。

### 解决方法
```verilog
ila_write(FCAPZ_TRIG_MASK,     32'h0004_0000);   // word0
ila_write(FCAPZ_TRIG_MASK + 1, 32'h0);           // word1
ila_write(FCAPZ_TRIG_MASK + 2, 32'h0);           // word2
ila_write(FCAPZ_TRIG_MASK + 3, 32'h0);           // word3
ila_write(FCAPZ_TRIG_MASK_EXT,  32'h0);           // word4 (EXT 地址)
```

---

## 任务 3: TCP SYN 帧注入 → SYN+ACK 验证

### 进展和结果
在 testbench 中构造完整 TCP SYN 帧 (Ethernet + IP + TCP 含正确校验和)，通过 GMII 注入 FPGA。固件成功处理并返回 SYN+ACK (Flags=0x12)。

### 验证通过
- TCP Flags = 0x12 (SYN|ACK) ✓
- 源/目的 MAC、IP 正确交换 ✓
- FPGA ISN = 0x10000000, ACK_Num = 0x20000001 ✓

---

## 任务 4: ILA 探针全面扩展 (13 → 27 探针)

### 进展和结果
将 ILA 核 #0 从 13 探针 (96bit) 扩展到 27 探针 (151bit)，覆盖 GMII TX、MAC TX、CPU FIFO 脉冲、总线写数据、LED 等全部关键信号。

### 新增 14 个探针
gmii_tx_er, mac_tx_sop/en/data/eop/err, cpu_rd_empty, cpu_wr_full,
cpu_rd_rpkt_pop_ind, cpu_wr_wpkt_push_ind, cpu_wr_wen_ind, cpu_rd_ren,
bus_wdata (32bit), led_val (4bit)

---

## 任务 5: 修复仿真 VCD 生成 — 内嵌 testbench 冲突

### 进展和结果
仿真通过但主 tb_fast.vcd 不生成。根因是 RTL 模块内嵌的 testbench module 被 iverilog 当作额外顶层运行，其 `$dumpvars` 先于主 TB 的 `$dumpfile` 执行 → 主 VCD 被阻止。

### 解决方法
在 7 个含内嵌 testbench 的 RTL 文件中，用 `ifndef IVL_SIM` / `endif` 包裹内嵌 TB module。修改后 tb_fast.vcd 正常生成。

### 修改的文件
`dual_clock_simple_dual_port_ram.v`, `package_fifo_v2.v`, `gmii2mac.v`, `riscv32_top.v`, `uart.v`, `single_clock_true_dual_port_ram.v`, `single_clock_simple_dual_port_ram.v`

---

## 任务 6: ILA 标准模式重构

### 进展和结果
将项目 ILA 集成从自定义模式 (sim_mod mux + 专用仿真端口) 重构为标准模式，严格遵循 fpga_ila 快速入门指南的三要素结构。仿真验证通过。

### 修改的文件
`rtl/webserver_cpu_top.v`, `sim/tb_fast.v`

### 标准模式结构

```verilog
// ① 核总线 (多核共用 addr/wdata, 每核独立 we/re/rdata)
localparam ILA_NUM_CORES    = 1;
wire [ILA_NUM_CORES-1:0]     ila_we;
wire [ILA_NUM_CORES-1:0]     ila_re;
wire [15:0]                  ila_addr;
wire [31:0]                  ila_wdata;
wire [ILA_NUM_CORES*32-1:0]  ila_rdata;

// ② ILA 核 — 直连总线
soft_ila_top_fcapz #(...) u_ila_core0 (
    .reg_we        (ila_we[0]),
    .reg_re        (ila_re[0]),
    .reg_addr      (ila_addr),
    .reg_wdata     (ila_wdata),
    .reg_rdata     (ila_rdata[0*32 +: 32])
);

// ③ ILA Hub — 直连总线
ila_hub_top #(...) u_ila_debug (
    .core_reg_we    (ila_we),
    .core_reg_re    (ila_re),
    .core_reg_addr  (ila_addr),
    .core_reg_wdata (ila_wdata),
    .core_reg_rdata (ila_rdata)
);
```

---

## 任务 7: fpga_ila Web GUI 环境搭建

### 进展和结果
在 fpga_ila 安装目录创建 Python venv，安装全部依赖。Web 版已验证启动成功 (localhost:9527)，加载 signals.json 后可看到 27 个命名探针。

### 启动方式
```bash
cd /home/zhihuiw/fpga_work/Prj/RiscV_WebSoC_3
./ila_gui.sh web        # Web 版 (http://localhost:9527)
./ila_gui.sh            # Qt GUI 版
```

---

## 任务 8: Vivado 上板编译配置

### 进展和结果
修改 `build.tcl` 自动包含 fpga_ila RTL 文件 (22 个)，修改 `pins.xdc` 将板载 UART (L21/M21) 分配给 ILA 调试接口。标准模式代码适配 Vivado 综合 (修复 part-select 常量表达式)。

### 修改的文件
`build_xilinx/build.tcl` — 添加 fpga_ila 源文件自动发现
`build_xilinx/pins.xdc` — ILA UART 占用板载串口引脚
`rtl/webserver_cpu_top.v` — `reg_rdata` 去掉 part-select (Vivado 兼容)

### 遇到的问题
1. Vivado 综合报 `module 'soft_ila_top_fcapz' not found` — build.tcl 原来只加了 `../rtl/*.v`，没有 fpga_ila 文件
2. `part-select width could not be resolved to a constant` — Vivado 无法计算 `ila_rdata[0*32 +: 32]` 中的常量表达式，改为直接 `ila_rdata`
3. 板子只有一组 UART (L21/M21)，ILA 调试和 CPU 主串口无法同时使用

### 下一步
Vivado 生成 bitstream → 烧录 FPGA → Web GUI + 串口抓 ILA 波形

---

### ILA 标准模式使用问题总结

#### 问题 1: 标准指南省略了 `reg_re` / `core_reg_re` 端口

快速入门指南的示例代码只显示了 `.reg_we` / `.core_reg_we`，没有 `.reg_re` / `.core_reg_re`。但实际 RTL 中这两个端口是 `input`/`output` 声明，无默认值，**不连接会编译报错**。

**解决:** 标准模式也必须连接 `reg_re` 和 `core_reg_re`:
```verilog
soft_ila_top_fcapz:  .reg_re(ila_re[0])
ila_hub_top:         .core_reg_re(ila_re)
```

#### 问题 2: Web GUI 加载 signals.json 无反应

Web GUI 的 `_loadSignals()` 函数只拷贝 `name`/`probes` 字段，漏掉 `data_depth`/`num_probes`。后续 `_populateCoreSelect()` 用 `(c.data_depth || 0) > 0` 过滤死核，导致刚加载的核被当作死核丢弃，页面静默无反应。

**解决:** 修改 `index.html` 的 `_loadSignals()`，完整拷贝 `data_depth`、`num_probes`、`total_width`、`max_windows` 等字段。

#### 问题 3: Web GUI 默认波特率 115200 vs FPGA 921600

Web GUI 连接对话框默认波特率 115200，但标准模式下 `ILA_BAUD = 921600`。不匹配导致连接超时。

**解决:** 连接时手动改波特率为 921600，或修改 HTML 中默认值。

#### 问题 4: 仿真中 testbench 如何访问 ILA 寄存器

标准模式下 Hub 直接驱动核总线，没有仿真 bypass 路径。testbench 需要通过 `force` 语句覆盖总线信号:

```verilog
task ila_write(input [15:0] addr, input [31:0] data);
    @(posedge ila_clk);
    force dut.ila_we   = 1'b1;
    force dut.ila_addr = addr;
    force dut.ila_wdata = data;
    @(posedge ila_clk);
    force dut.ila_we   = 1'b0;
endtask
```

#### 问题 5: 上板前必须确认 FPGA 已烧录

Web GUI 通过 UART 发送 fcapz 协议命令，需要 FPGA 上的 ILA Hub 响应。如果 FPGA 未烧录 bitstream，串口能打开但 Hub 不响应 → 连接超时。

---

## 仿真运行

```bash
cd sim
make clean && make      # 编译
make run                # 仿真 → tb_fast.vcd
make wave               # gtkwave 看完整波形
```

## 硬件调试

```bash
./ila_gui.sh web        # Web 版 (http://localhost:9527)
./ila_gui.sh            # Qt GUI 版
```

> ⚠️ 硬件调试前提: FPGA 已烧录 bitstream + UART 连接 + 波特率设为 921600

---

> 更新日期: 2026-08-06

---

# 2026-08-05

> 状态: 第一阶段 Ping 通 ✅ | 第二阶段 TCP 开发中 (任务 7-9 完成, 任务 10-13 待完成)

### 参考资料

- [C代码编写技巧](C代码编写技巧.md) — .h/.c 文件编写规范、数据类型速查、声明与定义的区别
- [TCP协议基础学习笔记](TCP协议基础学习笔记.md) — TCP 报文格式、三次握手、状态机、校验和

---

## 第一阶段: Ping 通网络 ✅ 已完成

第一阶段跑了 3 个任务 + 修了 5 个 Bug, 最终 FPGA 能回复 ARP 和 ICMP。

### 任务 1: 搭建 iverilog 仿真环境

### 进展和结果
用 iverilog 搭了一个完整的仿真环境。FPGA 的 RTL 代码用了很多 Xilinx 原语(IDELAYE2、IDDR、ODDR、MMCM、xpm_memory), iverilog 不认识这些, 手写了行为级 Stub 来替换。仿真时 BFM 读 firmware.hex 模拟 JTAG 写指令 RAM, 然后 testbench 注入 ARP Request 帧到 RGMII 接口, 观察整个链路是否走通。仿真验证通过了: gmii_rx_dv 拉高 → mac_rx_sop/eop 脉冲 → cpu_rd_empty 拉低, 链路全通。固件也顺便编译过了, 2634 bytes。

### 仿真覆盖了哪些

```
tb.v (注入 ARP Request 帧到 GMII)
  → xilinx_stubs.v   (用行为级代码替换 Xilinx 原语)
  → vendor_stubs.v   (MMCM 时钟直通, 跳过 PLL)
  → lcpu_bfm.v       (读 firmware.hex, 模拟 JTAG 写固件到指令 RAM)
  → RTL 全部真实模块  (rgmii_gmii_bridge → gmii2mac → mac_top → cpu_channel → pktfifo → picorv32)
```

仿真的链路: 帧从 RGMII 进来 → IDDR 采样转 GMII → MAC 定界 → FIFO 缓存 → PicoRV32 读包 → C 固件处理 → 写回复包到 TX FIFO → MAC 发送 → ODDR 转 RGMII 输出。

仿真覆盖的是逻辑正确性, 不覆盖 PHY 配置、物理时序、UART、真实 JTAG。这些只能上板调。

### 遇到的问题

RGMII 驱动时序写错了。testbench 在 rgmii_rxc 同一边沿驱动 rgmii_rxd 数据, IDDR 采样时新旧值还没稳定, gmii_rx_dv 永远不拉高。改成 opposite-edge: negedge 设低 4 位, posedge 设高 4 位, 给 IDDR 留出一个半周期的稳定窗口。

iverilog 仿真非常慢, 因为 RTL 模块里内嵌了 testbench(gmii2mac、crc、fifo 等), 仿真时这些内部 testbench 也在跑, 占掉了大部分仿真时间。

### 仿真文件清单

```
sim/
├── Makefile           # 一键编译+运行
├── tb.v               # 主 testbench, 注入 ARP Request 帧
├── tb_fast.v          # 精简版 testbench (跳过内部自检, 仿真更快)
├── lcpu_bfm.v         # BFM, 从 firmware.hex 读固件模拟 JTAG 写入
├── xilinx_stubs.v     # Xilinx 原语行为级替换
└── vendor_stubs.v     # MMCM 时钟 Stub
```

使用 tb_fast.v 时需将 Makefile 中 `SIM_SRCS` 的 `tb.v` 改为 `tb_fast.v`。

运行:
```
cd sim
python3 ../c_build/tcl2hex.py ../tcl/InstructRAM.tcl firmware.hex   # 固件转 hex
make clean && make      # 编译
make run                # 跑仿真, 生成 tb.vcd
make wave               # GTKWave 看波形
```

---

### 任务 2: 上板验证 + 烧录

### 进展和结果
上板烧录后 LED 流水灯在跑, 证明 CPU 活着。PHY 的 Link LED 亮, ping 时 Activity LED 闪, 说明 PHY 收到了 PC 发的包。但 FPGA 不回复——折腾了很久才发现问题根本不在代码。

### 遇到的问题

最大的坑: 固件根本没烧进 FPGA。v3 的 build.tcl 只生成了纯 RTL 的 bitstream, PicoRV32 的指令 RAM 初始值全是 0, CPU 起来读到全零指令直接崩溃。v4 能跑是因为用了 updatemem 把 firmware.bin 嵌入 BRAM INIT。复制 v4 的 update_and_program.sh, 加一步 updatemem 把 bank*.mem 写入 bitstream, 烧录后 LED 自检正常——问题解决。

FPGA 软核和 STM32 不一样。STM32 烧录时自动把程序写进 Flash, FPGA 软核的 bitstream 里 BRAM 初始值是什么 CPU 起来就跑什么。这步理解错了, 浪费了很多时间。

---

### 任务 3: Ping 验证

### 进展和结果
修好烧录问题后, `ping 169.254.1.1` 一次成功。第一个包 1059ms(PC 先发 ARP 学 MAC), 后续稳定 2ms。全链路验证通过。

---

### 第一阶段修掉的 5 个 Bug

- 栈指针未初始化: PicoRV32 上电 sp=0, 第一个函数调用写 0xFFFFFFFC 跑飞。rename_entry 加 `la sp, _stack_top` 解决。
- .bss 段未清零: 绕过 picolibc CRT, 静态变量值不确定。加 bss 清零循环。
- 仿真延时过长: 5M 次 nop 循环在 iverilog 里跑 3 秒。加 SIM_FAST 宏跳过。
- BFM 复位卡死: timer 变量在 delay 和 OP_DONE 间复用。拆成两个独立计数器。
- RGMII 采样竞争: 同边沿驱动。改 opposite-edge。

---

## 第二阶段: TCP 协议实现

第二阶段从零写 TCP 协议栈。按照自底向上的顺序: 先定义数据结构和解析, 再做校验和, 最后实现握手状态机。

### 任务 7: 新建 tcp.c/tcp.h — 头文件和数据定义

##### 7a. 头文件和结构体

### 进展和结果
创建了 tcp.h(声明) 和 tcp.c(实现)。tcp.h 里定义了: 7 个 TCP 状态常量、6 个 Flag 宏、MAX_TCP_CONN=4、tcp_conn_t 结构体(8 个字段: state/remote_ip/remote_port/local_seq/remote_seq/remote_ack/retry_count/timeout)、extern 全局变量声明、API 函数原型。tcp.c 实现了连接表管理(tcp_init/find_conn/alloc_slot/free_slot)和 RX FIFO 读辅助函数(tcp_rd16/tcp_rd32)。

### 遇到的问题
tcp.h include 路径写错了——`#include "inc/lcpu_general.h"`。tcp.h 自己在 inc/ 目录下, 再加 inc 前缀找成了 inc/inc/lcpu_general.h, 编译挂了。

### 收获到的经验

.h 和 .c 的 include 路径规则不一样。编译时 Makefile 通过 -I 指定了 inc 目录, .c 文件里写 `#include "inc/xxx.h"` 能找到。但 .h 文件自己就在 inc 目录里, 互相 include 时直接写文件名不加路径, 加 inc/ 多了一层反而找不到。一个简单规则: inc 里的头文件互相 include 不加前缀, .c 文件 include inc/ 下的头文件才写 `#include "inc/xxx.h"`。

extern 只是声明不占内存。在 .h 里写 `extern uint32 src_ip;` 是告诉编译器"这个变量存在", 但必须在某一个 .c 里写 `uint32 src_ip;` 才能真正分配内存。忘了在 .c 里定义, 链接时就会报 undefined reference。

typedef 不分配变量。`typedef struct { int x; } Point;` 只是给类型起了名字, 并没有创建变量。初学者容易写成 `Point;`(啥也没做)或忘记写 `Point p;` 然后用 p 访问。

static 函数不写进 .h。加了 static 的函数只在当前 .c 文件内可见, .h 里不需要声明它, 其他 .c 文件也看不到。这是一种封装——把不需要暴露给外部的内部函数藏起来。

RX FIFO 和 TX FIFO 是不同的硬件。LCPU_RD_SET_ADDR 操作的是 RX 读指针, LCPU_WR_BYTE 操作的是 TX 写指针。已经从 RX 读了数据写到 TX 了, 就别想再从 TX 读回来了——硬件没这条路。

宏重复定义编译有 warning。TCP_SYN_RETRY_TICKS 和 HTTP_PORT 在 lcpu_general.h 和 tcp.h 各有一份, 删掉 tcp.h 的重复定义, 统一用 lcpu_general.h 的值。

注释风格统一。去掉了 `/* ====` 大块分隔线和任务编号注释, 改用单行 `//` 标注。`[cite: N]` 粘贴残留全部清掉。代码逻辑一行没改, 编译 0 warning 0 error。

---

##### 7b. TCP 报文解析

### 进展和结果
实现了 tcp_parse_header: LCPU_RD_SET_ADDR 跳到帧偏移 34(TCP 头起点), 用 tcp_rd16/tcp_rd32 逐字段读 SrcPort(2B)、DstPort(2B)、SEQ(4B)、ACK_Num(4B)、DataOffset(高4bit→头长度)、Flags(1B)、Window(2B)。TCP payload 长度 = ip_total_len - ip_header_len - 实际的 TCP 头长度。解析完自动调 tcp_find_conn 用 src_ip + tcp_src_port 查连接表, 返回匹配的连接槽索引。

### 遇到的问题
这一版没有编译错误, 但之前写的校验和函数 tcp_checksum_out 有个根本性错误——想从 RX FIFO 读取刚写到 TX FIFO 的数据。两个 FIFO 是独立硬件, 互相读不到。这个错误留到任务 8 重做。

### 收获到的经验

数组越界在嵌入式环境特别危险。`for (i = 0; i < MAX_TCP_CONN; i++)` 没问题, 但写成 `i <= MAX_TCP_CONN` 就多访问了一个元素。嵌入式没有 MMU 保护, 越界不会 segfault, 会静默破坏隔壁变量的值, 极难排查。

C 语言函数用 -1 表示"找不到"。tcp_find_conn 返回 -1 时, 调用方必须检查, 不检查就 `conn_table[-1]` 访问, 行为未定义。

---

##### 7c. TCP 报文发送通路

### 进展和结果
实现了 tcp_wr16/tcp_wr32——向 TX FIFO 写大端 16/32 位值。TCP 发包的顺序是: eth_proc 先预写 MAC 头(源 MAC + 交换 MAC) → ip_header_update 预写 IP 头(交换 IP + 重算 checksum) → TCP 层写 TCP 头字段(SrcPort→DstPort→SEQ→ACK→DataOffset+Flags→Window→Checksum→Urgent) → LCPU_WR_PUSH_PACKET 推送。握手包 54 字节 < 64, 补零到最小帧。

### 收获到的经验

网络字节序是大端——高位字节在前, 低位字节在后。`tcp_wr16(addr, 0x1234)` 写出来 addr 地址是 0x12, addr+1 是 0x34。如果误用 x86 的小端思维, 写成了 0x34 在前, Wireshark 看到的端口号就是错的, PC 内核直接丢弃。

以太网最小帧 64 字节。TCP 握手只有 54 字节不够, 末尾必须补零。忘了补零就是 runt frame, 很多交换机/PHY 会直接扔掉。

发包顺序非常重要——必须先写 MAC 头再写 IP 头再写 TCP 头。反过来就不行了, 因为 eth_proc 和 ip_header_update 操作的是 RX FIFO 的读指针, 而 TCP 发包函数操作的是 TX FIFO 的写指针——它们之间不共享地址空间, 但硬件协议栈要求数据在 TX FIFO 里按 MAC→IP→TCP 的顺序排列。

---

### 任务 8: TCP 校验和 + IP 分派

##### 8a. 校验和计算

### 进展和结果
重写了校验和函数, 新的 tcp_calc_checksum 完全基于函数参数传入的值——不读任何 FIFO。9 个参数: src_ip, dst_ip, src_port, dst_port, seq, ack, flags, window, payload_len。先算伪首部 6 次 cks_sum_cal, 再算 TCP 头 10 次 cks_sum_cal, 取反返回。发包函数在外面先调 tcp_calc_checksum 算出值, 再写 TCP 头时把校验和回填进去。

### 遇到的问题
上一版 tcp_checksum_out 的错误根源——想从 RX FIFO 的地址空间去读 TX FIFO 刚写入的数据。这两个 FIFO 硬件上就是独立的, 互访做不到。只能在写 TX 之前用纯寄存器参数算好校验和。

### 收获到的经验

校验和的伪首部不实际发送。12 字节伪首部(SrcIP + DstIP + Zero + Proto + TCP_Length)只参与 Checksum 计算, 一定不要写进 TX FIFO。很多初学者把它当作 TCP 头的一部分也写了进去, 导致 TCP 头多 12 字节。

校验和最后一步必须取反。`return ~sum` 不是 `return sum`。忘了取反, Wireshark 会显示 TCP Checksum Incorrect 的红色标记, PC 内核静默丢掉这个包——表面上"发出去没回复", 其实包发出去了但被对方丢了。

---

##### 8b. main.c 增加 TCP 分派

### 进展和结果
main.c 已加入 `#include "inc/tcp.h"`、`tcp_init()` 调用、主循环 `TCP_PROC` 分支。固件从 2634 → 4508 bytes (1127 words, 22.0%)。TCP 函数已链接进固件。

### 收获到的经验

连接 TCP 分派后固件涨了 1874 字节——链接器把 tcp.o 里被引用的符号(tcp_init、tcp_proc、tcp_parse_header、tcp_calc_checksum、tcp_send_syn_ack、tcp_send_rst、tcp_handle_listen、tcp_handle_syn_received 及它们依赖的 tcp_wr16/wr32/rd16/rd32、cks_sum_cal 等)全部拉进了最终 ELF。

仿真验证: tb_fast.v 从 ARP 帧去掉了多余的 8 字节前导码(GMII 层不需要), 帧注入延迟到 RTL 内部 testbench 全部跑完后(50ms), 避免内部自检模块抢读 RX FIFO。主固件已包含 TCP 代码, 仿真验证 ARP+ICMP 链路是否仍然正常。

---

### 任务 9: SYN 处理 — 回复 SYN+ACK

### 进展和结果
实现了三个核心函数: tcp_proc(主入口, 解析→分发), tcp_handle_listen(收到 SYN→分槽→回 SYN+ACK), tcp_handle_syn_received(收到 ACK→验证→进 ESTABLISHED)。整个握手流程: PC 发 SYN(DstPort=80) → FPGA tcp_parse_header 解析 → tcp_handle_listen 分配槽位、记录对方 IP 和端口和 SEQ、生成自己的 ISN=0x10000000 → tcp_send_syn_ack 构造回复包(Flags=SYN|ACK=0x12, ACK_Num=对方SEQ+1, Window=1460, Checksum 预计算) → LCPU_WR_PUSH_PACKET 发送。PC 收到后发 ACK → FPGA tcp_handle_syn_received 验证通过 → 连接进入 ESTABLISHED。

### 收获到的经验

SYN 也消耗一个序号——这是 TCP 初学者最容易写错的点。握手第二步的 ACK_Num 是对方的 SEQ+1, 不是 SEQ。因为 SYN 标志消耗了对方的序号, 下一个期望的字节序号就是 SEQ+1。忘了 +1, PC 的 TCP 栈会发现 ACK 号不对, 连接建立失败。

开发阶段固定 ISN 是可以的。正式 TCP 实现用随机 ISN(防攻击), 但调试时固定值(如 0x10000000)的好处是 Wireshark 里一眼就能认出来是自己发的包。

switch/case 的 default 不要空着。tcp_proc 的 switch 有 default: break, 但如果状态机意外进入未处理状态, 包被静默丢弃。至少该加个 LED 闪烁或调试输出提示异常。

网络调试助手发 SYN 前 PC 会先发 ARP。别奇怪 Wireshark 里先看到 ARP 再看到 TCP——这是正常的, PC 必须知道 FPGA 的 MAC 地址才能发 IP 包。如果之前 ping 过, ARP 表有缓存, 就跳过这一步。

---

---

> 更新日期: 2026-08-05

---

# 2026-08-04

> 状态: 第一阶段启动 — 仿真环境搭建 + TCP 协议学习

---

## 任务 6: 学习 TCP 协议基础

### 进展和结果

TCP 报文格式: 固定头 20 字节, SrcPort(2)+DstPort(2)+SEQ(4)+ACK_Num(4)+DataOffset(4bit)+Flags(9bit)+Window(2)+Checksum(2)+Urgent(2)。六个 Flag: FIN(0x01) SYN(0x02) RST(0x04) PSH(0x08) ACK(0x10) URG(0x20)。TCP 校验和需 12 字节伪首部 (SrcIP+DstIP+Zero+Proto+TCP_Length), 覆盖伪首部+TCP头+Payload。三次握手: PC SYN(SEQ=x) → FPGA SYN+ACK(SEQ=y,ACK=x+1) → PC ACK(SEQ=x+1,ACK=y+1)。关键: SYN 和 FIN 各占一个序号, ACK_Num = 对方SEQ+1。状态机: CLOSED→LISTEN→SYN_RECEIVED→ESTABLISHED。连接表用并行数组, 避结构体偏移。详细笔记见 [TCP协议基础学习笔记](TCP协议基础学习笔记.md)。

### 收获到的经验

TCP 校验和伪首部是虚拟的不实际发送, 与 IP/ICMP 校验和的关键区别。项目现有 cks_sum_cal() 可复用。

---

> 更新日期: 2026-08-04
